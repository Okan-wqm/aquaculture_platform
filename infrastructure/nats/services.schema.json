{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://aquaculture.dev/schemas/nats-services.json",
  "title": "NATS Per-Service Identity + ACL Registry",
  "description": "Single source of truth for NATS per-service authentication. Validates infrastructure/nats/services.yaml. Consumed by the CI invariant test nats-invariants.spec.ts.",
  "type": "object",
  "required": ["version", "services"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Schema version. Bump only on breaking shape changes; add migration notes in ADR-015."
    },
    "services": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/service"
      },
      "description": "Ordered list of NATS services. Order is preserved by the nats.conf generator for auditable diffs."
    }
  },
  "definitions": {
    "service": {
      "type": "object",
      "required": ["name", "application", "description", "publish", "subscribe"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_-]*[a-z0-9]$",
          "minLength": 3,
          "maxLength": 40,
          "description": "NATS user identifier. MUST exactly match the mTLS client cert CN for this service (verify_and_map maps CN \u2192 user). Lowercase snake_case."
        },
        "application": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*[a-z0-9]$",
          "minLength": 3,
          "maxLength": 64,
          "description": "Unique apps/<directory> runtime that owns this certificate identity. APP_TO_SERVICE and deploy invariants derive from this field; identities may not be shared."
        },
        "description": {
          "type": "string",
          "minLength": 10,
          "description": "One-line summary of what this service publishes/consumes. Appears in ADR and runbook generation."
        },
        "publish": {
          "type": "array",
          "minItems": 3,
          "items": {
            "$ref": "#/definitions/subject"
          },
          "description": "Subjects this service is permitted to publish to. JetStream applications use scoped API subjects; stream mutation is reserved for nats_stream_provisioner."
        },
        "subscribe": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/subject"
          },
          "description": "Subjects this service is permitted to subscribe to. JetStream request replies use _INBOX.>; core subscriptions are explicitly enumerated."
        }
      }
    },
    "subject": {
      "type": "string",
      "pattern": "^(events\\.|telemetry\\.|dlq\\.|quarantine\\.|commands\\.|request\\.|config\\.runtime\\.|config\\.marine_credentials\\.|sensor\\.|st\\.language\\.|policy\\.|\\$JS\\.API\\.|_INBOX\\.|_INBOXBILLINGCFG\\.|_INBOXFARMMARINECFG\\.)[A-Za-z0-9_.*>-]+$",
      "minLength": 3,
      "maxLength": 120,
      "description": "NATS subject pattern in a canonical platform namespace. The legacy AQUACULTURE_EVENTS. prefix is structurally rejected because it is a stream name, never a publish subject. Secret-bearing request/reply paths use distinct first-token inboxes: `_INBOXBILLINGCFG.` is restricted to billing_service and config_service, while `_INBOXFARMMARINECFG.` is restricted to farm_service and config_service, so the platform-wide `_INBOX.>` grant cannot read either reply stream."
    }
  }
}
