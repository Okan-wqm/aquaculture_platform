# Enterprise-v2 Agent System Audit — 2026-04-16

**Mode:** CATCHER (audit-style, repository-wide)
**Scope:** `.claude/agents-enterprise-v2/**`, `.claude/knowledge/**`, `.claude/agents-enterprise-v2/_shared/**`, `CLAUDE.md`, `orchestrator.md` routing alignment vs real repo surface.
**Triggered by:** user request "kod sistemiyle ne derecede uyumlu ya da uzman sensin her açıdan kontrol et".

## Executive summary

Enterprise-v2 agent sistemi yüksek kaliteli içeriğe sahip (converted ajanlar gerçek incident kanıtlarına ve ADR'lara yaslanıyor) ancak operasyonel olarak ~%40 phantom altyapıya bağımlı: `.claude/skills/`, `tools/gates/`, `docs/reviews/_registry/`, `root-cause-auditor` agent — hiçbiri repo'da mevcut değil. Ayrıca üç paralel agent dizini (`agents/`, `agents-enterprise-v2/`, `test-agents/`) `name:` frontmatter çakışmasıyla yaşıyor; routing tablosu repo'nun 13 path'ini boş bırakıyor; 3 ownership çakışması pair-review invariant'ını kararsız kılıyor; 4 SSoT drift TEACHER çıktılarını potansiyel olarak yanlış yönlendiriyor.

Faz 0 düzeltmeleri (bu commit) SİSTEM BOZAN parçaları kapatır: SSoT drift (P0-1), routing coverage (P0-2), ownership çakışmaları (P0-3), CLAUDE.md servis sayısı drift (P0-4). Phantom altyapı inşası + dizin konsolidasyonu sonraki fazlara bırakıldı (bkz. `/root/.claude/plans/abstract-brewing-mochi.md`).

## Findings (by severity)

### CRITICAL

#### P0-1 — `createTenantQueryKey` imzası knowledge'da tersine yazılmış

**Severity:** CRITICAL
**Layer:** 1 (tech — React SSoT)
**State:** RESOLVED (bu commit)

**Evidence**
- `.claude/knowledge/layer-1-react.md:36` (pre-fix): `createTenantQueryKey(queryKey, tenantId)` — **yanlış parametre sırası**
- `web/shared-ui/src/utils/tenant-query-keys.ts:39` (gerçek): `createTenantQueryKey(tenantId: string, ...segments: readonly unknown[])` — `tenantId` FIRST parametre

**Rule violated**
SSoT ile kod gerçeği arasında imza drift. TEACHER mode output'u agent'ı invoke eden kullanıcıya yanlış örnek verir; bu hata tenant cache bleed üretir (FE-CRITICAL-001 sınıfı).

**Proposed fix direction**
- layer-1-react.md:36 imza düzelt (DONE)
- Phase 4 `knowledge-ssot.spec.ts` invariant'ı knowledge iddialarını gerçek kod'a karşı otomatik doğrulasın (PENDING)

**Expected closer**
Bu commit + Phase 4 invariant testi.

### HIGH

#### P0-2 — Orchestrator routing tablosu repo'nun 13 path'ini kapsamıyor

**Severity:** HIGH
**Layer:** 3 (ADR — orchestrator prompt'un kendi kuralı)
**State:** RESOLVED (bu commit)

**Evidence**
- `orchestrator.md:105` kuralı: "Every changed file MUST map to at least one primary agent. Any unmatched path is a PROCESS HIGH ownership gap."
- Kuralın ihlali: `apps/db-migrate/**`, `libs/shared-contracts/**`, `scripts/**` (ci, nats, deploy), `docs/**` (adr, runbooks, reviews, research, architecture, security, api, guides), `nx.json`, `tsconfig.base.json`, `jest.config.*`, `.claude/knowledge/**`, `.claude/allowlists/**`, `.claude/skills/**` (gelecek), `tools/gates/**` (gelecek), `CLAUDE.md`.

**Rule violated**
Orchestrator kendi "unmatched = PROCESS HIGH" kuralını her review döngüsünde tetikler. Ayrıca `apps/db-migrate/` (16. servis) ve `libs/shared-contracts/` (yeni lib) sahipsiz — PR'lar anonim geçiyor.

**Proposed fix direction**
- Orchestrator routing tablosuna 14 yeni glob ekle (DONE)
- Phase 4 `orchestrator-routing-coverage.spec.ts` invariant'ı repo top-level'ının tam kapsamını CI-lock yapsın (PENDING)

**Expected closer**
Bu commit + Phase 4 invariant testi.

#### P0-3 — 3 ownership çakışması (pair-review invariant'ı kararsız)

**Severity:** HIGH
**Layer:** 3 (ADR — agent pair-review invariant)
**State:** RESOLVED (bu commit)

**Evidence**
- `platform/libs/outbox/**` — data-expert.md:34 + messaging-expert.md:32 + orchestrator.md:96 üç farklı "primary" atıf
- `libs/backend-common/src/database/**` — data-expert.md full + multi-tenant-saas-expert.md:30-31 partial primary (etiketsiz)
- `libs/backend-common/src/redis/**` — auth-security-expert full + multi-tenant-saas-expert.md:33 partial primary (etiketsiz)

**Rule violated**
`_shared/operating-modes.md:39,63` pair-review invariant: TEACHER→WRITER farklı agent instance'a gitmeli. Çakışan primary sahiplerinde orchestrator routingi deterministic değil.

**Proposed fix direction**
- `_shared/handoff-protocol.md`'a **primary / secondary reviewer / delegated** grammar ekle (DONE)
- outbox primary = data-expert, messaging = secondary (DONE)
- multi-tenant database + redis alt-path'leri "delegated from data-expert / auth-security-expert" etiketle (DONE)
- Phase 4 `agent-ownership-uniqueness.spec.ts` invariant'ı her glob için tek primary'yi CI-lock yapsın (PENDING)

**Expected closer**
Bu commit + Phase 4 invariant testi.

### MEDIUM

#### P0-4 — CLAUDE.md "15 services" ve layer-3-adrs.md "4 misfiled" sayı drift

**Severity:** MEDIUM
**Layer:** 3 (ADR index doğruluk)
**State:** RESOLVED (bu commit)

**Evidence**
- `CLAUDE.md:51`: "Backend Services (`apps/`) — 15 services" — gerçek 16 (`apps/db-migrate/` eklenmiş, dökümante edilmemiş)
- `layer-3-adrs.md:31`: "Four files under `docs/architecture/ADR-01{0,1,2,3}-*.md`" — gerçek 5 dosya (ADR-010 iki variant: AI-REVIEW + AI-SELF-LEARNING)
- `layer-1-typeorm.md:32`: "2 messaging migrations still carry session-scope `search_path`" — sayı doğrulanamadı (gerçek daha fazla olabilir)

**Rule violated**
SSoT sayısal iddiaları gerçek kod/dizin state'inden drift; agent'lar bu sayıları evidence olarak alıntılar ve yanlış rapor üretir.

**Proposed fix direction**
- CLAUDE.md `db-migrate` satırı ekle + "16 services" güncelle (DONE)
- layer-3-adrs.md "Five files" ile açıkla (DONE)
- layer-1-typeorm.md count-agnostic reformat (DONE; DATA-HIGH-003 tam enumeration'a referans verir)

**Expected closer** Bu commit.

### Phantom infrastructure (not resolved — sonraki fazlar)

**P0-5** (CRITICAL/HIGH sınıfı, RESOLVED değil) — 21 phantom artefakt agent prompt'larında referanslı ama repo'da yok:
- `tools/gates/{tier-claim-lint,banned-phrase,commit-msg-validator,finding-registry,migration-sql-lint}.ts` (Phase 2)
- `.claude/skills/{add-entity-field,change-event-contract,add-shared-table,add-rls-policy,provision-tenant,pre-migration-restore-test,run-migration-prod}.md` + README (Phase 3)
- `docs/reviews/_registry/findings.jsonl` + schema (Phase 6)
- `.github/workflows/{finding-state-sweep,closes-footer-check,quality-gates,rule-health-report}.yml` (Phase 2/6/7)
- `tests/invariants/{upcaster-chain,knowledge-ssot,orchestrator-routing-coverage,agent-ownership-uniqueness,three-store-invariants}.spec.ts` (Phase 4)
- `.claude/agents-enterprise-v2/root-cause-auditor.md` (Phase 5)

**P0-6** (HIGH) — Üç agent dizini konsolide değil. `.claude/agents/` (20 legacy) + `.claude/agents-enterprise-v2/` (24) + `.claude/test-agents/` (28). `settings.json`'da dizin seçici yok; `name: data-expert` frontmatter üç yerde aynı. (Phase 0.1, user onayı ile).

**P0-7** (HIGH) — 10 agent hâlâ >200 satır cap ihlâli (security-reviewer 317, orchestrator 308, implementation-planner 279, frontend-expert 246, platform-services 218, hr-expert 197, database-reviewer 192, context-manager 184, admin-expert 177, prompt-writer 175). W3 conversion wave in-flight (Phase 1).

## Cross-domain dependencies flagged

Hiçbiri architectural-arbiter'a eskale edilmedi; tümü tek-ajan-ownership fix pattern'i.

## Verdict

**CONDITIONAL PASS** — P0-1 CRITICAL + P0-2/P0-3 HIGH bu commit ile kapandı. P0-5/P0-6/P0-7 açık kalan HIGH sınıfı bulgular; `/root/.claude/plans/abstract-brewing-mochi.md` Phase 1-7 bunları sırayla kapatır. Deploy blocker değil (agent sistemi operasyonel — sadece tam enterprise-grade değil).

## References

- Plan: `/root/.claude/plans/abstract-brewing-mochi.md` (bu audit'ten çıkan konsolidasyon planı)
- Master plan: `/root/.claude/plans/declarative-riding-shamir.md` W3 wave (in-flight)
- ADR-011 schema ownership, ADR-015 NATS cert-is-identity (SSoT'un kendi prensibi: drift yasak)
- Prior cycles: `docs/reviews/_audit/2026-04-W16-*.md` (9 slice audit dosyası)
