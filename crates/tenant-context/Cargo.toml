[package]
name = "tenant-context"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Compile-time tenant pinning — TenantId, SchemaName newtypes + Scoped<'t,T> lifetime brand. Cross-tenant access is structurally impossible (PhantomData / GhostCell pattern)."

[lib]
path = "src/lib.rs"

# [dependencies] intentionally empty during Faz 0 PR-A scaffold; uuid +
# serde + thiserror land in Faz 2 with the first concrete TenantId type.

[lints]
workspace = true
