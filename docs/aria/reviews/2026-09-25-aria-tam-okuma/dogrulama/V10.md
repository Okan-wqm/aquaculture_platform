<!-- ARIA-CURRENT-STATE-NOTICE: İkinci tur adversarial doğrulama raporu (2026-09-25, main @ 19efd07f,
aria/state @ e6f462fb); docs/aria/CURRENT_STATE.md ve executable contracts'a tabidir. Ham kanıt
kaydıdır, runtime otoritesi değildir. -->

# V10: B10 adversarial doğrulaması (ikinci tur)

Kapsam: `docs/aria/reviews/2026-09-25-aria-tam-okuma/B10.md` (39 dosya: state_store_lifecycle_arcs …
workflow_contract_registry). Ek sahiplik: ana kayıt §8.2-S4, §8.2-S5, §3.3 adaptör/tool_registry
satırları, §2 K7, §1 canlı registry durumları. Kod: `main @ 19efd07f` (dal HEAD ffe0d4a6). Canlı
durum: `origin/aria/state @ 06b4ba4a` (2026-09-25 13:12 +0200). MIMARI'nin andığı `e6f462fb` ile
`tools/registry.json` arasında fark yok. Yöntem: her iddia için kod yeniden açıldı. Gerektiğinde tüm
repo grep'lendi (aria-kernel, tools/, .github/, scripts/, infrastructure/). Salt-okuma `python3 -c`
sondaları yalnız saf fonksiyonlarla yapıldı (`canonical_command_satisfied_by`, `matches_glob`,
`lower_effort`, `classify_pressure`, `read_strict_jsonl` scratchpad dosyasıyla,
`WORKFLOW_CONTRACTS`, lifecycle sabitleri, telemetri adları).

## Özet

|                      | Sayı   |
| -------------------- | ------ |
| Kontrol edilen iddia | **84** |
| DOĞRULANDI           | **66** |
| KISMEN               | **14** |
| ÇÜRÜTÜLDÜ            | **2**  |
| DOĞRULANAMADI        | **2**  |

(Sayım: B10 tutarsızlık tablosu 22 + ölü kod listesi 15 + risk listesi 17 + dosya notlarından
seçilmiş 14 alt iddia + ana kayıt/MIMARI'deki grup iddiaları 16.)

## Çürütülen ve kısmen doğru iddialar (öncelikli)

| #   | Kaynak                                                                   | İddia                                                                                                                                                         | Gerçek                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Kanıt file:line                                                                                                                           | Verdikt                                |
| --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 1   | B10-10; B10 §11                                                          | Şema 3 çapraz inceleme köprüsü plan metinlerini `<untrusted_*>` içine **base64** ile sarar, düz metin değil                                                   | Çapraz inceleme ve primary-revision prompt'ları iki plan metnini **düz, kaçışsız** biçimde `<untrusted_primary_plan>` / `<untrusted_challenger_plan>` içine gömer. Base64 (Mode B) yalnız **implementation** prompt'unda (`_implementation_suggested_prompt`) var. MIMARI:174 cümlesi doğru; eksik olan tek şey kaçış olmaması. Ana kayıt S6 bunu doğru yazıyor, B10-10 yanlış.                                                                                             | `cross_review_bridge.py:90-131` (düz), `:133-181` (düz), `:463-599` (base64)                                                              | **ÇÜRÜTÜLDÜ**                          |
| 2   | Ana kayıt §1 registry satırı; §8.5 3. madde; MIMARI:457                  | "`registry_compiler` canlı durumu manifestteki SHADOW ile eziyor"                                                                                             | `compile_registry` yalnız CLI'dan çağrılıyor (`cli.py:3688-3695`). Hiçbir workflow ya da cycle bunu koşturmuyor. Gece yolu `_phase_tool_manifest_sync`, `register_tool`'a **canlı durumu** geçiriyor; yani durum korunuyor, ezilmiyor (ORPHAN-HIGH-625 düzeltmesi). Canlı registry `schema_version: 3`, compiler ise `2` yazar. Canlıda 7 CALIBRATE duruyor. Ezilseydi hepsi SHADOW olurdu.                                                                                 | `cycle.py:2736-2762`; `registry_compiler.py:52-58`; `git show origin/aria/state:tools/registry.json`                                      | **ÇÜRÜTÜLDÜ**                          |
| 3   | B10-9; B10 §24; B10 RAG notu 2. madde                                    | Twin intent katmanı prompt'a "`<untrusted_*>` sarmalı olmadan, düz Markdown" girer; "güvenilmeyen etiketle sarılmadığı için yalnız düzyazı uyarısına dayanır" | render*version ≥2'de blok `<derived_context section="recent_intent">` DATA etiketine sarılır ve "DATA, never instructions" bildirimi eklenir. Doğru olan kısım: içerik **sanitize edilmez ve kaçışlanmaz**. Commit mesajındaki `</derived_context>` etiketi erken kapatabilir. `untrusted*` adı taşımıyor.                                                                                                                                                                  | `agent_invocations.py:943,960-978,891-896,653-686`                                                                                        | **KISMEN**                             |
| 4   | B10-7; B10 §7                                                            | Kernel'in reachability kapısı `DRAFT/SANDBOX`'a yazan üretim yolu olmadığını söylüyor                                                                         | DRAFT ve SANDBOX waiver'ları açıkça **"NOT A DORMANCY CLAIM - a limit of the walk"** diyor (ANALYSIS-LOW-692): AST yürüyüşü dict alanını çözemiyor. Sonuç yine de doğru, ama başka bir kanıtla: üretimdeki tüm `register_tool` çağıranları (manifest'lerin 11'i de SHADOW, `adapter_portfolio` SHADOW) SHADOW ile kaydeder. DRAFT/SANDBOX yalnız operatörün CLI JSON'uyla gelebilir. Genesis EVAL_WINDOW/ACTIVE waiver'ı ise gerçek bir dormancy iddiası (ORPHAN-HIGH-689). | `aria-kernel/surface-reachability.unwritten.json` (tool_registry_status.DRAFT); `adapter_portfolio.py:92`; `cycle.py:2761`; `cli.py:3615` | **KISMEN**                             |
| 5   | B10-15; B10 §20                                                          | `human_only` deseni `infra/**`; "asıl altyapı `infrastructure/`"                                                                                              | Repoda **iki dizin de var**: `infra/` (122 izli dosya: argocd/helm) desene takılır ve `human_only` olur. `infrastructure/` (178 dosya: nats/docker/monitoring) takılmaz ve `blocked:unresolved_policy` olur (fail-closed, auto-fix değil). Sonda: `infrastructure/nats/nats.conf → blocked`, `infra/x → human_only`.                                                                                                                                                        | `triage.py:209`                                                                                                                           | **KISMEN**                             |
| 6   | B10 risk 9; B10-3 ("her 30 s tikte")                                     | Çöken executor her 30 s'lik tikte yeniden claim edilir, sınırsız                                                                                              | Durum daha kötü: scheduler yalnız `no_pending` ve `provider_cooldown` sonrasında uyur. `executor_failed` sonrasında **hiç beklemeden** hemen sonraki iterasyona geçer. Orkestratör drain'i `max_iterations_per_phase` ile sınırlı, yani tur başına sınırlı, turlar arası sınırsız.                                                                                                                                                                                          | `autonomous_worker_scheduler.py:17-18,189-235`; `worker_dispatch_hook.py:391-411`; `autonomy_orchestrator.py:1668,1692`                   | **KISMEN**                             |
| 7   | B10 risk 1; §17; MIMARI:457 "Masum bir adaptör de karantinaya düşebilir" | Koşu sırasında daemon'lar `aria-tools/`'a yazarsa `scope_out_mutations` dolar ve adaptör anında karantinaya alınır                                            | Mekanizma doğru, fakat overlay sabit olarak `<workspace>/aria-tools`'u hash'liyor. CI'da gerçek store `.aria-state-store/tools` (gitignore'lu). Workspace'teki `aria-tools/` yalnız izli statik dosyalar içeriyor. Risk yalnız tools root'un workspace `aria-tools` olduğu host'larda (lokal/droplet) geçerli. Canlıda 108 koşunun hiçbiri scope-out ile karantinaya düşmedi (107 ok, 1 budget_exceeded).                                                                   | `tool_runner.py:487-510`; `state_store.py:102`; `.gitignore:22,48`; `.github/actions/restore-aria-state/action.yml:164-172`               | **KISMEN**                             |
| 8   | Ana kayıt §8.2-S4 ("timeout yakalamadan"); B10-17; B10 §35               | `verification_gate` timeout'u yakalamaz                                                                                                                       | `verify_worker_result` `TimeoutExpired`'ı gerçekten yakalamıyor. Ancak tek üretim çağıranı olan hook bunu geniş bir `except Exception` ile yakalıyor ve `worker_dispatch_verification_error` yazıyor. Durum `executor_failed` oluyor, **claim bırakılmıyor ve doğrulama satırı yazılmıyor**, atama da reaper'a kadar `picked_up`'ta kalıyor. S4'ün geri kalanı (tam env, sandbox yok, `--allow-legacy-no-token`) doğru.                                                     | `verification_gate.py:303-309`; `worker_dispatch_hook.py:413-432`                                                                         | **KISMEN**                             |
| 9   | B10 §20                                                                  | Triage "`cycle.py:1377` **ve** `learning.py:100` — aynı cycle'da iki yol"                                                                                     | Cycle'daki `triage` fazı **yalnız `burn_in`** modunda koşar. Standart gece cycle'ında triage yalnız learning hook'undan gelir. Canlı: `origin/aria/state`'te `tools/triage/` yok.                                                                                                                                                                                                                                                                                           | `cycle.py:3260-3266`; `learning.py:100`                                                                                                   | **KISMEN**                             |
| 10  | B10 §9                                                                   | task.py yorumu ("5 mission") ile §1'deki "117 olay" çelişiyor                                                                                                 | Bu bir çelişki değil, tarihsel yorum: yorum eski `next_action=title` yolunun ürettiği 5 mission'ı anlatıyor. Kodun bugünkü davranışı bundan etkilenmiyor.                                                                                                                                                                                                                                                                                                                   | `task.py:15-35`                                                                                                                           | **KISMEN**                             |
| 11  | B10 §36                                                                  | Dış watchdog "aria/state tipi ≥50 saat hareketsiz" olduğunda issue açar                                                                                       | Tip yaşının yanında producer (`aria-auto-cycle`) ve executor (`aria-agent-executor`) lane'lerinin son başarı yaşları da 50 saat eşiğiyle izleniyor                                                                                                                                                                                                                                                                                                                          | `.github/manifests/aria-state-watchdog.json` (`stateBranch`, `lanes[]`)                                                                   | **KISMEN**                             |
| 12  | B10 §4                                                                   | strict_jsonl_reader'ı "18 üretim modülü kullanıyor; `read_strict_jsonl(` çağrılarının 11'i tolerant"                                                          | AST sayımı: **17 modül, 24 çağrı, bunların 18'i `on_corruption="tolerant"`**. Beyansız yolda non-dict yield iddiası sondayla doğrulandı: `[{'a':1},[1,2],5]` döndü.                                                                                                                                                                                                                                                                                                         | `aria_kernel/**/*.py` AST; `strict_jsonl_reader.py:177-197`                                                                               | **KISMEN**                             |
| 13  | B10 §17 / risk 16                                                        | Oto-CALIBRATE `transition_tool`'u atlar ve `surface_reachability`'nin "tek yazıcı" iddiasını bozar                                                            | Doğru. Ek olarak, surface_reachability'nin dayandığı öncül de yanlış: "`_update_tool_internal` refuses a bare status change". Reddeden fonksiyon `update_tool`. `_update_tool_internal` korumasız birleştirme yapar.                                                                                                                                                                                                                                                        | `surface_reachability.py:129-133`; `tool_registry.py:1170-1199,1226-1231`; `tool_health.py:264-283`                                       | **KISMEN** (öz doğru, öncül de yanlış) |
| 14  | B10 §24                                                                  | twin `_git` timeout'suz ve "cycle fazı asılabilir"                                                                                                            | Timeout yok: doğrulandı. "Asılabilir" ise bir çıkarım: faz SIGALRM kapsamı bu turda ölçülmedi.                                                                                                                                                                                                                                                                                                                                                                              | `twin.py:885-888`                                                                                                                         | **KISMEN**                             |

DOĞRULANAMADI (2): suppression_scanner'da `++ x` içerikli eklenen satırın dosya yolunu değiştirmesi
(`:261-289`; ayrıştırıcı dalı okundu, sonda yapılmadı); telemetride "HELP/TYPE satırı yok"
(`_prometheus` yalnız değer satırı üretiyor, dışa aktarımın başka bir katmanda başlık ekleyip
eklemediği izlenmedi).

## Ana kayıt / şema dokümanı için gereken düzeltmeler

1. **Ana kayıt §1, `tools/registry.json (canlı)` satırı, Yorum sütunu.** Mevcut: "`lint-rules`
   kayıtlı değil; manifestlerin hepsi SHADOW der, `registry_compiler` canlı durumu ezer" Önerilen:
   "`lint-rules` kayıtlı değil. Manifestlerin 11'i de SHADOW der, ama gece `tool_manifest_sync` fazı
   `register_tool`'a **canlı durumu** geçirir (`cycle.py:2736-2762`); `registry_compiler` yalnız
   CLI'da ve üretimde koşmuyor. Tüm satırlarda `last_transition` boş, çünkü yeniden kayıt satırı
   tümden değiştiriyor."
2. **Ana kayıt §8.5, 3. madde:** "…**canlı registry** 7 CALIBRATE · 2 SHADOW · 1 QUARANTINED,
   lint-rules kayıtsız (B11)." cümlesi doğru ve kalsın. Aynı düzeltme MIMARI:457'ye uygulanmalı:
   "`registry_compiler` canlı durumu manifestteki SHADOW ile eziyor" → "manifest senkronu canlı
   durumu koruyor (`register_tool` + canlı status); `registry_compiler` üretimde çağrılmıyor".
3. **MIMARI:457**, "Masum bir adaptör de karantinaya düşebilir…" cümlesi → "Tools root'un workspace
   `aria-tools/` olduğu host'larda, koşu sırasında aynı dizine yazan başka bir süreç
   `scope_out_mutations`'ı doldurup adaptörü anında karantinaya alabilir. CI'da store
   `.aria-state-store/` altında ve overlay onu hiç görmüyor (bkz. kaçırılan #2)."
4. **B10.md B10-10 satırı** kaldırılmalı veya şöyle düzeltilmeli: "Şema 3 cümlesi doğru: çapraz
   inceleme plan metinlerini `<untrusted_*>`'a **kaçışsız düz metin** olarak sarar
   (`cross_review_bridge.py:123-129`). Base64 yalnız implementation prompt'unda (`:593-599`)."
   Sınıf: EKSİK-DOKÜMAN (kaçış yok), ÇÜRÜTÜLMÜŞ-İDDİA.
5. **B10.md B10-9 / §24 / RAG notu:** "`<untrusted_*>` sarmalı ve sanitizasyon olmadan düz Markdown"
   → "`<derived_context section="recent_intent">` DATA etiketiyle sarılı, ama sanitize edilmiyor ve
   kaçışlanmıyor. Commit mesajındaki `</derived_context>` etiketi erken kapatabilir. Şema 5'in
   '`<untrusted_*>` etiketli' ifadesi bu blok için yanlış etiket adı veriyor." Ana kayıt S6'daki
   "twin intent katmanı kaçışsız gömülür" ifadesi doğru ve değişmesin.
6. **B10.md B10-7:** kanıt cümlesi değişmeli: "DRAFT/SANDBOX waiver'ı dormancy iddiası değil
   (ANALYSIS-LOW-692). Üretimde bu durumlarla kayıt yapan yol yok, çünkü tüm üretim kayıtları SHADOW
   (`adapter_portfolio.py:92`, manifest'ler)."
7. **B10.md B10-15:** "asıl altyapı `infrastructure/**` eşleşmez" → "`infra/` (argocd/helm) eşleşir;
   `infrastructure/` (nats/docker/monitoring) eşleşmez ve `blocked:unresolved_policy` olur
   (fail-closed)."
8. **Ana kayıt §8.2-S4:** "timeout yakalamadan koşar" → "…`TimeoutExpired`'ı kendisi yakalamaz.
   Hook'un geniş `except`'i yakalar ama claim'i bırakmaz ve doğrulama satırı yazmaz. Ayrıca
   doğrulama komutlarını worker'ın **kendi submit ettiği** listeden alır; submit yalnız alt küme
   kontrolü yapar, dolayısıyla `required_tests`'in bir kısmı atlanabilir
   (`verification_gate.py:176-182,303`)." Worker dispatch yolunun canlıda hiç koşmadığı da not
   düşülmeli (`tools/dispatch/*` aria/state'te yok).
9. **B10.md risk 9 / B10-3:** "her 30 s tikte" → "scheduler `executor_failed` sonrasında uyumaz ve
   hemen yeniden claim eder (`autonomous_worker_scheduler.py:189-235`). Orkestratörde drain başına
   `max_iterations_per_phase` ile sınırlı."
10. **B10.md §20:** "aynı cycle'da iki yol" → "standart cycle'da yalnız learning hook; cycle
    `triage` fazı yalnız burn_in (`cycle.py:3263-3266`)."
11. **§2 K7:** doğru, ama bir ek önerilir: "Tüketim anı doğrulaması HMAC + tool_id bağından ibaret.
    Payload'da süre ya da nonce kontrolü yok, bu yüzden token süresiz ve tekrar kullanılabilir (S8
    ile tutarlı)." (`adapter_calibration.py:168-203`)

## Doğrulanan iddialar

- B10-1 prune no-op: `worker_dispatch.py:403` yalnız `{completed,cancelled,expired}` durumlarını
  budar. Reducer bunları üretmez (`:855-937`), üretici grep'i boş döndü. Tek çağıran CLI
  `worktree-prune` (`cli.py:4350`). `.github`/`scripts`/`tools`'ta çağrı yok.
- B10-2 retry 3 → yalnız governance satırı: `worker_dispatch_hook.py:507-525`,
  `record_human_required` yok.
- B10-3 retry sayacı yalnız `verification_gate_failed`'ı sayar: `worker_dispatch.py:760-781`
  (cadence düzeltmesi için bkz. KISMEN #6).
- B10-4 `create_dispatch_request` yalnız CLI'dan çağrılır (`cli.py:4206,4216`). Otonom yol v2
  satırını kendisi yazar (`promotion_controller.py:152-184`).
- B10-5 merge literali: `worker_dispatch_hook.py:450-470`. `merged` durumuna ulaşılamaz.
- B10-6 / S5 karantinadan kaçış: `promote_tool` yalnız `CALIBRATE→SHADOW`'da fixture ister
  (`promotion.py:22-23`). `transition_tool` yalnız `QUARANTINED→CALIBRATE`'i kısıtlar
  (`tool_registry.py:1397-1401`). CLI `--target-status` seçenekleri `SHADOW|ACTIVE` (`cli.py:1038`).
- B10-8 watchdog kaynağı: `watchdog_freeze.py:1-35`, `merge_authority.py:29,59`.
  `aria_watchdog.py`'de freeze/merge yok. Manifest yorumu "01:13", cron ise `13 17 * * *`
  (`aria-auto-cycle.yml:66`).
- B10-11 telemetri: 43 farklı `aria_*` adı (sonda). `aria_agent_fitness_score` toplanıyor
  (`telemetry.py:111-112,225-229`).
- B10-12 workflow sözleşmeleri: 10 workflow, 11 job, 4 exclusion (sonda). Executor 510 dk
  (`aria-agent-executor.yml:148`), cycle 360 dk (`aria-auto-cycle.yml:173`). Docstring "3
  aria-kernel\*" diyor, gerçekte 1 (`:20-36`).
- B10-13 surface_reachability: 5 yüzey (`:96-169`), docstring "four" diyor (`:20-26`).
  `undeclared_written_members` var (`:184-210`).
- B10-14 `next_action` konmayabilir: `task.py:15-35,376-385`.
- B10-16 matris kapısı: EXISTENCE tüm repoyu glob'luyor (`validation_matrix_gate.py:328-341`). Nx
  dışı gereksinimde alt-dize eşlemesi (`:474-478`). Risk türü yoksa yalnız hash doğrulanıyor
  (`:630-653`, `validation_runs_ledger.py:526-552` status bakmıyor). `test:schema-invariants`
  script'i Lane-A izin listesinde yok (`validation.py:1001-1013`, `package.json:128`).
- B10-17 verification gate env: `verification_gate.py:307`'de `env=` yok ve sandbox yok.
- B10-18 adaptör tam env ile koşar: `tool_runner.py:132-147`.
- B10-19..22: tur bütçesi 120 ve tavan 400, docstring "60" (`turn_budget_policy.py:17,64-83`).
  `lower_effort('low')→'medium'` (sonda). 3 gecelik seri (`tool_degradation.py:65`). Kilit sabitleri
  300/5400/2700/900 (sonda).
- Ölü kod: `run_all_validators` yalnız testte; `tests/invariants/aria-surface-manifest.spec.ts` yok.
  `service_map_v1_to_v2` yalnız testte, `discovery.py:208` docstring. `explain_task`/`latest_tasks`
  hiç çağrılmıyor (testler dahil). `consecutive_degraded_cycles` yalnız testte.
  `_workspace_snapshot(_raw)` çağrılmıyor. `_latest_request_states` ve `recover_orphan_governance`
  çağıransız. `cancel`/`mark_picked_up` etkisiz ve `already_cancelled` dalına ulaşılamaz.
  `aria_suppression_honor` yalnız `aria-tools/*suppression*.json`'u arıyor
  (`suppression_scanner.py:102-105`). `if: always()` regex'i yok. `self_features` pilotu.
  `validate_file`/`verify_workflow_registry` 2026-10-09 waiver'lı, diğer 6 kontrol 2026-10-20
  waiver'lı. `batch-containment.waivers.json` yok.
- Riskler 2, 3 (ts-node `any(part…)` `validation.py:967-969`; unittest kısıtsız `:973-974`), 4
  (sonda: `--base=HEAD --head=HEAD` ve `--exclude=*` → True), 5 (`apply_engine.py:746-755`
  `change_author_identity` geçmiyor), 6, 7, 8 (`promotion.py:106`), 10 (reaper `submitted`'ı canlı
  sayıyor: `worker_dispatch.py:53-58,1104`; executor timeout `lease+60`:
  `worker_dispatch_hook.py:302`), 11 (`trailer_scan.py:27,83-94`), 12 (`trust.py:14-19`,
  `feedback.py:315-319`), 13 (`typed_judgment.py:527-560`: `quote.strip()` boş değilse ve alt-dize
  ise geçerli), 15 (sonda: `apps/x/.env`, `a/secrets/k` eşleşmiyor), 16 (`register_tool`
  SHADOW/ACTIVE→QUARANTINED/ARCHIVED ve SHADOW→CALIBRATE geçişlerini engellemiyor:
  `tool_registry.py:985-1010`), 17 (`turn_budget.py:246-251`).
- Ana kayıt §2 **K7**: `tool_registry.py:1444-1466` → `verify_auto_promote_token` →
  `hmac.compare_digest` (`adapter_calibration.py:200`). NASIL:248'deki "henüz wired değil" cümlesi
  gerçekten bayat.
- §3.3: 11 manifest var ve 11'i de SHADOW (manifest taraması); `panel_approval_token` üçüncü yetki
  (`tool_registry.py:1355-1383`); auto_promote `enabled:false`, 0.95, yalnız autonomous
  (`data/genesis_policy_default.json:6-15`; `aria-config/genesis_policy.json` override etmiyor).
- §1 canlı registry: 10 araç, 7 CALIBRATE, 2 SHADOW, 1 QUARANTINED (agent-harness-security-adapter),
  ACTIVE yok, lint-rules yok (`origin/aria/state@06b4ba4a`).
- §8.3 prune ve merge literali; §8.4 watchdog, telemetri, workflow satırları; MIMARI:213 notu
  (worker kısmı); MIMARI:677 notu (matris kısmı).
- `verification_gate._resolve_active_claim_for_submit` sıralama yapmıyor (`:65-68`).
  `--allow-legacy-no-token` lease kontrolünü atlıyor (`:186`, `cli.py:2852,5827`).

## Birinci turun kaçırdıkları

1. **Worker kendi doğrulama komutlarını seçiyor (alt küme yeterli).** `submit_worker_result` yalnız
   `any(command not in required)` kontrolünü yapıyor, yani gönderilen listenin `required_tests`'in
   alt kümesi olması yetiyor (`verification_gate.py:176-182`). `verify_worker_result` ise isteğin
   `required_tests`'ini değil, worker'ın gönderdiği `result["validation_commands"]` listesini
   koşuyor (`:303`). Worker gerekli komutlardan yalnız birini göndererek diğerlerini atlayabilir.
   (Boş liste gönderirse `required_tests`'e düşülüyor, bu yüzden yalnız kısmi atlama mümkün.)
2. **CI'da scope-out tespiti gerçek store'a kör.** `_aria_tools_dir_overlay` sabit olarak
   `<workspace>/aria-tools`'u hash'liyor (`tool_runner.py:487-510`). CI'da tools root
   `.aria-state-store/tools`, ve bu dizin gitignore'lu (`.gitignore:22`, `state_store.py:102`).
   Dolayısıyla git status da o dizini göstermiyor. CI'da bir adaptörün gerçek ledger'lara yazması
   `scope_out_mutations`'a hiç düşmüyor. Plan ARIA-V2 §3.4'ün "ledger mutasyonları görünür kalır"
   sözleşmesi bu yolda sağlanmıyor.
3. **Worker dispatch yolu canlıda hiç koşmadı.** `origin/aria/state`'te `tools/dispatch/` dizini
   yok, `tools/triage/` ve `tools/economy/` de yok. B10-1…B10-5, S4 ve risk 9-10 gizli (latent)
   kusurlar; canlı etkileri yok. Önem sırası buna göre ayarlanmalı.
4. **`transition_tool` governance satırı yazmıyor.** Yalnız `last_transition` alanını güncelliyor
   (`tool_registry.py:1479-1490`). Gece manifest senkronu da bu alanı siliyor (canlıda 10 satırın
   hepsinde `last_transition: {}`). Sonuç olarak araç yaşam döngüsü geçişlerinin (SHADOW, CALIBRATE,
   ACTIVE) kalıcı bir denetim izi yok. Yalnız karantina (`quarantine` defteri) ve oto-CALIBRATE
   (`calibration.jsonl`) iz bırakıyor.
5. **`ACTIVE→ACTIVE` ve `ACTIVE`'ten herhangi bir duruma demotion kontrolsüz.**
   `transition_tool`'daki ACTIVE dalı yalnız `current == "SHADOW"` için eşik ve yetki kontrolü
   yapıyor. `current == "ACTIVE"` iken hiçbir kontrol çalışmıyor (`tool_registry.py:1403-1476`). Bu
   küçük bir bulgu; B10-6'nin "genel matris yok" tespitini destekliyor.
