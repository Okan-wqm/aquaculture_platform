# =============================================================================
# Aquaculture Platform - DigitalOcean Droplet (4 CPU / 8GB RAM)
#
# All services enabled. Total memory budget: ~7GB
#
# Usage:
#   docker compose -f docker-compose.droplet.yml pull
#   docker compose -f docker-compose.droplet.yml up -d
# =============================================================================

# SEC-013: Trust boundary — all inter-service traffic (AUTH headers, GraphQL queries,
# NATS messages) travels on the aqua-internal Docker bridge network in plain HTTP/TCP.
# aqua-internal is marked internal: true so it cannot route to the internet.
# If a container escape occurs, traffic on this network is readable by other containers.
# For stronger isolation, consider a service mesh (e.g., Traefik mTLS or Consul Connect).

networks:
  aqua-network:
    driver: bridge
  aqua-internal:
    driver: bridge
    internal: true

volumes:
  postgres_data:
  redis_data:
  nats_data:
  minio_data:
  mosquitto_data:
  mosquitto_log:

services:
  # ===========================================================================
  # Infrastructure
  # ===========================================================================

  postgres:
    # Pinned to the exact version running on this droplet's data volume.
    # NEVER change this to a lower version — PostgreSQL will refuse to start
    # if the data was created with a newer TimescaleDB version.
    # To upgrade: pg_dump → destroy volume → start with new image → restore.
    image: timescale/timescaledb:2.25.1-pg16
    container_name: aqua-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aquaculture}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
      POSTGRES_DB: ${POSTGRES_DB:-aquaculture}
      POSTGRES_SHARED_BUFFERS: 512MB
      POSTGRES_EFFECTIVE_CACHE_SIZE: 1536MB
      POSTGRES_WORK_MEM: 16MB
      POSTGRES_MAINTENANCE_WORK_MEM: 128MB
      # SEC-015: Per-service DB passwords (passed to 00-init-schemas.sh)
      # If not set, random passwords are generated at first init.
      AUTH_SERVICE_DB_PASS: ${AUTH_SERVICE_DB_PASS:-}
      FARM_SERVICE_DB_PASS: ${FARM_SERVICE_DB_PASS:-}
      SENSOR_SERVICE_DB_PASS: ${SENSOR_SERVICE_DB_PASS:-}
      BILLING_SERVICE_DB_PASS: ${BILLING_SERVICE_DB_PASS:-}
      HR_SERVICE_DB_PASS: ${HR_SERVICE_DB_PASS:-}
      ALERT_SERVICE_DB_PASS: ${ALERT_SERVICE_DB_PASS:-}
      ADMIN_SERVICE_DB_PASS: ${ADMIN_SERVICE_DB_PASS:-}
      GATEWAY_SERVICE_DB_PASS: ${GATEWAY_SERVICE_DB_PASS:-}
      NOTIFICATION_SERVICE_DB_PASS: ${NOTIFICATION_SERVICE_DB_PASS:-}
      HYDROPONICS_SERVICE_DB_PASS: ${HYDROPONICS_SERVICE_DB_PASS:-}
      MESSAGING_SERVICE_DB_PASS: ${MESSAGING_SERVICE_DB_PASS:-}
      # IP-1: PostgreSQL SSL — entrypoint wrapper copies certs to $PGDATA
      # with correct postgres:postgres ownership + chmod 600 on key.
      POSTGRES_SSL: "on"
    entrypoint: ["/usr/local/bin/postgres-ssl-entrypoint.sh"]
    command: >
      postgres
      -c max_connections=300
      -c search_path=public,auth,admin
      -c ssl=on
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/docker/init-scripts:/docker-entrypoint-initdb.d:ro
      - ./certs/postgres:/var/lib/postgresql/ssl:ro
      - ./infrastructure/docker/scripts/postgres-ssl-entrypoint.sh:/usr/local/bin/postgres-ssl-entrypoint.sh:ro
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 1536M
          cpus: '1.0'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aquaculture}"]
      interval: 15s
      timeout: 5s
      retries: 5

  # ---------------------------------------------------------------------------
  # db-init: Idempotent role password sync (initContainer pattern)
  #
  # Runs ONCE after postgres is healthy, then exits.
  # Ensures per-service DB roles have the passwords from the current .env,
  # even when postgres was initialized with randomly-generated passwords.
  # restart: no — intentional, this is a one-shot init container.
  # ---------------------------------------------------------------------------
  db-init:
    image: timescale/timescaledb:2.25.1-pg16
    container_name: aqua-db-init
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGUSER: ${POSTGRES_USER:-aquaculture}
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      PGDATABASE: ${POSTGRES_DB:-aquaculture}
      AUTH_SERVICE_DB_PASS: ${AUTH_SERVICE_DB_PASS:-}
      FARM_SERVICE_DB_PASS: ${FARM_SERVICE_DB_PASS:-}
      SENSOR_SERVICE_DB_PASS: ${SENSOR_SERVICE_DB_PASS:-}
      BILLING_SERVICE_DB_PASS: ${BILLING_SERVICE_DB_PASS:-}
      HR_SERVICE_DB_PASS: ${HR_SERVICE_DB_PASS:-}
      ALERT_SERVICE_DB_PASS: ${ALERT_SERVICE_DB_PASS:-}
      ADMIN_SERVICE_DB_PASS: ${ADMIN_SERVICE_DB_PASS:-}
      GATEWAY_SERVICE_DB_PASS: ${GATEWAY_SERVICE_DB_PASS:-}
      NOTIFICATION_SERVICE_DB_PASS: ${NOTIFICATION_SERVICE_DB_PASS:-}
      HYDROPONICS_SERVICE_DB_PASS: ${HYDROPONICS_SERVICE_DB_PASS:-}
      MESSAGING_SERVICE_DB_PASS: ${MESSAGING_SERVICE_DB_PASS:-}
    command: >
      bash -c "
        update_role() {
          local role=$$1 pass=$$2
          if [ -n \"$$pass\" ]; then
            psql -c \"ALTER ROLE $$role WITH PASSWORD '$$pass'\" 2>/dev/null && echo \"[db-init] $$role password updated\" || echo \"[db-init] $$role role not found, skipping\"
          fi
        }
        update_role auth_service      \"$$AUTH_SERVICE_DB_PASS\"
        update_role farm_service      \"$$FARM_SERVICE_DB_PASS\"
        update_role sensor_service    \"$$SENSOR_SERVICE_DB_PASS\"
        update_role billing_service   \"$$BILLING_SERVICE_DB_PASS\"
        update_role hr_service        \"$$HR_SERVICE_DB_PASS\"
        update_role alert_service     \"$$ALERT_SERVICE_DB_PASS\"
        update_role admin_service     \"$$ADMIN_SERVICE_DB_PASS\"
        update_role gateway_service   \"$$GATEWAY_SERVICE_DB_PASS\"
        update_role notification_service \"$$NOTIFICATION_SERVICE_DB_PASS\"
        update_role hydroponics_service  \"$$HYDROPONICS_SERVICE_DB_PASS\"
        update_role messaging_service    \"$$MESSAGING_SERVICE_DB_PASS\"
        echo '[db-init] Done'
      "
    networks:
      - aqua-internal

  redis:
    # Pinned 2026-04-05. To update: verify changelog at https://github.com/redis/redis/releases, test in staging first.
    image: redis:7-alpine
    container_name: aqua-redis
    restart: unless-stopped
    # IP-1: Redis TLS ready but disabled for phased rollout.
    # Phase 1 (current): plaintext — ensures deploy stability
    # Phase 2 (next deploy): enable TLS by mounting redis-tls.conf + certs, switching to rediss://
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD required} --maxmemory 192mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=${REDIS_PASSWORD} redis-cli ping"]
      interval: 15s
      timeout: 5s
      retries: 5

  nats:
    # Pinned 2026-04-05. To update: verify changelog at https://github.com/nats-io/nats-server/releases, test in staging first.
    image: nats:2.10.24-alpine
    container_name: aqua-nats
    restart: unless-stopped
    # ARCH-010/SEC-H01: Use config file for JetStream limits + authentication.
    # CLI flags replaced with nats.conf which includes authorization{} block.
    # NATS_USER and NATS_PASS are substituted by NATS from container environment.
    command: ["--config", "/etc/nats/nats.conf"]
    environment:
      # SEC-H01: NATS credentials for authenticated broker access.
      # SEC-NEW01: Mandatory credentials — no default fallback allowed in any environment.
      # Generate with: openssl rand -base64 32
      NATS_USER: ${NATS_USER:-nats_internal}
      NATS_PASS: "${NATS_PASS:?NATS_PASS is required}"
    volumes:
      - nats_data:/data
      - ./infrastructure/docker/nats/nats.conf:/etc/nats/nats.conf:ro
      - ./infrastructure/docker/nats/nats-tls-enabled.conf:/etc/nats/nats-tls.conf:ro
      - ./certs/nats:/etc/nats/certs:ro
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 192M
          cpus: '0.25'
    healthcheck:
      # SEC-H01: Use HTTP monitoring endpoint (enabled in nats.conf http_port: 8222)
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 6

  mosquitto:
    build: ./infrastructure/mosquitto
    container_name: aqua-mosquitto
    restart: unless-stopped
    # Inject MQTT_AUTH_SECRET into mosquitto config at startup (replaces placeholder)
    entrypoint: ["/bin/sh", "-c", "sed -i \"s|__MQTT_AUTH_SECRET__|$MQTT_AUTH_SECRET|g\" /mosquitto/config/mosquitto.conf && exec mosquitto -c /mosquitto/config/mosquitto.conf"]
    environment:
      MQTT_AUTH_SECRET: ${MQTT_AUTH_SECRET:?MQTT_AUTH_SECRET required}
    volumes:
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.15'
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 1883 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  minio:
    # Pinned 2026-04-05. To update: verify changelog at https://github.com/minio/minio/releases, test in staging first.
    image: quay.io/minio/minio:RELEASE.2025-04-03T14-56-28Z
    container_name: aqua-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_USER:?MINIO_USER is required}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD:?MINIO_PASSWORD is required}
    volumes:
      - minio_data:/data
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ===========================================================================
  # Backend Services
  # ===========================================================================

  gateway-api:
    image: ghcr.io/okan-wqm/aquaculture_platform/gateway-api:latest
    container_name: aqua-gateway
    restart: unless-stopped
    environment:
      SERVICE_NAME: gateway-api
      NODE_ENV: production
      GATEWAY_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=384'
      # M-RL-01: Redis-backed rate limiting — prevents multi-replica bypass
      RATE_LIMIT_USE_REDIS: "true"
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-1h}
      AUTH_SERVICE_URL: http://auth-service:3000/graphql
      FARM_SERVICE_URL: http://farm-service:3000/graphql
      SENSOR_SERVICE_URL: http://sensor-service:3000/graphql
      ALERT_SERVICE_URL: http://alert-engine:3000/graphql
      HR_SERVICE_URL: http://hr-service:3000/graphql
      BILLING_SERVICE_URL: http://billing-service:3000/graphql
      NOTIFICATION_SERVICE_URL: http://notification-service:3000/graphql
      HYDROPONICS_SERVICE_URL: http://hydroponics-service:3000/graphql
      ADMIN_SERVICE_URL: http://admin-api-service:3000/graphql
      CONFIG_SERVICE_URL: http://config-service:3000/graphql
      MESSAGING_SERVICE_URL: http://messaging-service:3000/graphql
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: "false"
      MINIO_ACCESS_KEY: ${MINIO_USER:?MINIO_USER is required}
      MINIO_SECRET_KEY: ${MINIO_PASSWORD:?MINIO_PASSWORD is required}
      MINIO_BUCKET: ${MINIO_BUCKET:-aquaculture}
      MINIO_REGION: us-east-1
      TRUST_PROXY: "1"
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      WS_CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      THROTTLE_DEFAULT_LIMIT: 500
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      nats:
        condition: service_healthy
      # Subgraph services: wait for healthy (GraphQL endpoint ready) before gateway starts.
      # This prevents introspection failures caused by subgraphs still bootstrapping.
      auth-service:
        condition: service_healthy
      farm-service:
        condition: service_healthy
      sensor-service:
        condition: service_healthy
      alert-engine:
        condition: service_healthy
      billing-service:
        condition: service_healthy
      hr-service:
        condition: service_healthy
      hydroponics-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      # ARCH-GW-001: notification-service and messaging-service are federated subgraphs
      # (listed in RetryableIntrospectAndCompose in app.module.ts). They MUST be healthy
      # before gateway starts, otherwise supergraph composition fails and blocks NestJS
      # startup entirely — preventing even /health/live from responding.
      notification-service:
        condition: service_healthy
      messaging-service:
        condition: service_healthy
      # Non-subgraph services: service_started is sufficient (gateway doesn't introspect them)
      admin-api-service:
        condition: service_started
      observability-service:
        condition: service_started
    networks:
      - aqua-network
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health/live >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  auth-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/auth-service:latest
    container_name: aqua-auth
    restart: unless-stopped
    environment:
      SERVICE_NAME: auth-service
      NODE_ENV: production
      AUTH_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=384'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false"
      DATABASE_LOGGING: "true"
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-1h}
      SUPER_ADMIN_EMAIL: ${SUPER_ADMIN_EMAIL:-admin@localhost.dev}
      SUPER_ADMIN_PASSWORD: ${SUPER_ADMIN_PASSWORD:?SUPER_ADMIN_PASSWORD is required}
      # MFA & WebAuthn (optional -- MFA disabled if not set)
      MFA_ENCRYPTION_KEY: ${MFA_ENCRYPTION_KEY:-}
      WEBAUTHN_RP_ID: ${WEBAUTHN_RP_ID:-app.suderra.com}
      WEBAUTHN_RP_NAME: ${WEBAUTHN_RP_NAME:-AquaCulture Platform}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  farm-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/farm-service:latest
    container_name: aqua-farm
    restart: unless-stopped
    environment:
      SERVICE_NAME: farm-service
      NODE_ENV: production
      FARM_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 5
      # ARCH-V11-008: Farm-service is the heaviest backend (71 entities, 25+ modules,
      # GraphQL federation schema, CQRS handler discovery). Under 256MB heap and 0.5 CPU,
      # NestJS bootstrap + TypeORM metadata loading + Apollo schema build can take 60-90s.
      # 120s start_period prevents premature unhealthy marking during cold start.
      start_period: 120s

  sensor-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/sensor-service:latest
    container_name: aqua-sensor
    restart: unless-stopped
    environment:
      SERVICE_NAME: sensor-service
      NODE_ENV: production
      SENSOR_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      MQTT_AUTH_MODE: http
      MQTT_AUTH_SECRET: ${MQTT_AUTH_SECRET:?MQTT_AUTH_SECRET required}
      MQTT_SENSOR_SERVICE_HASH: ${MQTT_SENSOR_SERVICE_HASH:?MQTT_SENSOR_SERVICE_HASH is required}
      MQTT_USERNAME: sensor_service
      MQTT_PASSWORD: ${MQTT_SENSOR_SERVICE_PASSWORD:?MQTT_SENSOR_SERVICE_PASSWORD is required}
      # SEC-C02: No hardcoded default — credential encryption key must be provided externally
      CREDENTIAL_ENCRYPTION_KEY: ${CREDENTIAL_ENCRYPTION_KEY:?CREDENTIAL_ENCRYPTION_KEY is required}
      MQTT_BROKER_HOST: app.suderra.com
      MQTT_BROKER_PORT: 8883
      MQTT_BROKER_URL: mqtt://mosquitto:1883
      EDGE_AGENT_GITHUB_REPO: Okan-wqm/aquaculture_platform
      PROVISIONING_API_BASE_URL: ${PROVISIONING_API_BASE_URL:-https://app.suderra.com}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mosquitto:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  admin-api-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/admin-api-service:latest
    container_name: aqua-admin-api
    restart: unless-stopped
    environment:
      SERVICE_NAME: admin-api-service
      NODE_ENV: production
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      ADMIN_CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
      FRONTEND_URL: ${FRONTEND_URL:-https://app.suderra.com}
      THROTTLE_DEFAULT_LIMIT: 500
      THROTTLE_DEFAULT_TTL: 60
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
      - aqua-network  # needs internet for external SMTP email delivery
    deploy:
      resources:
        limits:
          memory: 384M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  alert-engine:
    image: ghcr.io/okan-wqm/aquaculture_platform/alert-engine:latest
    container_name: aqua-alert
    restart: unless-stopped
    environment:
      SERVICE_NAME: alert-engine
      NODE_ENV: production
      ALERT_ENGINE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  billing-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/billing-service:latest
    container_name: aqua-billing
    restart: unless-stopped
    environment:
      SERVICE_NAME: billing-service
      NODE_ENV: production
      BILLING_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  hr-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/hr-service:latest
    container_name: aqua-hr
    restart: unless-stopped
    environment:
      SERVICE_NAME: hr-service
      NODE_ENV: production
      HR_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  hydroponics-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/hydroponics-service:latest
    container_name: aqua-hydroponics
    restart: unless-stopped
    environment:
      SERVICE_NAME: hydroponics-service
      NODE_ENV: production
      HYDROPONICS_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  notification-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/notification-service:latest
    container_name: aqua-notification
    restart: unless-stopped
    environment:
      SERVICE_NAME: notification-service
      NODE_ENV: production
      NOTIFICATION_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
    networks:
      - aqua-internal
      - aqua-network  # needs internet for external SMTP email delivery
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  observability-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/observability-service:latest
    container_name: aqua-observability
    restart: unless-stopped
    environment:
      SERVICE_NAME: observability-service
      NODE_ENV: production
      PORT: 3009
      NODE_OPTIONS: '--max-old-space-size=256'
      # INTERNAL_API_KEY must be set; guard rejects all requests in production without it
      INTERNAL_API_KEY: ${OBSERVABILITY_INTERNAL_API_KEY:?OBSERVABILITY_INTERNAL_API_KEY required}
      # observability-service reads DB_* env vars (not DATABASE_*) — see app.module.ts
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: ${POSTGRES_USER:-aquaculture}
      DB_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      # Observability reads cross-service metrics from the main aquaculture DB
      # (sensor, auth, alert, farm schemas). No own entities — schema-based isolation.
      DB_NAME: aquaculture
    depends_on:
      postgres:
        condition: service_healthy
    # Attached to aqua-internal only — no published ports, not reachable from external network
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3009/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  config-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/config-service:latest
    container_name: aqua-config
    restart: unless-stopped
    environment:
      SERVICE_NAME: config-service
      NODE_ENV: production
      PORT: 3000
      CONFIG_SERVICE_PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: "false" # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_URL: redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      CONFIG_ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:?INTERNAL_SERVICE_SECRET required}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  messaging-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/messaging-service:latest
    container_name: aqua-messaging
    restart: unless-stopped
    environment:
      SERVICE_NAME: messaging-service
      NODE_ENV: production
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=384'
      DATABASE_HOST: postgres
      DATABASE_SSL: "true"
      DATABASE_SSL_REJECT_UNAUTHORIZED: "false"
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: 'false'
      DATABASE_MIGRATIONS_RUN: 'true'
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      REDIS_DB: '3'
      NATS_URL: tls://nats:4222
      NATS_AUTH_USER: ${NATS_AUTH_USER:-nats_internal}
      # SEC-NEW01: NATS password must be provided via .env — no default fallback
      NATS_AUTH_PASS: "${NATS_AUTH_PASS:?NATS_AUTH_PASS is required}"
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-1d}
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: 'false'
      # SEC-NEW02: MinIO credentials must be provided — no default fallback for object storage access
      MINIO_ACCESS_KEY: ${MINIO_USER:?MINIO_USER is required}
      MINIO_SECRET_KEY: ${MINIO_PASSWORD:?MINIO_PASSWORD is required}
      TRUST_PROXY: 'true'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://aquamonitoring.net}
    networks:
      - aqua-internal
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      nats:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      # ARCH-HC-001: Use /health/live for Docker healthcheck (consistent with all other services).
      # /health/ready checks DB+Redis+NATS and returns 503 on partial failure — too strict for
      # Docker liveness. The gateway only needs messaging's GraphQL endpoint reachable, which
      # /health/live confirms (NestJS HTTP listener is bound = supergraph introspection will work).
      test: ['CMD', 'wget', '-q', '--spider', 'http://localhost:3000/health/live']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # ===========================================================================
  # Frontend Services
  # ===========================================================================

  shell:
    image: ghcr.io/okan-wqm/aquaculture_platform/shell:latest
    container_name: aqua-shell
    restart: unless-stopped
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  dashboard:
    image: ghcr.io/okan-wqm/aquaculture_platform/dashboard:latest
    container_name: aqua-dashboard
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  farm-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/farm-module:latest
    container_name: aqua-farm-module
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  sensor-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/sensor-module:latest
    container_name: aqua-sensor-module
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  admin-panel:
    image: ghcr.io/okan-wqm/aquaculture_platform/admin-panel:latest
    container_name: aqua-admin-panel
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  tenant-admin:
    image: ghcr.io/okan-wqm/aquaculture_platform/tenant-admin:latest
    container_name: aqua-tenant-admin
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  hr-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/hr-module:latest
    container_name: aqua-hr-module
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  hydroponics-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/hydroponics-module:latest
    container_name: aqua-hydroponics-module
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  aquamobil:
    image: ghcr.io/okan-wqm/aquaculture_platform/aquamobil:latest
    container_name: aqua-mobile
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
      - /var/run:uid=101,gid=101
      - /var/cache/nginx:uid=101,gid=101
    networks:
      - aqua-network
    deploy:
      resources:
        limits:
          memory: 64M

  # ===========================================================================
  # Reverse Proxy (Nginx)
  # ===========================================================================
  nginx:
    # Pinned 2026-04-05. To update: verify changelog at https://nginx.org/en/CHANGES, test in staging first.
    image: nginx:1.27.5-alpine
    container_name: aqua-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8883:8883"
    volumes:
      - ./infrastructure/nginx/droplet.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
    depends_on:
      - gateway-api
      - shell
      - mosquitto
    networks:
      - aqua-network
      - aqua-internal
    deploy:
      resources:
        limits:
          memory: 64M
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:80/nginx-health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
