# /aria-status

Show governed ARIA tool state and audit integrity.

```bash
PYTHONPATH=aria-kernel python3 -m aria_kernel --tools-dir aria-tools tool list
PYTHONPATH=aria-kernel python3 -m aria_kernel --tools-dir aria-tools integrity verify
```
