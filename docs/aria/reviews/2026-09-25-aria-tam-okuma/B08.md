<!-- ARIA-CURRENT-STATE-NOTICE: Tam-okuma ajan raporu (2026-09-25, main @ 19efd07f); docs/aria/CURRENT_STATE.md ve executable contracts'a tabidir. Ham kanıt kaydıdır, runtime otoritesi değildir. -->

# B08 — tam okuma raporu (planner_dispatch_hook … runtime_artifacts, 39 dosya, 17 155 satır)

Repo main @ 19efd07f, canlı durum `origin/aria/state @ e6f462fb`. Yollar aksi belirtilmedikçe `aria-kernel/aria_kernel/` altında. 39/39 dosya baştan sona okundu. Salt-okuma; yalnız bu rapor yazıldı (release_reason pariteyi doğrulamak için bir `python -c` içe-aktarma kontrolü koşuldu, test suite koşulmadı).

## Dosya envanteri

| Dosya                    | Satır | Ne yapar                                                                                                            | Üretimde bağlı mı?                                                                                               | Şema               |
| ------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------ |
| planner_dispatch_hook.py | 728   | Bekleyen planner isteğini claim edip istek-anchor worktree'sinde `ci_executor.py` alt sürecini koşar                | EVET `autonomy_orchestrator.py:1009-1012` → `autonomous_planner_dispatcher.py:128`                               | 4/5                |
| policy_approval.py       | 120   | L3 PR'ları için risk_owner + exception_owner onay satırları                                                         | EVET `merge_authority.py:81-88` (yalnız L3); kayıt CLI                                                           | 7/17               |
| pr_manager.py            | 966   | PR gövdesi, hard-fail perimetresi, `gh pr create`, manuel branch/commit/push şeridi                                 | EVET `implementation_delivery.py:641`, `cycle.py:3016` (dry-run); prepare/commit/push fiilen ölü (bkz. proposal) | 17                 |
| pr_tracking.py           | 484   | Merge olaylarını etki haritasına taşır, inançları/bulguları revalidation'a düşürür, change-intelligence zarfı basar | EVET `cycle.py:1627-1680`, `gateway/router.py:151`; `plan_incremental_cycle` ölü                                 | yok (12/14 civarı) |
| preflight.py             | 762   | Profil ön-koşulları (branch protection, sandbox, disk) + workflow-job preflight                                     | EVET `autonomy_orchestrator.py:1108`; 10 workflow inline                                                         | 19 (W3), 1         |
| pressure.py              | 1194  | Cycle baskısı (15 kaynak) + ayrı workspace baskı durum makinesi                                                     | EVET `cycle.py:1401,1492` + 8 modül                                                                              | 9                  |
| proactive_priority.py    | 142   | impact × opportunity araç sıralaması                                                                                | EVET cycle fazı, `task.py`                                                                                       | 9                  |
| product_fitness.py       | 183   | Ürünü 11 boyutta workflow sonuçlarıyla ölçer, yeşil-gece serisi                                                     | EVET `cycle.py:1158,3360`                                                                                        | yok                |
| progress.py              | 195   | Ajan stream-json olaylarının sanitize canlı akışı                                                                   | EVET `ci_executor.py:4620,4877`; CLI/MCP                                                                         | yok                |
| promotion.py             | 168   | SHADOW→ACTIVE üç otorite (operatör/auto-token/panel)                                                                | EVET `autonomy_orchestrator.py:879`                                                                              | 10                 |
| promotion_controller.py  | 294   | CONVERGED plan → dispatch satırı (WIP, kanıt sınıfı)                                                                | YALNIZ CLI `cli.py:4497`                                                                                         | 0/3                |
| promotion_panel.py       | 150   | Hazır adaptörler için panel sorusu açar, onayda veto penceresi kurar                                                | EVET cycle + `human_required_adjudication`                                                                       | 7/10               |
| promotion_veto.py        | 545   | 24 s operatör veto penceresi, panel token mint/verify, settle                                                       | EVET cycle (`_phase_tool_manifest_sync`), `tool_registry`                                                        | 7/10               |
| proposal.py              | 298   | Öneri ledger'ı, operatör/makine onay kolonu                                                                         | EVET `apply_engine.py:758,783`, `ci.py:324`, `self_improvement.py:308`; `approve_proposal` yalnız test           | 17                 |
| provider_cooldown.py     | 218   | Kota tükenmesi → sağlayıcı soğuma satırı ve admission reddi                                                         | EVET `ci_executor`, `worker_executor`, `claude_runtime`, `native_admission`                                      | 5                  |
| pytest_native_only.py    | 27    | pytest eklentisi: unittest.TestCase öğelerini düşürür                                                               | CI yalnız `scripts/ci/aria-suite-run.sh:49,57`                                                                   | yok                |
| quarantine.py            | 56    | Aracı QUARANTINED'e geçirip quarantine.jsonl yazar                                                                  | EVET `tool_health.py:251`                                                                                        | 7/10               |
| readiness.py             | 204   | Adaptör ACTIVE hazırlık engelleri                                                                                   | EVET promotion\*                                                                                                 | 10                 |
| readiness_proofs.py      | 1262  | 7 enterprise kanıt ailesinin üreticileri + claim birleştirici                                                       | YALNIZ CLI `cli.py:3487` ← `aria-readiness-claim.yml`                                                            | 7                  |
| recovery.py              | 212   | Dış-efekt intent/receipt ve yeniden-dispatch sınıflandırması                                                        | EVET `ci_executor.py:1724`; pr_manager, implementation_delivery                                                  | yok                |
| recursive_impact.py      | 1040  | 6 kaynaklı (grep tabanlı) etki grafiği                                                                              | YALNIZ CLI `cli.py:5109`                                                                                         | 14                 |
| reflection.py            | 1733  | Reflection satırı + günlük rapor + next_cycle_queue                                                                 | EVET cycle + orkestratör                                                                                         | 9                  |
| reflection_inputs.py     | 92    | Reflection kwarg kurucusu + warn-mode pedagogy lint                                                                 | EVET `autonomy_orchestrator`                                                                                     | 9/22               |
| registry_compiler.py     | 87    | Manifestlerden registry.json derler                                                                                 | YALNIZ CLI `cli.py:3688`                                                                                         | 10                 |
| release_reason.py        | 196   | Kapalı release-reason sözlüğü + fault_domain                                                                        | EVET `agent_invocations.py:4022`                                                                                 | yok                |
| report.py                | 494   | Günlük "anchor" frontmatter + reflection gövdesi                                                                    | EVET CLI ← `aria-daily-report.yml`; MCP                                                                          | 8                  |
| report_ingestion.py      | 441   | `docs/reviews/_registry/findings.jsonl` → feedback                                                                  | EVET `learning.py:96` — ama HER cycle ValueError                                                                 | yok                |
| request_worktree.py      | 126   | İstek-başı `aria-worktrees/req-<id>` worktree                                                                       | EVET planner hook, `ci_executor_drain.py:512`                                                                    | 4 (yok)            |
| research.py              | 245   | Dış kaynak fetch (SSRF korumalı)                                                                                    | YALNIZ CLI; `fitness.py:40` okur                                                                                 | 18                 |
| reverify.py              | 118   | Workspace baskılarını örnekleyip arşivler                                                                           | YALNIZ CLI `cli.py:4172`                                                                                         | 12                 |
| review_record.py         | 142   | Operatör inceleme kaydı                                                                                             | YALNIZ CLI `cli.py:4807`                                                                                         | 17                 |
| review_runner.py         | 480   | Gate B adversarial yargıç döngüsü                                                                                   | EVET orkestratör `review_runner` kwarg                                                                           | 16                 |
| risk_policy.py           | 213   | Değişen dosyaları L1/L2/L3/blocked şeridine sınıflar                                                                | EVET `merge_authority.py:69`, `auto_merge.py:177`, `human_required_adjudication.py:295`                          | 7                  |
| rollback_bundle.py       | 117   | Rollback bundle/simülasyon ledger + verify                                                                          | EVET `readiness_proofs`, `merge_authority`                                                                       | 7                  |
| rule_health.py           | 184   | Kural-başı TP/FP, kural karantinası, kusur bulgusu                                                                  | EVET `feedback_store.py:1218`, `cycle.py:1905`, reflection                                                       | 10                 |
| runner_attestation.py    | 264   | Runner ortam kanıtı (probe) + merge doğrulaması                                                                     | EVET `merge_authority`; probe ← `.github/actions/probe-runner-attestation`                                       | 7                  |
| runner_availability.py   | 241   | Self-hosted runner online mı (hosted preflight)                                                                     | EVET `python -m` ← `.github/actions/require-self-hosted-runner/action.yml:73`                                    | 21 (yok)           |
| runs_reader.py           | 102   | runs.jsonl strict okuyucu + upcast                                                                                  | EVET 13 modül                                                                                                    | 6/10               |
| runtime_artifacts.py     | 2632  | Koşu artefaktları, doğrulama (verified/retained/compacted), retention, v2 promosyonu, autonomy özeti                | EVET tool_runner, promotion_controller, state_store/integrity, CLI                                               | 15/6               |

## Doküman tutarsızlıkları (yalnız YENİ)

| #   | Doküman iddiası                                                                                           | Kod gerçeği                                                                                                                                                                                                                             | Kanıt                                                             | Sınıf                                   |
| --- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------- |
| B1  | Review §1: "kalıcı bulgu (findings.jsonl) **0**; 0 finding"                                               | İkinci bulgu deposu var ve DOLU: reflection "Committed Findings"i `aria-findings/F-*.json`'dan okur; `origin/aria/state:findings/aria-findings/` F-001…F-013, F-101, F-102 — hepsi OPEN (2026-08-17 → 09-20)                            | `reflection.py:475-519`; `git ls-tree -r origin/aria/state`       | TUTARSIZ (review eksik) / EKSİK-DOKÜMAN |
| B2  | MIMARI Şema 0/3: CONVERGED → `promotion_controller` (WIP + kanıt) → dispatch otomatik ok                  | `promote_converged_plan_to_dispatch` yalnız CLI'dan, `acknowledge=True` zorunlu; otonom zincirde çağıran yok                                                                                                                            | `promotion_controller.py:149-150`; tek çağıran `cli.py:4497-4507` | TUTARSIZ                                |
| B3  | MIMARI Şema 17 A1: "operatör şeridi: proposal approved_for_apply + insan onayı"                           | Operatör onayını yazan tek fonksiyon `approve_proposal` yalnız testlerden çağrılıyor; CLI komutu yok → `ci.py` `ready_for_operator` / self_improvement `open` önerileri ve `pr prepare/commit/push` operatör şeridi üretimde erişilemez | `proposal.py:115-143`; `pr_manager.py:611,660`; grep              | EKSİK-KOD                               |
| B4  | MIMARI Şema 14: "recursive_impact — 6 kaynak … bilinmeyen = unknown → kapıyı bloklar"                     | Hiçbir kapı okumaz; yalnız CLI + governance satırı                                                                                                                                                                                      | `recursive_impact.py:990-1033`; tek çağıran `cli.py:5109`         | TUTARSIZ                                |
| B5  | CLAUDE.md / entity-schema invariant: kiracı-başı tablolar `schema:` beyan ETMEZ                           | `_db_entity_source` `schema:` yoksa "ADR-011 violation" `unknown`                                                                                                                                                                       | `recursive_impact.py:650-664`                                     | TUTARSIZ                                |
| B6  | MIMARI Şema 18: research "alan adı izin listesi"                                                          | Politika kaydı yoksa mod `open` — her public host serbest; redirect hop'larında domain yeniden sınanmaz                                                                                                                                 | `research.py:124-131,206-215`                                     | TUTARSIZ                                |
| B7  | MIMARI Şema 18: research kaynakları `runtime_unverified`, asla kanıt                                      | Modülde trust_grade yok; kaynak VARLIĞI `fitness.dependency_currency` puanına girer                                                                                                                                                     | `research.py:82-94`; `fitness.py:40-49`                           | EKSİK-KOD                               |
| B8  | MIMARI Şema 6: "memory.py — tek inancı yazar modül"; defterler append-only                                | `pr_tracking` `beliefs.jsonl` + `learning-events.jsonl`'a doğrudan yazar, `findings.jsonl`'ı `rewrite_jsonl` ile yeniden yazar                                                                                                          | `pr_tracking.py:345-394`                                          | TUTARSIZ                                |
| B9  | MIMARI Şema 9: override ledger'ı (`calibration/weight-overrides.jsonl`), "her şey hash zincirli" (Şema 6) | Ham `open("a")`, zincirsiz; okuyucu bozuk satırı sessizce atlar                                                                                                                                                                         | `pressure.py:79-92,132-133`                                       | TUTARSIZ                                |
| B10 | MIMARI Şema 9: baskı sistemi tek (run_pressure)                                                           | İkinci sistem: workspace baskısı `faded 90 / sleeping 180 / archived 365` gün + `closed/satisfied` durum makinesi, 3 closed_signal ile kapanış                                                                                          | `pressure.py:175-182,810-904`                                     | EKSİK-DOKÜMAN                           |
| B11 | MIMARI Şema 4: trailer "Closes-Pressure: id"                                                              | Terfi hep `Addresses-Pressure:`; Closes yalnız `auto_fix_safe`                                                                                                                                                                          | `promotion_controller.py:163`; `worker_dispatch.py:140`           | KAYMA                                   |
| B12 | MIMARI Şema 5: alt süreç env'i `ARIA_LEASE_TOKEN + ARIA_CLAIM_METADATA`                                   | Metadata artık 0600 DOSYA (`ARIA_CLAIM_METADATA_FILE`), MAX_ARG_STRLEN (ARIA-HIGH-085)                                                                                                                                                  | `planner_dispatch_hook.py:86-97,171-187`                          | KAYMA                                   |
| B13 | MIMARI Şema 7: "çekirdek kapsamına dokunan araçlar asla panel onayıyla ACTIVE olamaz"                     | Yalnız 3 prob yolu denenir (`cycle.py`, `test_cycle.py`, `pyproject.toml`); çekirdeğin alt dizinini okuyan kapsam kaçar                                                                                                                 | `promotion_veto.py:75-79,136-137`                                 | TUTARSIZ (zayıf)                        |
| B14 | Review_runner docstring: gaps'te `aria-evidence-judge` ikinci görüş, `judge_split`                        | Evidence judge hiç basılmaz, `judge_split` hiç dönmez; Gate B tek yargıç                                                                                                                                                                | `review_runner.py:14-25,89-91`                                    | TUTARSIZ                                |
| B15 | preflight docstring: "4 required rules"                                                                   | Kod 3 alan                                                                                                                                                                                                                              | `preflight.py:5-7,136-140`                                        | KAYMA                                   |
| B16 | `aria-config/product_fitness_charter.json` v2: stages A/B/C, ratchet, bütçeler, outcome metrics           | Kod yalnız workflow sonuçlarını + `consecutive_green_nights_required` okur                                                                                                                                                              | `product_fitness.py:72-162`; `cycle.py:1221-1224`                 | EKSİK-KOD                               |
| B17 | `docs/aria/policy/policy-approval.json` `max_expiry_hours: 24`                                            | Uygulanmaz; yalnız "gelecekte mi"                                                                                                                                                                                                       | `policy_approval.py:16-27,101-102`                                | EKSİK-KOD                               |
| B18 | `risk-policy.json` `auto_merge_candidate_lanes: ["L1"]`                                                   | Doğrulanır, hiç okunmaz                                                                                                                                                                                                                 | `risk_policy.py:157-160`                                          | EKSİK-KOD                               |
| B19 | Kod içi yorum `pr_manager.py:374-384`: "pr_lifecycle fazı üretimde koşmaz, tek yol cli"                   | `pr_lifecycle` CYCLE_PHASES satırı; ayrıca `implementation_delivery` canlı                                                                                                                                                              | `cycle.py:3481`; `implementation_delivery.py:641`                 | KAYMA (yorum)                           |
| B20 | Kod içi yorum `runtime_artifacts.py:267-272`: "by-cycle aria/state'e yayımlanan TEK runs ledger'ı"        | `tools/runs.jsonl` de yayımlı                                                                                                                                                                                                           | `git ls-tree origin/aria/state`                                   | KAYMA (yorum)                           |
| B21 | report.py: `cycle_ids_sealed` = o günün mühürlenmiş cycle'ları                                            | Tarih süzgeci yok, tüm geçmiş                                                                                                                                                                                                           | `report.py:123-140,303`                                           | TUTARSIZ                                |

## Bağlı olmayan / ölü / stub kod

- `proposal.approve_proposal` — yalnız test (bkz. B3); `proposal_packet_from_task`, `record_proposal_from_amplification` — çağıran yok `proposal.py:243-288`.
- `pr_tracking.plan_incremental_cycle` — çağıran yok `pr_tracking.py:315`.
- `recursive_impact`: `_make_stub_source` `:268`, `_TS_IMPORT_RE`/`_PY_IMPORT_RE` `:182-199`, `_GRAPHQL_OP_FIELD_RE` `:430`, `_TYPEORM_RELATION_RE` `:579` tanımlı ve kullanılmıyor; modülün tamamı yalnız CLI.
- `review_runner._EVIDENCE_JUDGE_AGENT/_EVIDENCE_ROLE` `:89-91` ve `judge_split` verdict'i ulaşılamaz.
- `reverify` cursor dosyası yazılır, hiç okunmaz `reverify.py:56-64`.
- `pr_manager.push_prepared_branch` içinde `if True else None` ölü ifade `:724`.
- Yalnız CLI'dan erişilen üretim modülleri: `promotion_controller`, `readiness_proofs`, `recursive_impact`, `registry_compiler`, `research`, `reverify`, `review_record`.
- NoOp/yutma: `planner_dispatch_hook.py:645-649` (breaker kaydı `except Exception: pass`), `pressure.py:1039-1040`, `reflection.py` ~12 renderer `except Exception: return []`, `promotion.attempt_auto_promotions` çağrısı `autonomy_orchestrator.py:884` `except Exception`.

## Hata / risk gözlemleri (kanıtlı)

1. **Runner attestation hiç yazılamaz → merge yapısal olarak kapalı.** Composite action `ephemeral`/`group-approved`/`runner-group` girdilerini zorunlu ve varsayılansız tanımlar (`.github/actions/probe-runner-attestation/action.yml:29-47`), iki workflow yalnız `tools-dir` geçer (`aria-auto-cycle.yml:558-559`, `aria-agent-executor.yml:563-564`) → `_env_flag("")` False → `runner_attestation_ephemeral_runner_required` (`runner_attestation.py:209-210,246-249`). `merge_authority` → `runner_attestation_required_for_merge`.
2. **report_ingestion her cycle düşüyor (canlı kanıt).** 2159 satır > 500 eşiği, `learning.py:96` onay bayrağı geçmez → `ValueError: large_backfill_requires_confirm_large_backfill_and_acknowledge` (`report_ingestion.py:53-54`); `origin/aria/state:workspace/00cc2f30056cf43a/aria-memory/governance.jsonl`'da 42 `learning_hook_failed`. Lane-A/B inceleme bulguları ARIA'ya hiç akmadı.
3. **Token proof GH App yoksa `valid=False`** (`readiness_proofs.py:811`) → readiness claim kaydedilemez; preflight aynı durumu yalnız uyarı sayar (`preflight.py:393-399`).
4. **Terfi kanıt sınıfı aşırı katı:** tek non-ok araç koşusu `INTEGRITY_FAILED` (`runtime_artifacts.py:877-887`) → `cycle_not_artifact_bearing` (`promotion_controller.py:120-121`); modülün kendi özeti aynı durumu `degraded` sayar (`:1566-1579`).
5. **Varsayılan dispatch kapsamı = READONLY çekirdek:** `allowed_scope` varsayılanı `aria-kernel/**, .claude/**`, ürün kodu yasak (`promotion_controller.py:176-177` ↔ `implementation_safety.py:73-75`).
6. **Recovery yanlış "confirmed":** push için uzak SHA karşılaştırılmaz, PR için `--state all` herhangi bir PR yeter (`recovery.py:132-151`).
7. **Gate B yargıcına diff referansı verilmez** (`review_runner.py:285-305`); timeout'ta eski istek iptal edilmeden yenisi basılır, orkestratör 3×1800 s senkron poll (`:310-347`).
8. **uncertainty_repeat baskısı kendini besler ve sönmez** (`pressure.py:625-679,709-718`).
9. **pr_tracking çoklu merge'de yalnız sonuncuyu işler** (`pr_tracking.py:255,340-342`).
10. **reflection mission bölümü terminal mission'ları aktif gösterir** (küçük harf süzgeç vs `mission.py:101-107` büyük harf) `reflection.py:1260-1263`.
11. **registry_compiler** manifest `status:"SHADOW"` ile canlı ACTIVE/QUARANTINED durumlarını ezer, QUARANTINED koruması yok (`registry_compiler.py:127-151`).
12. **risk_policy L1** (en düşük risk) `aria-kernel/tests/**` (invariant testleri dahil) ve `tools/aria-adapters/*.tool.json` (runner argv) içerir (`docs/aria/policy/risk-policy.json`; `risk_policy.py:173-179`).
13. **policy_approval SoD** tek kişinin iki aşamayı tutmasına izin veren küme sayımı; iptal satırı önceki onayı düşürmez (`policy_approval.py:64-78`).
14. **`_normalize_path` `lstrip("./")`** `.github/…` → `github/…` (`pr_manager.py:965-966`).
15. **DLP diff-kapsam kontrolü fail-open** (`readiness_proofs.py:946-964`); rollback bundle dosyaları StateSurface değil → `aria/state`'e yayımlanmaz (`state_manifest.py:512-513`, `state_store.py:1067-1086`).
16. **İki "managed auth" tanımı:** attestation `CLAUDE_CODE_OAUTH_TOKEN` varlığı (`runner_attestation.py:125,141`) ↔ runtime `authMethod=="claude.ai"` (`tools/aria-poc/status_answers.py:40-43,88`).
17. **Zaman aşımsız git alt süreçleri:** `pr_manager.py:93,121,337,954`; `promotion_controller.py:266`; `readiness_proofs.py:571,579`.
18. **Cycle sırası sözlük karşılaştırmasıyla** (`str(cycle_id) <`) `pressure.py:1176`, `reflection.py:1713` — `train-*`/`cyc-*`/`auto-cycle-*` önekleri karışınca yanlış "önceki koşu".
19. **promotion_panel "tek soru, ebediyen"** — reddedilen/veto edilen adaptör için sweep asla yeniden sormaz (`promotion_panel.py:46-52,73-75`).
20. **Claim sızıntısı:** planner hook'ta claim sonrası metadata/worktree adımı istisna atarsa claim bırakılmaz (`planner_dispatch_hook.py:499-553`).

## Hiçbir dokümanda olmayan önemli davranışlar (MIMARI'ye eklenmeye değer)

- **İki bulgu deposu:** `tools/findings.jsonl` (adaptör/consensus) ve `aria-findings/F-*.json` (committed; aria/state'te 15 OPEN) — rapor ikincisini okur (`reflection.py:475-519`).
- **Enterprise readiness claim zinciri** (`readiness_proofs.py`): workflow-run, branch-protection v3, remote-CAS kira, rollback+retention (git bundle), token, DLP (9 desen), artifact → tek claim; merge_authority bunu + runner attestation + rollback + policy approval (L3) ister. Şema 7'de tek kutu.
- **Ürün uygunluk enstrümanı** (`product_fitness.py`, `product-fitness.jsonl`): 11 boyut, `unknown` = seri kırılır.
- **Dış-efekt intent/receipt + recovery sınıflandırması** (`recovery.py`, Plan 032).
- **Sağlayıcı kota soğuması** (`provider_cooldown.py`) — admission sağlayıcıyı adıyla reddeder, diğerine geçer.
- **İstek-başı worktree** (`request_worktree.py`, ARIA-HIGH-176) ve **runner availability preflight** (`runner_availability.py`).
- **Panel onayı + 24 s sessizlik-onayı veto penceresi, 7 pencere sonra EXPIRED** (`promotion_veto.py:93-94`) — Şema 7 yalnız "24 s" der.
- **Workspace baskı çürüme kovaları** (90/180/365 gün) ve `reverify` arşivleme (bkz. B10).
- **Günlük rapor = anchor frontmatter + reflection gövdesi**, anchor ilk yazımda donar (`report.py:399-446`); ROI `usd_per_merge`.
- **Runtime artefakt üç-yollu doğrulaması** (verified / retained / compacted attestation, ARIA-HIGH-117) `runtime_artifacts.py:1789-1830`.

## RAG / kanıt modeli ile ilgili not

- Bu gruptaki modüller review §7 tespitini destekler: vektör/embedding yok. "Benzerlik" yalnız grep/fnmatch alt-dizge eşleşmesi (`recursive_impact` stem grep, `pr_tracking._matches`, `risk_policy` fnmatch).
- Kanıt modelini zayıflatan noktalar: `research` dış metni `extracted_claims` olarak saklar ama trust_grade taşımaz (B7); `record_proposal` evidence yollarını doğrulamaz (`proposal.py:62-63`); Gate B yargıcına incelenecek diff verilmez (risk 7); `readiness.py` sıfır-bulgu şeridi yargısız ACTIVE'e izin verir, terfide `precision=1.0` (`readiness.py:119-122`, `promotion.py:104`).
- Güçlü noktalar: Gate B verdict'i sealed payload hash'i yeniden hesaplanarak okunur (`review_runner.py:385-421`); panel onayı arm/mint/consume anlarında adjudication kaydından yeniden çözülür (`promotion_veto.py:179-209,336-349`); artefakt referansları sha256 ile yeniden doğrulanır, `tmp/`, `.aria-poc/`, `runner-temp/` gibi öz-çıktı URI'leri reddedilir (`runtime_artifacts.py:66-71,1836-1842`).

---

## Ek — dosya dosya çalışma notları

## Çalışma notları (ara kayıt — dosya dosya)

### planner_dispatch_hook.py (728)

- Üretim: `autonomy_orchestrator.py:1009-1012` → `autonomous_planner_dispatcher.run_planner_dispatch_daemon` → `:128-129` varsayılan hook; CLI `cli.py:4263`. Şema 4/5.
- Claim metadata artık ENV değil DOSYA (`ARIA_CLAIM_METADATA_FILE`, 0600, `runtime/claim-metadata/<claim>.json`) — ARIA-HIGH-085 (MAX_ARG_STRLEN) `:86-97,171-187,553-561`. MIMARI Şema 5 "env: ARIA_LEASE_TOKEN + ARIA_CLAIM_METADATA" → KAYMA.
- Executor timeout = `max(300, lease-120)` (lease içinde) `:202-211`; timeout'ta `circuit_breaker.record_failure(kind="subprocess_timeout")` — breaker'ın zamanlanmış şeritteki TEK canlı üreticisi (yorum `:594-629`), `except Exception: pass` ile yutulur `:645-649`.
- İstek anchor'ında ayrı worktree (`request_worktree.add_request_worktree`) — ARIA-HIGH-176 `:482-526`; git yanıt vermezse claim `NATIVE_RUNTIME_CONTROL_UNAVAILABLE` ile bırakılır.
- Çocuk exit 0 olsa da claims ledger'daki release reason'a göre `provider_undecided / provider_control_unavailable` statüsü (ARIA-HIGH-107) `:696-709`.
- RİSK: claim alındıktan SONRA `_serialise_claim_metadata_for_env` / `_write_claim_metadata_file` / `add_request_worktree` bir istisna fırlatırsa (try dışı `:499-553`) claim bırakılmaz → lease süresi (1800 s) boyunca asılı kalır; yalnız timeout/exit≠0 dalları `_release_abandoned_claim` çağırır.
- `_release_abandoned_claim` her çağrıda tüm claims.jsonl'ı okur (O(n)) `:234-245,271-278`.

### policy_approval.py (120)

- Üretim: `merge_authority.py:81-88` (yalnız lane == L3); CLI `cli.py:6118-6122` (record). Şema 7/17.
- EKSİK-KOD: `docs/aria/policy/policy-approval.json` `max_expiry_hours: 24` beyan eder, kod hiç okumaz — `_validate_approval_row` yalnız "gelecekte mi" bakar `:101-102`; 10 yıllık `expires_at` kabul.
- SoD zayıf: `actors` kümesi tüm eşleşen satırlardan toplanır `:76-78`; risk_owner'ı A+B, exception_owner'ı A imzalarsa geçer (A iki rolü de tutar). `actor` kendi-beyan string (kimlik doğrulaması yok).
- İptal yok: `state` alanı satır-bazlı filtrelenir `:69`; sonradan yazılmış `revoked` satırı daha önceki `approved` satırı geçersiz kılmaz.

### pr_manager.py (966)

- Üretim: `implementation_delivery.py:641` (executor teslimi, gerçek `gh pr create`), `cycle.py:3016` (`pr_lifecycle` fazı, dry_run=True, `cycle.py:3481` CYCLE_PHASES satırı), CLI `cli.py:5175-5245`. Şema 17.
- YORUM KAYMASI: `:374-384` (ORPHAN-CRITICAL-498) "`_run_extended_phases` üretimde koşmaz, tek canlı yol `cli pr open`" diyor; oysa `pr_lifecycle` artık `CYCLE_PHASES` satırı (`cycle.py:3481`, RC-1; `planner_dispatch_hook.py:614-619` bunu doğru anlatır) ve `implementation_delivery.py:641` ikinci canlı yol. Kod içi yorum çelişkisi.
- Dry-run ile canlı farklı kapı: dry_run `observe_perimeter` (girdisi olmayan kontroller `not_evaluable` sayılır), canlı `run_hard_fail_checks` `:445-468`.
- Timeout yalnız `gh pr create`'te (300 s) `:47,536`; `git diff`/`git log`/`git rev-parse`/`_git` (push dahil) zaman sınırsız `:93,121,337,954-958`.
- `push_prepared_branch` profil kapısı (`enforce_profile_for_action`) ve operatör onayı kontrolü YOK (prepare/commit'te `require_operator_approval` var, push'ta yok) `:696-742`; ölü ifade `head_sha = … if True else None` `:724`.
- `commit_prepared_branch` çalışma ağacındaki TÜM değişiklikleri (`git status --porcelain`) `git add` eder — kapsam sınırı yok `:670,686`.
- HATA: `_normalize_path` `lstrip("./")` karakter kümesi siler → `.github/workflows/x.yml` → `github/workflows/x.yml`; `plan_pr_split` gruplaması bozulur `:965-966`.
- `plan_pr_lifecycle` yalnız öneri (`recommendation_only`) yazar; kapatma/yorum eylemi yok `:787`.
- `_effect_request_id` intent/receipt (Plan 032) — push ve PR create iki-fazlı; timeout'ta receipt yazılmaz (bilinçli) `:538-547`.

### pr_tracking.py (484)

- Üretim: `cycle.py:1627-1680` `_phase_change_intelligence` (ingest → plan_pr_impact → dispatch_change_intelligence); `gateway/router.py:151-156` (webhook → `observe_pr_event`). Şema 12/14 civarı; MIMARI'de modül adı YOK.
- ÖLÜ: `plan_incremental_cycle` `:315` — repo genelinde hiçbir üretim/CLI çağıranı yok (cycle.py:1632 docstring'i bunu kendisi söylüyor).
- TUTARSIZ (MIMARI Şema 6 "memory.py — tek inancı yazar modül"): `_mark_beliefs_for_revalidation` `memory/beliefs.jsonl` ve `learning-events.jsonl`'a doğrudan yazar `:345-380`, memory.py API'sinden geçmez.
- "Append-only" iddiasına aykırı: `_mark_findings_for_revalidation` `findings.jsonl`'ı `rewrite_jsonl` ile yeniden yazar `:383-394`.
- HATA: bir cycle'da >1 merge ingest edilirse `plan_pr_impact` yalnız SON olayı işler (`_latest_pr_or_merge_event` → `rows[-1]`) `:255,340-342`; önceki merge'lerin değiştirdiği dosyalara bağlı inançlar needs_revalidation'a düşmez.
- `_impacted_adapters` desenleri `aria-tools/registry.json`, `aria-tools/fixtures/**` — `aria-tools/` gitignore'lu, merge `changed_files`'ında asla yer almaz → pratikte hep boş `:421-427`.
- `dispatch_change_intelligence`: merge başına 1 zarf, `allowed_scope=["**"]`, run başına ≤3 mint `:38,228-244` (E14 rolünün ilk üretim üreticisi).

### preflight.py (762)

- Üretim: `verify_preflight` ← `autonomy_orchestrator.py:1108` (her eylem-yetkili profil); `verify_workflow_preflight` ← 10 workflow YAML'ı içinden inline Python (`aria-auto-cycle.yml`, `aria-agent-executor.yml`, `aria-merge-authority.yml`, `finding-closure-reconcile.yml` …); `probe_branch_protection` ← `readiness_proofs.py:329`. Şema 19 (W3), Şema 1.
- KAYMA (modül docstring): "main branch carries the **4** required rules" `:5-7`; kod 3 alan ister (`restrictions.users` 2026-05-19'da düşürüldü) `:136-140`.
- Standard/strict için `valid` her zaman True `:417`; `environment_preconditions_not_met` (sandbox yok, node_modules yok, disk düşük) yalnız uyarı satırı → gece `standard` koşusu sandbox'sız da başlar (`autonomy_orchestrator.py:699-735`). Merge-sınıfı profilde ise `node_modules_absent` failure_class'a EKLENMEZ `:379-382` (ters asimetri: autonomous node_modules'suz geçer).
- İmza anahtarı kontrolü yalnız `aria-debts/keys/` DİZİNİ var mı `:186-199` (docstring de itiraf eder); GH App yokluğu hard-fail değil, operatör PAT fallback'ine izin (`:393-399`) — MIMARI Şema 22 "işçiye asla operatör PAT'i geçmez" ifadesiyle gerilim (fallback `gh_token_factory.py:947-950` Mode B).
- `MIN_FREE_DISK_GB` import anında env'den okunur `:147`; ölçülemeyen disk geçer `:371`.
- `_git_worktree_offending_paths` `aria-tools/`, `docs/aria/CURRENT_STATE.md`, `docs/aria/generated/`, `tools/quality/format-scope.json` önekini kirli saymaz `:724-729` (ORPHAN-HIGH-793).

### pressure.py (1194)

- Üretim: `run_pressure` ← `cycle.py:1401,1492` (pressure fazı); workspace-pressure API'si ← `reverify/triage/learning/promotion_controller/worker_dispatch/capability_gap/trailer_scan/semantic_dedup` + CLI `cli.py:4142-4185`. Şema 9.
- EKSİK-DOKÜMAN: modülde İKİ ayrı baskı sistemi var — (a) cycle baskısı `run_pressure` → `pressure/<cycle>.json` (çürüme yok, `recency_decay=1.0` sabit `:979`), (b) workspace baskısı `aria-memory/pressure.jsonl` + `pressure_state.jsonl`, yaşa göre `faded 90 / sleeping 180 / archived 365` gün kovaları + `closed/satisfied` terminal durumlar `:175-182,861-904,1085-1096`; `close_pressures_from_signals` ≥3 `closed_signal` + ≥2 farklı evidence_ref ile kapatır `:838`. MIMARI Şema 9 yalnız (a)'yı çizer; (b)'nin durum makinesi hiçbir dokümanda yok.
- TUTARSIZ (kod içi): `uncertainty_repeat` yorumu "rows stop olunca decay ile söner" der `:625-628,675`; ama sayım `uncertainties.jsonl`'ın TÜM tarihini zaman penceresiz sayar `:643-652` ve (a) sisteminde decay yoktur → eşik bir kez aşılınca baskı sonsuza dek her cycle üretilir. Üstelik `_filter_candidate_tools` her cycle aynı ledger'a yeni satır ekler `:709-718` → kendini besleyen döngü.
- `calibration/weight-overrides.jsonl` hash-zincirsiz ham `open("a")` ile yazılır, okuyucu bozuk satırı sessizce atlar `:79-92,132-133` (Şema 6 "her şey hash zincirli" iddiasının istisnası); `operator_approval_ref` yalnız boş-olmayan string `:117`.
- Tip tutarsızlığı: `post_merge_ci: 95.0` float, override yalnız int kabul `:56,90,113`.
- Performans: `_raw_finding_delta` her koşu için runs.jsonl'ı yeniden okur (O(n²)) ve cycle sırasını `str(cycle_id) < cycle_id` sözlük karşılaştırmasıyla belirler — `train-*`/`cyc-*`/`auto-cycle-*` önekleri karışınca yanlış "önceki" koşu `:1171-1182`.
- `_phase2_effective_context` `except Exception: return set(), {}` — trust hatası sessizce "güvenilir anahtar yok"a düşer `:1034-1040`.
- Kanıt dizgileri sabit `aria-tools/memory/...` `:342,670`; canlı şeritte tools kökü `.aria-state-store/tools` (aynı dosyada `:457-461` B4 notu bu sınıfı başka yerde düzeltmiş).

### proactive_priority.py (142)

- Üretim: `compute_proactive_priorities` ← `cycle.py` proactive fazı; `latest_priorities` ← `task.py` (task-candidate). Şema 9 yan dönüş.
- Doğru: `priority = impact(tool_id token'ı: security/tenant/auth… 1.0, domain 0.7, diğer 0.5) × opportunity(0.2 taban + goldset yok 0.4 + az yargılanmış ≤0.4 + kalibrasyon bozuk 0.1)` `:37-84`. Yeni: kalibrasyon bozukluğu yalnız SON satırın `degraded_judges` alanına bakar `:58-62`. Yazım `feedback_store.append_jsonl` üzerinden `proactive/priorities.jsonl` `:122-125`.

### product_fitness.py (183)

- Üretim: `cycle.py:1158-1225` `_phase_product_fitness` (`CYCLE_PHASES` `:3360`, record_and_continue) → `product-fitness.jsonl`. HİÇBİR MIMARI şemasında yok (EKSİK-DOKÜMAN): ürünü 11 boyutta, main'de zaten oy veren GitHub workflow sonuçlarıyla ölçen "ürün uygunluk" enstrümanı; `unknown` streak'i kırar.
- EKSİK-KOD: `aria-config/product_fitness_charter.json` v2 `stages` (A_instrumented 7 / B_bounded 14 / C_industrial 30 gece), `active_stage`, `ratchet`, bütçeler ve `outcome_metrics_stage_c` beyan eder; kod yalnız `dimensions[].workflows`, `statement`, `consecutive_green_nights_required` okur (`product_fitness.py:72-162`, `cycle.py:1221-1224`). Aşama/ratchet/bütçe/sıfır-tolerans hiçbir yerde uygulanmıyor; `measured_by != workflow_conclusions` → her zaman `unknown` `:140-147`.
- "Gece" = cycle satırı: `streak_from_history` satır sayar `:165-183`; aynı gün birden çok cycle (manuel/dispatch) streak'i şişirir.
- `evaluate_fitness` okuyucu hatasını `except Exception` ile `unknown`'a çevirir (bilinçli) `:125-131`.

### progress.py (195)

- Üretim: `ProgressWriter` ← `tools/aria-poc/ci_executor.py:4620,4877` (stream-json `on_event`); `tail_progress` ← CLI `cli.py:6540`; `read_progress` ← `mcp_server.py:141`. Şema 5/15 (hiçbir şemada adıyla yok).
- `run-artifacts/hot/<request_id>/progress.jsonl`, hash-zincirli; metin 240 karakter + `scrub_text`; araç çağrıları `hooks.sanitize_journal_entry` ile redakte `:45-99`. `ProgressWriter.write` tüm istisnaları yutar `:123-125`.
- `seq` yazar örneği başına 0'dan başlar `:109,117`; aynı request yeniden claim edilince aynı dosyada tekrar eden seq değerleri oluşur. `tail_progress --follow` her 2 s'de tüm zinciri yeniden doğrulayarak okur `:154-163`.

### promotion.py (168)

- Üretim: `attempt_auto_promotions` ← `autonomy_orchestrator.py:879-881` (her gece, SHADOW adaptörleri için; `except Exception` ile yutulur); `promote_tool` ← CLI `cli.py:3639`. Şema 10.
- Operatör onay referansı KAYDEDİLMEZ: `transition_tool(..., operator_approval=bool(operator_approval_ref))` `:102` — ref dizgisi (içeriği) doğrulanmaz ve registry geçişine taşınmaz; herhangi bir boş-olmayan string ACTIVE kapısını açar (readiness şartıyla).
- Sıfır-bulgu şeridinde `precision=1.0` sabitlenir, `evidence_chains_valid=True` sabit geçilir `:104-106`.
- Docstring "ineligible adapter is RECORDED" `:127` — yalnız dönüş sözlüğünde (ilk 10) tutulur; governance'a yalnız başarılı terfi yazılır `:158-163`.

### promotion_controller.py (294)

- Üretim: YALNIZ CLI `cli.py:4497-4507` (`acknowledge=True` zorunlu `:149-150`). Otonom zincirde çağıran yok.
- TUTARSIZ (MIMARI Şema 0 "promotion_controller: WIP kapısı + kanıt bütünlüğü" otomatik ok; Şema 3 "Terfi"): CONVERGED → dispatch terfisi operatör komutu; `autonomy_orchestrator`/`cycle` bu fonksiyonu çağırmaz.
- RİSK: varsayılan `allowed_scope = ["aria-kernel/**", "aria-tools/**", ".claude/**"]`, `forbidden_scope = ["apps/**","libs/**","platform/**"]` `:176-177` — işçinin varsayılan yazma alanı tam olarak `implementation_safety.READONLY_PATHS`'in (`aria-kernel/aria_kernel/`, `.claude/agents/` `implementation_safety.py:73-75`) yasakladığı çekirdek; ürün kodu yasak. Kapsam verilmeden terfi edilen iş ya hiçbir şey yapamaz ya da öz-değişim perimetresine çarpar.
- İdempotent değil: `assignment_id` hash'ine `utc_now()` girer `:259-263`; aynı plan iki kez terfi edilirse iki ayrı dispatch satırı.
- `_validation_commands_from_ref`: ref bir dosya değilse ref dizgisinin KENDİSİ `required_tests` komutu olur `:291`.
- Trailer hep `Addresses-Pressure:` `:163` (MIMARI Şema 4 "Closes-Pressure: id"; `worker_dispatch.py:140` yalnız `auto_fix_safe` için Closes) → KAYMA.
- `_git_head` zaman aşımsız `:266-276`.

### promotion_panel.py (150)

- Üretim: `sweep_promotable_adapters_for_adjudication` ← `cycle.py` (tool lifecycle fazı); `execute_tool_promotion_panel_approval` ← `human_required_adjudication.py` (panel fold). Şema 7/10.
- "Adaptör başına TEK panel sorusu, ebediyen": `_escalation_id` deterministik `promote-<sha16>` ve dosya varsa atlanır `:46-52,73-75`. Panel reddettiyse ya da operatör veto ettiyse süpürme bir daha sormaz → adaptör operatör CLI'sı dışında kalıcı SHADOW. Dokümanda yok.
- Kanıt ref'i sabit `aria-tools/runs.jsonl#<tool>` `:105` (canlı tools kökü `.aria-state-store/tools`).

### promotion_veto.py (545)

- Üretim: `settle_pending_promotions` ← `cycle.py` (`_phase_tool_manifest_sync`); `verify_panel_approval_token` ← `tool_registry.transition_tool`; `veto_promotion` ← CLI `cli.py:3650`. Şema 7/10 ("Panel onayı 24 saatlik veto").
- Durum yalnız `governance.jsonl` olaylarından katlanır (PENDING/VETOED/ELAPSED/EXPIRED) `:28-35,150-176`; arm idempotent; bekleyen kayıt `veto_deadline + 7×24 h` sonra EXPIRED `:93-94,262-272`; settle anında readiness yeniden ölçülür `:498-503`. Panel ref'i arm, mint ve consume'da yeniden çözülür (`human_required.resolve_panel_adjudication_proof`) — sağlam.
- RİSK (çekirdek-kapsam istisnası örneklemeli): `tool_scope_touches_kernel` yalnız 3 prob yolunu dener (`aria-kernel/aria_kernel/cycle.py`, `aria-kernel/tests/test_cycle.py`, `aria-kernel/pyproject.toml`) `:75-79,136-137`. `aria-kernel/aria_kernel/gateway/**` gibi çekirdeğin ALT dizinini okuyan bir kapsam hiçbir probu yakalamaz → panel yolu ile ACTIVE olabilir. Docstring'in "exception cannot be defeated by scope SPELLING" iddiası `:118-125` bu sınıf için geçerli değil. `tools/aria-poc/`, `.claude/agents/` (READONLY) de problanmıyor.
- Sessizlik = onay; bildirim yalnız governance satırı (push/e-posta yok) `:15-17`.

### proposal.py (298)

- Üretim: `record_proposal` ← `apply_engine.py:758` (otonom şerit), `ci.py:324` (`ready_for_operator`), `self_improvement.py:308` (`open`); `record_machine_approval` ← `apply_engine.py:783`; `get_proposal/approval_source_of/require_operator_approval` ← `pr_manager.py`. Şema 17.
- ÖLÜ / EKSİK-KOD (önemli): `approve_proposal` (tek `approval_source="operator"` yazıcısı) `:115-143` repoda YALNIZ testlerden çağrılır; CLI'da `proposal approve` alt komutu yok. Sonuç: (a) `ci.py`'nin `ready_for_operator` ve `self_improvement`'ın `open` önerileri üretimde hiçbir zaman `approved_for_apply` olamaz; (b) `pr_manager.prepare_branch/commit_prepared_branch` (`require_operator_approval`) ve CLI `pr prepare/commit/push` operatör şeridi yapısal olarak ölü. MIMARI Şema 17 A1 "operatör şeridi: proposal approved_for_apply + insan onayı → worktree aç" → EKSİK-KOD.
- ÖLÜ: `proposal_packet_from_task`, `record_proposal_from_amplification` — hiçbir çağıran yok `:243-288`. Sonuncusu `str(response.get("title"))` ile eksik alanı `"None"` dizgisine çevirip boş-değil kontrolünü geçerdi `:277-285`.
- `record_proposal` evidence yollarını doğrulamaz (yalnız boş-olmayan string) `:62-63`; `status` parametresi doğrudan `approved_for_apply` verilebilir (approval_source None kalır; pr_manager bunu `open_pr_approval_source_unknown` ile reddeder `pr_manager.py:283-289`).

### provider_cooldown.py (218)

- Üretim: yazma ← `tools/aria-poc/ci_executor.py`, `worker_executor.py`, `ci_executor_judge_batch.py`, `claude_runtime.py` (`ClaudeCreditExhausted` kolu); okuma ← `native_admission.py`, `worker_dispatch_hook.py`. Şema 5 (MIMARI "kredi biterse ClaudeCreditExhausted → REQUEUED").
- EKSİK-DOKÜMAN: kota tükenmesi `governance.jsonl`'a `provider_quota_cooldown` satırı (`until = now + provider_cooldown_seconds`) yazar; sonraki admission soğuyan sağlayıcıyı adıyla reddeder ve diğer sağlayıcıya geçer, worker hook claim almadan atlar `:1-30,98-129,171-189`. Bozuk satır atlanmaz, `GovernanceError` fırlatır `:138-159` → tek bozuk satır tüm admission'ı durdurabilir (bilinçli fail-closed).
- Her admission tüm governance.jsonl'ı okur/doğrular `:162-168` (büyüyen ledger üzerinde O(n)).

### pytest_native_only.py (27)

- Üretim DEĞİL, CI eklentisi: `scripts/ci/aria-suite-run.sh:49,57` `-p aria_kernel.pytest_native_only`; `unittest.TestCase` öğelerini pytest'ten düşürür (unittest ile ayrı koşuldukları için). Test/araç kodu çekirdek paketin içinde yaşıyor. Şema yok.

### quarantine.py (56)

- Üretim: `quarantine_tool` ← `tool_health.py:20,251` (otomatik karantina), CLI `cli.py:3623`; `quarantine_log_path` ← `tool_sit_out.py:51,93,156`. Şema 7/10.
- `quarantine.jsonl` satırı `prior_findings_revalidation: "required"` yazar `:51` — bu alanı okuyan kod bu dosyada yok (bulguların needs_revalidation'a döndürülmesi başka yerde; bkz. MIMARI Şema 10 "aracın bulguları needs_revalidation'a döner").

### readiness.py (204)

- Üretim: `adapter_active_readiness` ← `promotion.py:83`, `promotion_panel.py:82`, `promotion_veto.py:498`, `adapter_calibration`. Şema 10 (review §3.3 engelleri zaten listeledi).
- YENİ: "sıfır-bulgu şeridi" — `precision_status == no_findings_to_judge` ise son 5 koşu 0 ham bulgu yeterli, hiçbir yargı/anchor aranmaz `:119-122`; terfide `precision=1.0` basılır (`promotion.py:104`, `promotion_veto.py:514`). Hiç bulgu üretmeyen adaptör yargısız ACTIVE olabilir.
- "Son 5 koşu" tüm zamanların son 5'i, SHADOW durumunda koşulmuş olması aranmaz `:40,48`.

### readiness_proofs.py (1262)

- Üretim: YALNIZ CLI `cli.py:3487-3491` (`readiness claim …`), `aria-readiness-claim.yml` iş akışından. Şema 7 ("enterprise_readiness + runner attestation + rollback bundle"). Modül adı hiçbir dokümanda yok (EKSİK-DOKÜMAN): 7 kanıt ailesinin (workflow-run, branch-protection v3, remote-CAS kira, rollback+retention, token, DLP, artifact) üreticileri + `produce_readiness_claim` birleştiricisi.
- Token ailesi: GH App yoksa (`fallback_active`) proof `valid=False` `:811` → claim kayıt edilemez → merge otoritesi yapısal olarak kapalı; preflight'ın aynı durumu yalnız uyarı sayması (`preflight.py:393-399`) ile asimetrik.
- "Restore simülasyonu" = `git bundle verify` (bundle bütünlüğü), gerçek geri-alma/revert provası değil `:512-518,579-586,630-631`. Bundle `git bundle create <path> <target_ref>` = hedef dalın TÜM geçmişi, her claim'de 2 kopya (`enterprise/rollback-artifacts/` + `.archive/rollback/`) `:568-595`; `git bundle` çağrılarında timeout yok `:571,579`.
- RİSK (yayın): bundle dosyaları `state_manifest`'te StateSurface değil (yalnız `.archive/runtime/**` beyanlı `state_manifest.py:512-513`); state_store yalnız beyanlı yüzeyleri commit'ler (`state_store.py:1067-1086`) → bundle baytları `aria/state`'e gitmez; verifier URI'leri tools kökünde yeniden hash'ler (`enterprise_readiness.py:880-884`) — claim'i üreten host dışında doğrulama dosyasız kalır.
- DLP diff-kapsam kontrolü fail-open: `workspace_root` yoksa ya da `git show head_sha` çözülemezse `diff_scope_check: skipped_*` yazılır ve DLP yine `passed` olabilir `:946-964`; kontrol "yol adı diff metninde geçiyor mu" alt-dizge araması `:969`.
- Kısmi başarısızlıkta (ör. token proof hatası) önceki aile satırları + edinilmiş remote-CAS kirası kalır, serbest bırakılmaz `:1114-1147` (docstring "partial claim never recorded" yalnız claim satırı için doğru).

### rollback_bundle.py (117)

- Üretim: `record_*` ← `readiness_proofs.py:613-635`; `verify_rollback_bundle` ← `merge_authority.py`. Şema 7.
- `verify_rollback_bundle` yalnız ledger satırlarının varlığına bakar (bundle + `status=passed` simülasyon); bayt/hash yeniden doğrulaması yok `:58-103` (hash kontrolü `enterprise_readiness` claim doğrulamasında).

### recovery.py (212)

- Üretim: `classify_recovery` ← `tools/aria-poc/ci_executor.py:1724-1725` (yeniden dispatch öncesi, `gh_remote_reader`), CLI `cli.py:6563`; `record_intent/receipt` ← `pr_manager.py:517,725`, `implementation_delivery.py:817`. Şema 5/20 (Plan 032 intent/receipt hiçbir şemada yok → EKSİK-DOKÜMAN).
- RİSK (yanlış "confirmed"): `gh_remote_reader` `git_push` için yalnız dal uzakta VAR mı bakar, `remote_sha`'yı intent'teki `head_sha` ile karşılaştırmaz `:142-151`; `pr_create` için `--state all` ile o head'e ait HERHANGİ bir PR (kapalı/eski dahil) `present` sayılır, `headRefOid` kontrol edilmez `:132-141`. Eski bir dal/PR, gerçekleşmemiş bir efekti "confirmed" yapar → istek `external_effect_check` ile devam eder.
- `EXTERNAL_EFFECT_KINDS` içindeki `pr_comment`, `gh_api_write`, `state_publish` için okuyucu yok (None) `:44,154` → bu türde receipt'siz intent her zaman `human_required`; üretimde bu türlerin intent yazıcısı da yok (yalnız `git_push`, `pr_create`).

### recursive_impact.py (1040)

- Üretim: YALNIZ CLI `cli.py:5109-5113`. Plan yakınsama / dispatch / cycle hiçbir yerde çağırmaz.
- TUTARSIZ (MIMARI Şema 14 "recursive_impact — 6 kaynak … bilinmeyen kaynak = unknown → kapıyı bloklar"): modül docstring'i de "convergent gate's dispatch rule (any unknown blocks dispatch)" der `:1-8`; üretimde hiçbir kapı `compute_recursive_impact` sonucunu okumaz — yalnız governance `impact_graph_computed` satırı + `impact-graphs/<fp>.json` yazar `:990-1033`.
- TUTARSIZ (repo kuralı): `_db_entity_source` `schema:` beyan etmeyen her `@Entity`'yi "ADR-011 violation" `unknown` işaretler `:650-664`; CLAUDE.md ve `tests/invariants/entity-schema-declaration.spec.ts`'e göre kiracı-başı tablolar `schema:` BEYAN ETMEMELİ → farm/sensor/hr/messaging/… varlık değişikliklerinin tamamı sahte `unknown` üretir.
- KAYMA (docstring): "Sources 3–6 ship with explicit unknown stubs" `:22-27` — dördü de gerçek (grep tabanlı) uygulanmış; `_make_stub_source` `:268-290` ölü.
- ÖLÜ: `_TS_IMPORT_RE`, `_PY_IMPORT_RE` `:182-199`, `_GRAPHQL_OP_FIELD_RE` `:430`, `_TYPEORM_RELATION_RE` `:579` tanımlı, hiç kullanılmıyor. "import_graph" gerçekte import ayrıştırmaz: dosya **stem**'ini (`index`, `main`, `types` …) 4 kök × 4 uzantıda düz `grep -rln` ile arar `:229-243`, isabetleri derinlik ≤5 özyinelemeyle yeniden besler `:956-964` → aşırı-yaklaşık ve patlayıcı (her dosya için 16 alt süreç).
- `npx nx graph` yedeği çalışma ağacına `.aria-poc/nx-graph.json` yazar `:117-127`; Şema 14 impact_graph'taki `.nx/cache/graph.json` ile aynı yol, ayrı ikinci okuyucu.

### reflection.py (1733)

- Üretim: `run_reflection` ← `cycle.py` (reflect fazı) ve `autonomy_orchestrator.py` (Gate A/B kwarg'larıyla). Şema 9 ("reflection.py: run_reflection → reports/daily … ~30 bölüm"; NASIL §13).
- ÖNEMLİ (review §1'e düzeltme): rapor "Committed Findings" bölümünü `tools/findings.jsonl`'dan DEĞİL, repo-durum kökündeki `aria-findings/F-*.json` dosya deposundan okur `:475-519` (`finding.findings_dir`). `origin/aria/state`'te bu depo DOLU: `findings/aria-findings/F-001…F-013, F-101, F-102` — hepsi `OPEN` (F-001 2026-08-17, F-013 2026-09-20; `git ls-tree -r origin/aria/state`). Review'ın "kalıcı bulgu (findings.jsonl) 0" satırı tek depoya bakıyor; ARIA'da İKİ ayrı bulgu deposu var (EKSİK-DOKÜMAN, her iki dokümanda da ayrım yok).
- HATA: `_render_mission_section` aktif mission'ı `state not in ("folded","closed","abandoned")` ile süzer `:1260-1263`; mission.py terminal durumları BÜYÜK harf `VERIFIED/POLICY_REJECTED/CANCELLED_BY_CONSTITUTION/SUPERSEDED/FAILED_AND_ROLLED_BACK` (`mission.py:101-107`) → terminal mission'lar raporda "aktif" listelenir.
- Günlük rapor `reports/daily/<gün>.md` her cycle'da `write_text` ile ÜZERİNE yazılır (atomik değil) `:1439,1610` — aynı gün birden çok cycle koşarsa yalnız sonuncusu kalır.
- 12 bölüm renderer'ı `except Exception: return []` `:1116,1133,1172,1203,1239,1285,1345,1379,1405` vb. — bozuk ledger ile boş ledger raporda ayırt edilemez (modülün kendi `dataflow_health` felsefesiyle çelişir); `_compute_dataflow_health` da `cycles.jsonl` okuma hatasını `rows=[]` yapar `:826-829`.
- `_previous_tool_run` cycle sırasını `str(cycle_id) <` sözlük karşılaştırmasıyla bulur `:1709-1715` (pressure.py ile aynı kusur; `train-*` < `cyc-*` değildir).
- `next_cycle_plan` yalnız ilk 3 baskı ve `blocked_by` boş olanlar kuyruğa `:180-212` (Şema 9 ile tutarlı).

### reflection_inputs.py (92)

- Üretim: `autonomy_orchestrator.py` (post-drain `run_reflection` çağrılarının kwarg kurucusu). Şema 9/22 (pedagogy_lint).
- `pedagogy_lint_snapshot` her cycle `.claude/agents` üzerinde warn-mode lint koşar; `OSError/ValueError`'da None (bölüm "atlandı" görünür) `:34-59`. Başka bulgu yok.

### registry_compiler.py (87)

- Üretim: YALNIZ CLI `cli.py:3688-3694` (`registry compile`). Şema 10 kapsam tablosunda adı var.
- RİSK: `compile_registry` `registry.json`'ı manifestlerden SIFIRDAN yazar; manifestlerin `status` alanı (hepsi `"SHADOW"`, `tools/aria-adapters/*.tool.json:5`) canlı registry'deki ACTIVE/QUARANTINED/CALIBRATE durumlarını ezer — `register_tool`'daki "QUARANTINED yalnız unquarantine ile çıkar" koruması (`tool_registry.py:987-991`) bu yolda yok `:127-151`. Yazım atomik değil `:150`.
- `STUB_RUNNER_TOKENS` alt-dizge eşleşmesi: argv parçasında "echo" geçen her şey (ör. `echo-chamber-check.ts`) `stub_runner_rejected` `:102,176`.

### release_reason.py (196)

- Üretim: `agent_invocations.py:4022-4024` (release satırına yapılandırılmış alanlar), `planner_dispatch_hook.py:41`, `implementation_identity.py`. Şema 5/20 (adı yok).
- Kapalı 38 kodlu sözlük + `fault_domain` (harness/request/operator/unclassified). Bütçe yakan karar AYRI tablodan (`agent_invocations.classify_release_reason` `:2501-2518`) verilir; iki tablo şu an eşit (salt-okuma paritesi: tüm `_LITERALS`/`_PREFIXES` için aynı domain), fakat eşitliği yapısal olarak zorlayan tek kaynak yok (docstring "same ownership" diyor `:15-16`).

### report.py (494)

- Üretim: `emit_anchor_to_path` ← CLI `report daily --emit-anchor` (`aria-daily-report.yml`); `parse_anchor_frontmatter` ← `mcp_server.py` + I-26 invariant. Şema 8 (daily-report workflow), NASIL §13.
- EKSİK-DOKÜMAN: yayımlanan günlük rapor = reflection raporu + YAML "anchor" frontmatter (governance zincir ucu, integrity kök, `state_manifest_root`, ROI `usd_per_merge`, `blocked_reason`) `:277-321`; anchor "git geçmişinin yerini alan denetim-güven kaynağı" iddiası `:3-7`.
- ZAYIF: zincir ucu doğrulanmadan okunur — `_tail_ledger_hash` bozuk satırları atlar, `events_emitted_count` ham satır sayısı `:41-69`; bozuk bir governance zincirinin ucu da "anchor"lanır.
- HATA: `cycle_ids_sealed` tarihe göre süzülmez — ledger'daki TÜM terminal cycle id'leri her günün anchor'ına yazılır `:123-140,303` (dokümanda "o günün mühürlenmiş cycle'ları").
- `_BLOCKED_REASON_KINDS` `preflight_standard_warnings` içerir `:76-86`; bu satır yalnız uyarıdır (cycle koşar, `autonomy_orchestrator.py:716-729`) → anchor'da "gece koşamadı" nedeni gibi görünür.
- Yayın dondurma: reflection gövdesi bir kez anchor'lanınca o tarih değişmez `:421-432`; reflection aynı günkü sonraki cycle'larda dosyayı yeniden yazsa da (`reflection.py:1610`) yayımlanan rapor ilk anchor anındaki sürümdür.

### report_ingestion.py (441)

- Üretim: `report_ingestion_scan` ← `learning.py:96` (`run_learning_pass` hook'u, `cycle.py:213`); `import_finding_file`, `list_ingested_findings` ← CLI. Şema 9/12 civarı; hiçbir dokümanda yok.
- CANLI KIRIK (kanıtlı): `docs/reviews/_registry/findings.jsonl` 2159 satır; `len(rows) > LARGE_BACKFILL_THRESHOLD (500)` ve `confirm_large_backfill/acknowledge` verilmemişse `ValueError` `:17-18,53-54`. `learning.py:96` bu bayrakları hiç geçmez → hook HER cycle düşer. `origin/aria/state:workspace/00cc2f30056cf43a/aria-memory/governance.jsonl` içinde 42 adet `learning_hook_failed` / `report_ingestion_scan` / `ValueError: large_backfill_requires_confirm_large_backfill_and_acknowledge`. Dış inceleme bulgularının (Lane-A/B raporları) ARIA feedback'ine akışı hiç çalışmadı; `learning.py:141-144` hatayı yutup cycle'ı sürdürdüğü için görünmez.
- Tasarım: ilk koşuda (cache yok) mevcut TÜM kayıtlar "bilinen" diye baseline'lanır, hiçbiri ingest edilmez `:56-90`; OPEN olmayan satır `known`'a eklenir ve sonradan yeniden açılsa bile bir daha ingest edilmez `:98-102`.

### request_worktree.py (126)

- Üretim: `planner_dispatch_hook.py:491-503,579-582`; `tools/aria-poc/ci_executor_drain.py:512-516`. Şema 4/5 (MIMARI Şema 4 yalnız worker worktree'sini `aria-worktrees/<assignment_id>` çizer; istek-başı `aria-worktrees/req-<id>` worktree'si — ARIA-HIGH-176 — dokümanda yok → EKSİK-DOKÜMAN).
- git "yapamam" derse (`returncode != 0`, ör. bilinmeyen sha) sessizce paylaşılan checkout'a düşer `:103-106`; docstring'e göre bu durumda native admission zaten `target_revision_mismatch` ile reddeder `:3-14` → geri düşüş pratikte ret demek.

### research.py (245)

- Üretim: YALNIZ CLI (`research fetch/record/policy`); `list_research_sources` ← `fitness.py:13,40-49` (kaynak VARLIĞI "dependency_currency" fitness puanına girer). Şema 18 ("Araştırma modu").
- TUTARSIZ (MIMARI Şema 18 "alan adı izin listesi"): hiç politika kaydı yoksa `_source_policy` → `{"status":"allowed","mode":"open"}` — varsayılan AÇIK, her public host fetch edilebilir `:124-131`.
- Allowlist yalnız İLK URL'ye uygulanır; `_fetch_url` redirect hop'larında yalnız public-IP kontrolü yapar, domain allowlist'i yeniden sınamaz `:61-63,206-215` → izinli domain'den izinsiz domain'e 30x ile geçilebilir.
- DNS-rebinding iddiası `:155-160` tam değil: `getaddrinfo` ile kontrol, ardından urllib bağlantıda ADI YENİDEN çözer (check-then-connect TOCTOU) `:172,210`.
- EKSİK-KOD (Şema 18 "kaynaklar `runtime_unverified` kabul edilir, asla kanıt olmaz"): research satırlarında trust_grade alanı yok `:82-94`; "asla kanıt olmaz" garantisi bu modülde değil, (varsa) evidence_trust'ta. `extracted_claims` = ilk 10 cümle `:239-241`.

### reverify.py (118)

- Üretim: YALNIZ CLI `cli.py:4172`. Şema 12 kapsam tablosunda adı var.
- HATA / ölü alan: `reverify_cursor.json` yazılır (`last_visited_pressure_event_id`) `:56-64` ama hiçbir yerde OKUNMAZ (repo-genel grep: yalnız `reset_cursor` ile silinir) → örnekleme her zaman `event_id` sırasındaki İLK %10'u alır `:33-36`; kalan baskılar hiç yeniden doğrulanmaz.
- Aktif baskı asla arşivlenmez, "needs_operator_review" döner `:82-83`; arşiv yalnız tüm ref'ler stale/missing ve 30 günde yeni feedback yoksa `:92-95`.

### review_record.py (142)

- Üretim: YALNIZ CLI `cli.py:4807-4821` (operatör inceleme kaydı `reviews.jsonl`). Şema 17 kapsam tablosu.
- Docstring "existence checks … advisory" `:192-196` — kodda hiçbir varlık kontrolü yok (ne advisory ne fail-closed). `REV-…-NNN` tahsisi kilit dışında oku-sonra-ekle `:160-177,216-228` → eşzamanlı iki kayıt aynı ID'yi alabilir. `reviewer` kendi-beyan.

### review_runner.py (480)

- Üretim: `select_review_runner` → `run_review_runner`, orkestratöre `review_runner` kwarg'ı olarak (`autonomy_orchestrator.py:919`, CLI kurulumu). Şema 16 "GATE B adversarial yargıç hash doğrulanmış VERDICT satırı" — DOĞRU (VERDICT regex + output_hash yeniden hesaplama `:69-76,385-421`).
- TUTARSIZ (modül docstring'i ↔ kod): docstring 2. adım "gaps varsa `aria-evidence-judge` ikinci görüş, verdict'ler birleştirilir", 4. adım `judge_split` `:14-25,230-231`; kodda `_EVIDENCE_JUDGE_AGENT/_EVIDENCE_ROLE` tanımlı ama HİÇ kullanılmıyor `:89-91`, `judge_split` hiçbir yoldan dönmüyor. Gate B = TEK adversarial yargıç.
- `gaps_open` gelince tur döngüsü devam etmez, hemen döner `:422-436`; `max_review_rounds` yalnız ardışık zaman aşımlarıyla ulaşılabilir. Zaman aşımında yeni zarf basılır, eski bekleyen istek iptal edilmez `:346-347` → yetim bekleyen yargıç istekleri birikir; orkestratör turu başına `judge_timeout_seconds` (varsayılan 1800 s × 3 tur = 90 dk) senkron poll ile bloke olur `:310-332`.
- RİSK: yargıç zarfı uygulamanın diff'ine/dalına/PR'ına hiçbir referans taşımaz — `allowed_scope=[f"cycle/{cycle_id}"]`, `evidence_refs=[f"cycle:{cycle_id}"]`, yalnız `must_satisfy` + `plan_revision_hash=worker_artifact_hash` `:285-305`. Yargıç "hangi değişikliği" denetleyeceğini zarftan çıkaramaz.
- `artifact_mismatch` kontrolü alt-dizge araması ve `impl_artifacts_ref` ya da `worker_artifact_hash` boşsa tamamen atlanır (fail-open) `:247-266`.

### risk_policy.py (213)

- Üretim: `record_risk_decision_for_pr` ← `merge_authority.py:69-78`; `classify_change` ← `auto_merge.py:177-179`, `human_required_adjudication.py:295-301`. Şema 7 ("risk politikası şeridi").
- EKSİK-KOD: `docs/aria/policy/risk-policy.json` `auto_merge_candidate_lanes: ["L1"]` beyan eder; kod yalnız varlığını doğrular `:157-160`, hiçbir yerde okumaz (repo-genel grep). Hangi şeridin merge edilebileceğini yalnız `autonomy_unlock` belirler.
- RİSK (sınıflandırma): L1 = "docs_tests_low_risk" şunları içerir: `aria-kernel/tests/**` (→ `aria-kernel/tests/invariants/**` de; READONLY_PATHS bunu "invariant test self-mod" diye korur `implementation_safety.py:84`) ve `tools/aria-adapters/*.tool.json` (adaptör manifestleri: `runner.argv`, kapsam, `status`) — yürütülen komutu ve okuma kapsamını tanımlayan dosyalar en düşük risk şeridinde. Sıralama L3→L2→L1 `:173-179`.
- `fnmatch` `*` `/`'yi de eşler; `**/test_*.py` kökteki `test_x.py`'yi EŞLEMEZ (salt-okuma doğrulandı) → kök düzey test dosyası `risk_unknown_path` ile bloke `:182-183`.

### rule_health.py (184)

- Üretim: `quarantined_rules` ← `feedback_store.py:1218-1220` (örnekleyici o kuralın bulgularını yargıya göndermez); `commit_rule_defect_findings` ← `cycle.py:1905-1907` (judgment_pipeline); `rule_stats` ← `reflection.py:1342`. Şema 10 "kural sağlığı FP ≥ 0.75 → karantina + adaptör kural kusuru bulgusu" — DOĞRU; ek eşik `MIN_JUDGED_FOR_QUARANTINE = 3` ground-truth yargı `:46-47`.
- "Kural karantinası" kuralın ÇALIŞMASINI durdurmaz, yalnız yargı örneklemesinden çıkarır (`:17-18`); adaptör ham bulgu üretmeye devam eder (doc-staleness'ın 29 939 satırı bu sınıfta).
- Performans: `_fingerprint_rules` tüm `raw-findings.jsonl`'ı (aria/state'te 34 500 satır) okur, kompakt satırlarda artefakttan çözer `:50-77`; `commit_rule_defect_findings` döngü İÇİNDE her karantinalı kural için `rule_stats(root)`'u yeniden hesaplar `:150` → O(kural × ham bulgu).

### runner_attestation.py (264)

- Üretim: `verify_runner_attestation` ← `merge_authority.py` (merge zorunlu kapısı); `probe_runner_attestations_for_claims` ← CLI `cli.py:3674-3680` ← composite action `.github/actions/probe-runner-attestation/action.yml:62-65` ← `aria-auto-cycle.yml:557-559`, `aria-agent-executor.yml:562-564`. Şema 7 ("runner attestation").
- YAPISAL BLOKAJ (kanıtlı): action `runner-group`, `ephemeral`, `group-approved` girdilerini `required: true` ve "no default" diye tanımlar (`action.yml:29-47`) fakat İKİ workflow da yalnız `tools-dir` geçer (`aria-auto-cycle.yml:558-559`, `aria-agent-executor.yml:563-564`); composite action'da eksik girdi boş dizge olur → `ARIA_RUNNER_EPHEMERAL=""` → `_env_flag` False `:209-210,231-232` → `_validate_attestation` her zaman `runner_attestation_ephemeral_runner_required` / `…approved_group_required` ile reddeder `:246-249`. Attestation satırı hiç yazılamaz → `merge_authority` her PR'da `runner_attestation_required_for_merge` ile düşer. (Ayrıca runner kalıcı self-hosted: `preflight.py:606-608` "persistent self-hosted runner" — ephemeral=true gerçeği de değil.)
- TUTARSIZ (auth tanımı): burada "managed" = `CLAUDE_CODE_OAUTH_TOKEN` env'i VAR `:125,141-143`; çalışma zamanı ise `claude auth status` → `authMethod == "claude.ai"` ister ve oauth_token modunu reddeder (`tools/aria-poc/status_answers.py:40-43,88`; review §6). İki "managed auth" tanımı çelişir.
- "platform_verified" yalnız `ACTIONS_ID_TOKEN_REQUEST_URL/TOKEN` env'lerinin VARLIĞI; OIDC token talep/doğrulanmaz `:152-155`.

### runner_availability.py (241)

- Üretim: `python3 -m aria_kernel.runner_availability` ← `.github/actions/require-self-hosted-runner/action.yml:73` ← `aria-auto-cycle.yml:137` vb. (hosted preflight job). Import eden Python modülü yok (CLI entrypoint). Şema 21 (hiçbir şemada adı yok → EKSİK-DOKÜMAN): self-hosted runner offline iken koşunun sessizce iptal edilmesi yerine adı konmuş hata ile kırmızı düşmesi; App token `administration:read` ile mint edilip okuma sonrası revoke edilir `:167-190`.
- `busy` online runner "uygun" sayılır `:96-98`.

### runs_reader.py (102)

- Üretim: 13 modül (`pressure`, `readiness`, `feedback_store`, `tool_health`, `memory`, `runtime_artifacts`, `architecture_spine_gate`, …). Şema 6/10.
- HATA (küçük): `upcast_run_row` docstring "source row'u mutasyona uğratmadan" `:53`; `dict(row)` sığ kopya, `runner` iç sözlüğü aynı nesne → `runner.setdefault(...)` kaynak satırın `runner`'ını değiştirir `:56,64-69`.
- Her okuma tüm runs.jsonl'ı strict zincirle yeniden okur; `pressure._raw_finding_delta` gibi döngü-içi çağıranlarda O(n²).

### runtime_artifacts.py (2632)

- Üretim: `write_run_artifact/append_run_by_cycle` ← `tool_runner`; `verify_runtime_artifacts` ← `promotion_controller.py:116`, `state_store`/`state_continuity_gate`/`integrity`/`doctor`; `classify_cycle_evidence` ← `promotion_controller.py:117`; `autonomy_output_summary/autonomy_exit_code` ← `cli.py` (autonomy run çıktısı); `resolve_finding_from_artifact` ← `feedback_store`, `rule_health`; `retention_*`, `restore_artifact`, `approve_runtime_v2_promotion` ← CLI. Şema 15 (kapsam tablosu), Şema 6.
- EKSİK-DOKÜMAN: ham bulgu/koşu yükleri `run-artifacts/hot/<cycle>/<run>/<kind>.json` artefaktlarında, satırlar yalnız ArtifactRefV2 işaretçisi (`uri+sha256+produced_by_workflow_run_id`) taşır; artefakt kaybı "compaction attestation" (`runtime_artifact_compactions` ledger'ı, ARIA-HIGH-117) ya da retention arşivi ile meşrulaşır `:636-700,1789-1830`. Şema 6'da "gzip arşiv + yeniden zincirleme" dışında bu üç-yollu doğrulama (verified / retained / compacted) yok.
- TUTARSIZ (modül içi): `autonomy_output_summary` ARIA-HIGH-098'e göre non-ok aracı `degraded`, yalnız artefakt-kayıp sınıfını `failed` sayar `:1566-1579`; `classify_cycle_evidence` ise TEK bir non-ok koşu ya da beklenen araç koşusunun eksikliğini `INTEGRITY_FAILED` yapar `:877-887` → `promotion_controller` `cycle_not_artifact_bearing` ile bloklar (`promotion_controller.py:120-121`). Bir SHADOW adaptörün timeout'u o cycle'dan plan terfisini imkânsız kılar.
- `classify_cycle_evidence` yalnız `runs.jsonl`'ı okur `:873`, `verify_runtime_artifacts` ise `runs/by-cycle/<id>.jsonl`'ı da ekler `:915-918` — iki okuyucu farklı koşu kümesi görebilir.
- KAYMA (yorum): `:267-272` "by-cycle is the ONLY runs ledger published to aria/state" — `origin/aria/state`'te `tools/runs.jsonl` de yayımlı (`git ls-tree`: `tools/runs.jsonl` + 31 by-cycle dosyası).
- Operatör onayı tutarsız: `approve_runtime_v2_promotion` ref'i `operator_approval.verify_operator_approval_ref` ile kayıtlı eyleme karşı çözer (ARIA-AUDIT-015) `:986-998`; `retention_apply`, `restore_artifact`, `rollback_retention` ise yalnız boş-olmayan string ister `:1070-1071,1165-1166,1215-1216`.
- `retention_apply` arşive kopyalar ama sıcak dosyayı SİLMEZ (tahliye "R3"e bırakılmış yorumu `:1115-1116`) → retention disk kazandırmaz. `rollback_retention` kopyalamadan önce arşiv hash'ini doğrulamaz, hedefi ezer, sonra hash'ler `:1228-1234`.
- `write_run_artifact` her istisnayı `artifact_status: write_failed` dönüşüne çevirir `:173-180` (çağıran satırı yine yazar; özet bunu integrity_failed sayar `:1554-1560`).
- `quarantine_count` ve `scope_out_count` aynı formül (`status == scope_violation`) `:1644-1645` — "karantina sayısı" gerçek karantinayı saymaz.
- `CompactedArtifacts.attesting`: compaction satırında `sha256` boşsa aynı id/uri'deki HER sürümü onaylar `:686-691`.
- `DEFAULT_RUN_LEDGER_FORMAT = "v2-shadow"`; `v2` operatör onaylı promosyon kaydı + tools kimlik hash'i + HEAD eşleşmesi ister `:30-32,1038-1079`.
