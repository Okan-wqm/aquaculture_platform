name: Performance Benchmark

# ARCH-CI-013: Fixed path triggers from non-existent frontend/**,backend/** to web/**,apps/**
# SEC-CI-002: All third-party actions pinned to full commit SHAs
# SEC-CI-018: pull_request trigger restricted to non-fork PRs only to prevent
#             execution of malicious package.json scripts from fork contributors

on:
  workflow_dispatch:
  pull_request:
    branches: [ main, develop ]
    paths:
      # ARCH-CI-013: Was 'frontend/**' and 'backend/**' — project uses web/** and apps/**
      - 'web/**'
      - 'apps/**'
      - '.github/workflows/performance-benchmark.yml'

jobs:
  lighthouse:
    name: Frontend Lighthouse CI
    runs-on: ubuntu-latest
    # SEC-CI-018: Skip on fork PRs to prevent execution of potentially malicious
    # package.json scripts contributed by fork authors (npm run preview runs project scripts)
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      # SEC-CI-002: pinned to SHA
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node
        # SEC-CI-002: pinned to SHA (was @v3)
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --legacy-peer-deps --ignore-scripts --no-audit

      - name: Build
        run: npm run build
        env:
          NX_DAEMON: 'false'
          NX_NO_CLOUD: 'true'

      - name: Start server
        run: |
          npm run preview &
          echo $! > server.pid
          sleep 5

      - name: Run Lighthouse
        # SEC-CI-002: pinned to SHA (was @v11)
        uses: treosh/lighthouse-ci-action@2f8dda0cf82e7b8f5b3e1e7d0dd88b9e35d5c2d4 # v11.4.0
        with:
          urls: |
            http://localhost:4173/
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Stop server
        if: always()
        run: |
          kill $(cat server.pid) || true

  k6-api:
    name: Backend k6 API benchmark
    runs-on: ubuntu-latest
    # SEC-CI-018: Skip on fork PRs (same guard as lighthouse job above)
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    steps:
      # SEC-CI-002: pinned to SHA
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup k6
        # SEC-CI-002: pinned to SHA (was @v1)
        uses: grafana/setup-k6-action@e5e8a2b7a6e30c9ee48bfbd39e5f8b6d6a7d4a34 # v1.1.0

      - name: Check if performance test script exists
        id: check-script
        run: |
          if [ -f "tests/performance/api-smoke.js" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "::warning::tests/performance/api-smoke.js not found — skipping k6 run."
          fi

      - name: Run k6 tests
        if: steps.check-script.outputs.exists == 'true'
        run: |
          k6 run tests/performance/api-smoke.js --vus 10 --duration 30s

      - name: Upload k6 results
        if: steps.check-script.outputs.exists == 'true'
        # SEC-CI-002: pinned to SHA (was @v3)
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.0
        with:
          name: k6-results
          path: ./k6-results/*

  summary:
    needs: [lighthouse, k6-api]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Compose summary
        run: |
          echo "# Performance Benchmark Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Lighthouse and k6 benchmarks have completed." >> $GITHUB_STEP_SUMMARY
          echo "- Review uploaded artifacts for detailed results." >> $GITHUB_STEP_SUMMARY
