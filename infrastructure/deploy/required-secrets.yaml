# Generated from platform service catalog.
# source: platform/libs/service-catalog/src/index.ts
# generator: scripts/service-catalog/generate-artifacts.ts
# generatorVersion: 1
# catalogHash: 099540e84aeb58d6adc570770f1368ac72c27a3d152a82dc3a89b4571ba2709e
# do not edit by hand
version: 1
compose_files:
  - docker-compose.droplet.yml
runtime_required_env:
  - name: MINIO_USER
    owner: platform-service-catalog
    class: runtime-env
    purpose: "runtime deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "operator supplied per release"
    provisioning_mode: "deploy environment"
    required_by: [docker-compose.droplet.yml]
  - name: TAG
    owner: platform-service-catalog
    class: runtime-env
    purpose: "runtime deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "operator supplied per release"
    provisioning_mode: "deploy environment"
    required_by: [docker-compose.droplet.yml]
secrets:
  - name: CONFIG_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: CREDENTIAL_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: INTERNAL_SERVICE_SECRET
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MFA_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MINIO_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_AUTH_SECRET
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_SENSOR_SERVICE_HASH
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_SENSOR_SERVICE_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: OBSERVABILITY_INTERNAL_API_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: PASSWORD_PEPPER
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: POSTGRES_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: REDIS_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: SUPER_ADMIN_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: WEBHOOK_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
