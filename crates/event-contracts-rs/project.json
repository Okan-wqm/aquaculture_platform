{
  "name": "event-contracts-rs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "crates/event-contracts-rs/src",
  "tags": ["scope:rust", "type:crate", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "build", "package": "event-contracts-rs", "release": false },
      "outputs": ["{workspaceRoot}/target/debug/libevent_contracts_rs.rlib"],
      "cache": true
    },
    "build-release": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "build", "package": "event-contracts-rs", "release": true },
      "outputs": ["{workspaceRoot}/target/release/libevent_contracts_rs.rlib"],
      "cache": true
    },
    "test": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "test", "package": "event-contracts-rs", "allFeatures": true },
      "cache": true
    },
    "lint": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "clippy", "package": "event-contracts-rs", "args": ["--all-targets", "--all-features", "--", "-D", "warnings"] },
      "cache": true
    },
    "fmt-check": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "fmt", "package": "event-contracts-rs", "args": ["--", "--check"] },
      "cache": true
    }
  }
}
