<!-- ARIA-CURRENT-STATE-NOTICE: İkinci tur adversarial doğrulama raporu (2026-09-25, main @ 19efd07f,
aria/state @ e6f462fb); docs/aria/CURRENT_STATE.md ve executable contracts'a tabidir. Ham kanıt
kaydıdır, runtime otoritesi değildir. -->

# V03 — B03 ikinci tur adversarial doğrulama (burn_in … control, 26 dosya)

Kapsam: `docs/aria/reviews/2026-09-25-aria-tam-okuma/B03.md` + ana kayıt §8.1-C, §3.2 cycle
satırları, §2 K4/K5/K14, §1 change-ledger satırı, §8.3/§8.4 B03 maddeleri, MIMARI tam okuma notları
(Ş7, Ş12, Ş13, Ş17, Ş22, Ş24). HEAD ffe0d4a6; canlı durum `origin/aria/state` @ 06b4ba4a (2026-09-25
13:12 +0200, tur-1'den SONRA bir executor push'u geldi). Tüm kontroller salt-okuma: grep, `sed`,
`python3 -c` ile import + saf fonksiyon/sabit sondası.

## Özet

|               | Sayı                                                                                              |
| ------------- | ------------------------------------------------------------------------------------------------- |
| Toplam iddia  | **96**                                                                                            |
| DOĞRULANDI    | **81**                                                                                            |
| KISMEN        | **15**                                                                                            |
| ÇÜRÜTÜLDÜ     | **0**                                                                                             |
| DOĞRULANAMADI | **0** (3 risk maddesi çıkarım olarak işaretlendi; kod yapısı doğrulandı, canlı etkisi gözlenmedi) |

Birinci turun B03 grubu çok sağlam. Çürütülen iddia yok. Kısmen doğru olanların çoğu sayı kayması
(canlı state 13:12'de büyüdü) ya da eksik kalmış çağıran. Önemli iki düzeltme var:
`closure_reachability` CI'da **birim testi üzerinden koşuyor**, ve capability resolver canlıda
**40/40 `request`** üretiyor (`extend` de hiç üretilmemiş).

## Çürütülen ve kısmen doğru iddialar (öncelikli)

| #   | Kaynak                                                                                     | İddia                                                                                                   | Gerçek                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Kanıt file:line                                                                                                                                                                | Verdikt                |
| --- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------- |
| 1   | B03 envanter `closure_reachability`; B03 "Bağlı olmayan" son madde; ana kayıt §8.4         | "yalnız `cli.py:3510`; workflow'da çağıran yok"; "CI'da ya da cycle'da onu tetikleyen bir kapı yok"     | CLI'ı çağıran workflow yok, bu doğru. Ama `aria-kernel/tests/test_closure_reachability.py:78-84` **gerçek repoyu** tarıyor ve `violations == ()` doğruluyor. Bu test `aria-kernel.yml:121` → `scripts/ci/aria-suite-run.sh:40,56` (`unittest discover aria-kernel`) ile her PR'da koşuyor (`aria-kernel/**`, `docs/aria/**` path filtresi). Yani CI kapısı var, ama `docs/reviews/orphan-findings.md` (ledger) path filtresinde olmadığı için yalnız ledger değiştiğinde tetiklenmiyor. Baseline canlıda 2 pin taşıyor. | `aria-kernel/tests/test_closure_reachability.py:78-84`; `.github/workflows/aria-kernel.yml:17-30,121`; `scripts/ci/aria-suite-run.sh:56`                                       | KISMEN                 |
| 2   | B03 B9; MIMARI Ş13 notu ("`extend` hiçbir şeyi değiştirmiyor")                             | `extend` = "liste boş değil"; çağıranlar yalnız `reuse`'a bakıyor; `reuse` pratikte erişilemez          | Özü doğru, ama durum daha sert: `related_existing_agents` hiç dolmuyor. `related_agents_for_paths` `latest_agent_priors`'a dayanıyor, onu yazan `map_agent_priors`'ın çağıranı yok. Registry ve observation gap'leri `related_agents=[]` literaliyle kuruluyor. Bu yüzden `existing` her zaman boş, karar her zaman **`request`**. Canlıda `capability-resolution/decisions.jsonl` 40 satır: **40/40 `request`** (20 agent, 20 skill). Ne `reuse` ne `extend` hiç üretilmemiş.                                          | `agent_priors.py:72-86`; `capability_gap.py:154,191,208,225,245,436`; `capability_resolver.py:27-37`; `git show origin/aria/state:tools/capability-resolution/decisions.jsonl` | KISMEN                 |
| 3   | B03 B3 + risk #5                                                                           | Feedback satırları `metadata` taşımıyor, "canlıda 138/138"; 138 satırın hepsi `ai_judge`/`ai_consensus` | Mekanizma aynen doğru: `feedback_store.py`'de `metadata` alanı yok; anahtar `tool_id` (`*-adapter`); `SOURCE_WEIGHTS`'ın 14 anahtarından hiçbiri eşleşmiyor, bu yüzden çarpan her kaynakta 1.0. Sayı ise eskimiş: 13:12 push'undan sonra **157/157** (148 `ai_judge` · 9 `ai_consensus`). 138, `923dacc5` (09-21) sürümündeki sayıydı.                                                                                                                                                                                  | `calibrated_intelligence.py:83-103,111-135`; `pressure.py:238-248`; `git log origin/aria/state -- tools/operator-feedback.jsonl`                                               | KISMEN (sayı)          |
| 4   | B03 B11 / envanter `cli.py`                                                                | "370 `add_subparser`"                                                                                   | `grep -c "add_subparser("` = 370 ama 1'i tanım satırı, yani **369 çağrı**. Çalışma zamanında `build_parser()` ağacında **384 parser** var, **85 üst komut** ("~90" değil). Doküman ("~170") ve kod yorumu ("168", `cli.py:241`) için KAYMA sonucu değişmiyor.                                                                                                                                                                                                                                                           | `python3 -c` ile `cli.build_parser()` sondası                                                                                                                                  | KISMEN                 |
| 5   | B03 risk #14                                                                               | `governance.jsonl` canlıda 9.4 MB / 11 279 satır (~%56)                                                 | Şu an **9 667 643 B (9.2 MiB) / 11 523 satır**. Tavanlar doğru (`_ASSESSMENT_CAPTURE_BYTES=16 MiB`, `_ASSESSMENT_CAPTURE_ROWS=20_000`, `change_outcome.py:185-186`). Akış zaten ölü (`_record_change_assessment`'in üretim çağıranı yok).                                                                                                                                                                                                                                                                               | `git cat-file -s origin/aria/state:tools/governance.jsonl`                                                                                                                     | KISMEN (sayı)          |
| 6   | Ana kayıt §3.2 "Post-tool hataları cycle'ı çökertmez" satırı                               | "Varsayılan `halt_sequence`: sonraki halt fazları `upstream_failure` ile atlanır" (`cycle.py:477`)      | Varsayılan `cycle.py:478`'de. Tetikleyici yalnız bir halt_sequence fazının hatası değil: `_first_phase_failure` **her `failed` sonucu** sayıyor, `record_and_continue` fazlarınınkini de (yalnız `swallow`→`degraded` hariç). Örneğin preflight'taki `mission_reconcile` ya da post_tool'daki `tool_degradation` hata verirse memory, pressure, mission_ingest, consensus, HR-adjudication, judge_calibration, proactive ve reflection fazlarının hepsi atlanıyor. Kod yorumu (`:295-297`) da bunu söylemiyor.          | `cycle.py:295-306,478,3627-3630,3663-3666,3841-3851`                                                                                                                           | KISMEN                 |
| 7   | Ana kayıt §1 `tools/change-ledger/*` "0 commit" + §8.1-C "Etki: Zincir committed'da durur" | Değişim zinciri hiç işlemedi / committed'da duruyor                                                     | Canlıda `change-ledger/planned.jsonl` **2 satır** var (F-009 `chg_7768f560b58a346d`, F-010 `chg_920bd2209e2ce825`, experiment/apply yolu). `committed.jsonl` ve `validated.jsonl` yok. Yani canlı zincir **planned'da** duruyor; committed'a bile ulaşmamış. Kod düzeyinde "validated otomatik yazılmaz" iddiası doğru (bkz. D-1), ama "committed'da durur" canlıda henüz sınanmadı.                                                                                                                                    | `git ls-tree -r origin/aria/state tools/change-ledger`                                                                                                                         | KISMEN                 |
| 8   | B03 envanter `candidate_blocks`                                                            | çağıranlar `task.py:161`, `mission.py:1116`                                                             | Ayrıca `capability_gap.py:11` de import ediyor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | grep                                                                                                                                                                           | KISMEN (eksik çağıran) |
| 9   | B03 envanter `capability_gap`                                                              | okuyucular `task.py:132`, `agent_genesis.py`, `learning.py:363`                                         | Ayrıca `self_improvement.py:182` var.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | grep                                                                                                                                                                           | KISMEN (eksik çağıran) |
| 10  | B03 envanter `control`                                                                     | çağıranlar listesi                                                                                      | Ayrıca `doctor.py:341` var. `ci_executor.py:4876,5087` import değil, kullanım satırları (import `:4619`).                                                                                                                                                                                                                                                                                                                                                                                                               | grep                                                                                                                                                                           | KISMEN (eksik çağıran) |
| 11  | B03 envanter `constants.py`                                                                | "yalnız test (`tests/test_enterprise_cycle.py:15,30`)"                                                  | Import yalnız `:30`'da. Özü (üretimde sıfır import) doğru.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `git grep aria_kernel.constants`                                                                                                                                               | KISMEN (satır)         |
| 12  | B03 "Hiçbir dokümanda olmayan" — Plan 032/033 CLI yüzeyi, `gateway serve`                  | "MIMARI ve NASIL'da hiçbiri geçmiyor"                                                                   | `gateway serve`, `mcp serve --allow-writes`, `8787`, security/SARIF/ZAP, `event ingest`, checkpoint, control MIMARI ve NASIL'da gerçekten geçmiyor. Ama **aria-gateway daemon'ı** MIMARI'de orijinalinden beri çizili: Ş0 `:17`, Ş8 `DA1[aria-gateway]` `:335`, Ş16 `:620`, Ş9 "gateway webhook" `:398`. Eksik olan, HTTP dinleyici ayrıntısı ve CLI verb'ü.                                                                                                                                                            | `ARIA-MIMARI-SEMALARI.md:17,335,398,620`; `git show 2b6dfaba:ARIA-MIMARI-SEMALARI.md`                                                                                          | KISMEN                 |
| 13  | MIMARI Ş13 notu                                                                            | "`agent_priors.map_agent_priors` çağrılmadığı için reuse tespiti de ölü"                                | Doğru, ama **extend tespiti de** ölü (bkz. #2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | yukarıda                                                                                                                                                                       | KISMEN                 |
| 14  | B03 risk #17                                                                               | `calibration_bootstrap.py:5-7`: "6 SHADOW adapter", "snowball"                                          | "6 SHADOW" `:5`'te, "snowball" ise `:13`'te.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `calibration_bootstrap.py:5,13`                                                                                                                                                | KISMEN (satır)         |
| 15  | Ana kayıt §9 "Satır referansları ve sayılar güncellendi" (B03 kapsamı)                     | MIMARI düzeltildi                                                                                       | B03'ün bulduğu KAYMA'ların MIMARI'deki metni **düzeltilmemiş**, üstelik not da eklenmemiş: Ş8 `:340` "~170 alt komut"; Ş9 `:406` clamp (yapısal no-op notu yok); Ş13 `:543` "6 kaynak"; Ş17 `:661` "change_chain_stale uyarısı" (üreticisi ölü); Ş19 `:710` "asla büyümez"; Ş22 `:853` "aşım → GovernanceError"; Ş7 `:301` "9 hata türü" (5'inin üreticisi yok; `breakers/` yayımlanmıyor). §8.4'te listelenmişler ama şemada okuyucu bunları görmüyor.                                                                 | `grep -n` ARIA-MIMARI-SEMALARI.md                                                                                                                                              | KISMEN                 |

## Ana kayıt / şema dokümanı için gereken düzeltmeler

1. **Ana kayıt §1, `tools/change-ledger/*` satırı.**
   - Şu an: "0 commit | değişim zinciri hiç işlemedi"
   - Olmalı: "2 planned (F-009, F-010) · 0 committed · 0 validated | zincir planned'da durdu;
     committed'a hiç ulaşmadı"
2. **Ana kayıt §8.1-C, "Etki" hücresi.**
   - Şu an: "Zincir "committed"da durur"
   - Olmalı: "Kod gereği zincir committed'ı geçemez (auto-merge üçlü kapısı ve `change_outcome`
     validated satırı ister). Canlıda zincir daha erken, planned'da durdu (2 planned, 0 committed)."
3. **Ana kayıt §3.2, "Post-tool hataları cycle'ı çökertmez" satırı.** Kod hücresine şu cümle
   eklenmeli: "`_first_phase_failure` herhangi bir `failed` fazı sayar; `record_and_continue`
   fazlarının (ör. `mission_reconcile`, `tool_degradation`, `pr_ci_scan`) hatası da sonraki tüm
   `halt_sequence` fazlarını (memory…reflection) atlatır." Kanıt `cycle.py:478,3627-3630,3841-3851`
   olmalı (`:477` değil).
4. **Ana kayıt §8.4, `closure_reachability` satırı** şuna genişletilmeli: "`--write` o anki tüm
   ihlalleri iğneler. Kapı CLI'da değil, `aria-kernel/tests/test_closure_reachability.py:78`
   üzerinden `aria-kernel.yml` PR koşusunda çalışır. `docs/reviews/orphan-findings.md` path
   filtresinde yok."
5. **MIMARI Ş13 notu** şöyle olmalı: "`agent_priors.map_agent_priors` çağrılmadığı için
   `related_existing_agents` hep boş. Resolver canlıda 40/40 `request` üretti; `reuse` de `extend`
   de hiç üretilmedi."
6. **MIMARI satır içi düzeltmeler** (ya da Ş8, Ş9, Ş17, Ş19, Ş22 için tam okuma notu):
   - `:340` "~170 alt komut" → "~370 alt komut (85 üst komut)"
   - `:543` "6 kaynak" → "7 kaynak (+ bilinmeyenler)"
   - `:661`'e "(üretici `emit_stale_chain_warnings` hiç çağrılmıyor)" eklenmeli.
   - `:710` "asla büyümez" → "`--write` o anki tüm ulaşılamaz kümeyi iğneler"
   - `:853` "aşım → GovernanceError" → "mint yolunda yalnız audit (`enforce_context_budget=False`);
     enforce yalnız `context-budget` CLI'da"
   - `:852` "acil 0.65" → "`human_required_packet` üretim rolü değil; `human_required_adjudication`
     0.40"
   - `:406`'ya "şu an yapısal no-op: satır anahtarı `tool_id`, `SOURCE_WEIGHTS` anahtarlarıyla
     eşleşmiyor" eklenmeli.
   - `:301`'e "5 türün üreticisi yok; `breakers/` aria/state'e yayımlanmıyor" eklenmeli.
7. **B03.md B3 ve risk #5:** "138/138" → "157/157 (06b4ba4a; 138 = 923dacc5)".
8. **B03.md risk #14:** "9.4 MB / 11 279" → "9.2 MiB / 11 523 (06b4ba4a)".
9. **B03.md B11:** "370 `add_subparser`" → "369 çağrı / 384 parser / 85 üst komut".

## Doğrulanan iddialar

### Ana kayıt, bana verilen maddeler

- D-1 **§8.1-C (çekirdek iddia):** `emit_change_validated`'ın üretim çağıranı yalnız `cli.py:5303`
  (manuel `change validate`) ve `tools/aria-poc/backfill_validated_chains.py:79`. Tüm repo tarandı:
  aria-kernel, tools/, .github/, scripts/, infrastructure/, .claude/. Hiçbir workflow, script ya da
  cycle bu CLI'ı ya da backfill'i çağırmıyor. Dinamik yol da yok: `validated.jsonl`'a başka yazan
  yok (`state_manifest.py:412` yalnız beyan). Cycle'ın `validation_matrix` fazı
  (`cycle.py:2880-2925`) validated satırını yalnız okuyor. Auto-merge üçlü kapısı
  `auto_merge.py:551-556`, `change_outcome` `:514-517`. ✔
- D-2 §3.2: `run_enterprise_cycle` `cycle.py:541`, `CYCLE_PHASES` `:3109` (kapanış `:3485`). ✔
- D-3 §3.2: bilinmeyen `mode` → `GovernanceError` (`cycle.py:584-587`); faz listesi girdi değil;
  `_assert_pipeline_is_well_formed` `:3488`, import anında çağrılıyor (`:3530`). ✔
- D-4 §3.2: pre_tool = `tool_manifest_sync` + `architecture_baseline` (`cycle.py:3199-3207`);
  `validation_matrix` ve `pr_lifecycle` post_tool'un sonunda (`:3475-3484`); `artifact_integrity`
  `propagate` (`:3452`). ✔
- D-5 **K4:** `len(CYCLE_PHASES)==47`, `python3` ile doğrulandı. NASIL §4 tablosu 16 satır; orijinal
  MIMARI (`2b6dfaba`) `:25,:585`'te "46 faz". ✔
- D-6 **K5:** `heartbeat.py` yok. İşleri `_phase_judgment_pipeline` (`cycle.py:1685-1700`),
  `_phase_fixture_refresh` (`:1955-1960`) ve ayrıca `calibration_recommendation` (`:2090-2096`)
  üstlendi. NASIL `:189` hâlâ `heartbeat_tick` diyor. ✔
- D-7 **K14:** `complete = len(allowed_paths) <= len(fates) and not missing` (`discovery.py:96`).
  `allowed_paths`, `fates`'in `tracked` alt kümesi (`snapshot.py:92`), dolayısıyla karşılaştırma
  totoloji. Committed modda kirli yollar yalnız uyarılıp dışarıda bırakılıyor
  (`discovery.py:39-57`). ✔
- D-8 §8.1-E `cli.py:2377-2388`: `--challenger-timeout-seconds 300`, `--max-rounds 2`; workflow
  `--max-rounds` geçmiyor (`aria-auto-cycle.yml:810-817`); orkestratör varsayılanı 4
  (`autonomy_orchestrator.py:928`). ✔
- D-9 §8.3 breaker maddesi, §8.4 bağlam bütçesi / Beta-Binomial / closure_reachability satırları
  (closure için #1'deki nüans geçerli). ✔
- D-10 MIMARI Ş7, Ş17 ve Ş24 RC-C notlarındaki "`change_validated` otomatik yazılmıyor"; Ş12 notu
  ("`cycles_rejected` hiçbir kararı etkilemiyor"); Ş22 notu (`enforce_context_budget=False`). ✔

**B03 envanteri: 21 satır doğru.** burn_in (`cli.py:6068`, `aria-auto-cycle.yml:849`,
`aria-operational-proof.yml:124`), calibrated_intelligence, calibration, calibration_bootstrap,
canonical_path, capability_resolver, change_ledger, change_outcome (`cycle.py:1131`), checkout_root,
checkpoint, claude_settings, command_policy, confidence, containment_probe
(`implementation_safety.py:757`), contention_replay, context_budget_gate, context_compiler,
contract_digest, burn_in şema eşlemesi vb.

### B03 tutarsızlık tablosu

- B1 ✔ (D-1).
- B2 ✔: `cycles_rejected`'i yalnız `knowledge_graph.py:939` yazıyor ve `doctor.py:275` okuyor.
  Thompson `cycles_merged`/`cycles_minted` kullanıyor (`mission_scheduler.py:218-224`), sıralama
  `converged/minted` (`knowledge_graph.py:1068-1071`). `funnel_health` bu alanı yalnız docstring'de
  anıyor.
- B4 ✔: `ROLE_CAP_MAP` `context_budget_gate.py:81-107`; `enforce_context_budget=True` geçen üretim
  kodu yok (`agent_invocations.py:1207,1586`); `human_required_packet` rol değil
  (`agent_surface.py:53`).
- B5 ✔: `FAILURE_KINDS` 9 tür (`circuit_breaker.py:76-103`). Üreticiler yalnız `subprocess_timeout`
  (`planner_dispatch_hook.py:634`, `ci_executor_drain.py:508,64`), `state_integrity_gap`
  (`memory_gap.py:723`) ve `executor_environment/selection_failure` (`ci_executor_drain.py:65,68`).
  `validator_rejection`, `sandbox_red`, `ci_red`, `gh_api_failure`, `operator_rollback` için üretici
  yok (`delivery_closure`'daki `ci_red` başka bir enum).
- B6 ✔: `state_manifest.py`'de `breakers`/`autonomous-failures` yok; `origin/aria/state`'te yok.
- B7 ✔: `closure_reachability.py:196-221`.
- B8 ✔: stale fonksiyonların çağıranı yok.
- B10 ✔: 7 kaynak (`capability_gap.py:72-81`).
- B12 ✔: bkz. D-8.
- B13 ✔: `command_policy.py:353-371`.
- B14 ✔: `produce_ci_review` → `_failure_by_id` zinciri ölü;
  `security`/`tenant`/`database`/`test`/`infra` ajanları `.claude/agents`'ta yok.
- B15 ✔: `contention_replay.py:65-71,611-614`.
- B16 ✔: `evidence_trust.py:241-257`.

**Ölü kod listesi (D1–D11)**

- ci.py'deki 7 fonksiyonun üretim çağıranı yok. `workflow-inventory.jsonl` canlıda yok, dolayısıyla
  `_workflow_class_for_run` hep `pr_required` döndürüyor (`ci.py:523-531`). ✔
- change_ledger stale fonksiyonları ve `enforce_validation_matrix=False` üretimde kullanılmıyor. ✔
- `_record_change_assessment` (`change_outcome.py:984`) üretim çağıranı yok; `validation_runs`
  `:119`'da kabul edilen kaynaklar arasında. ✔
- `require_capability_resolution` ve `reject_duplicate` üretilmiyor. ✔
- `list_calibration_recommendations` yalnız testte;
  `label_finding`/`finalize_corpus`/`list_corpus_status` yalnız `__main__` (`:337-389`);
  `adapter-run` komutu yok. ✔
- constants ✔.
- `from aria_kernel.circuit_breaker import *` gerçekten
  `AttributeError: ... 'assert_within_breaker'` fırlatıyor (sondalandı). `autonomous-state.json`
  yazılıyor (`:445,520`) ama okuyan yok. ✔
- 0.65 tavanı ölü. ✔
- `contention_replay.py:631-636`: `transaction is None` dalı erişilemez, çünkü `:502-520` kendi
  transaction'ıyla özyinelemeli çağırıyor. ✔
- `burn_in.py:247-258` gereksiz kontrol (`_validate_args` `:375-380`'de zaten zorluyor). ✔
- `CONFIDENCE_KINDS`: yalnız `instinct_score` kullanılıyor, onun tek çağıranı `instinct_candidate`
  da ölü. ✔

**Risk maddeleri**

- #1 ✔: `is_runtime_path(" M aria-tools/x")=True`, strip'lenmiş hali `False` (sondalandı).
  `worktree.py:93-96` bu hatayı bilerek ham subprocess kullanıyor.
- #2 ✔: zap validate başarı dalında `return` yok (`cli.py:6384-6393`), akış `parser.error`
  (`:6604`) → exit 2'ye düşüyor.
- #3 ✔: iki ayrı yazım (`:423`, `:426`), reset `write_text("")` (`:522`), quarantine `write_text`
  (`:655`), ham `append_jsonl`. Docstring'deki varsayılan pencere "72" diyor, gerçek varsayılan **96
  saat** (`minimum_window_hours(3)`, sondalandı).
- #4 ✔: `finalize_corpus` idempotent değil; `record_seeding_finding` dedup yapmıyor,
  `feedback_store.py:470`'deki yorum yanlış.
- #5 ✔: judge_calibration'daki insan-imza filtresi (`judge_calibration.py:83-93`) burada yok.
- #6 ✔: `cli.py:5624,5786`.
- #7 ✔: `cli.py:1856` (`required=True`, env varyantı yok).
- #8 ✔: `autonomy_orchestrator.py:1159-1160` yalnız kaydediyor; `cli.py:5933-5944` "telemetry, not
  a gate".
- #9 ✔: `checkpoint.py:285-293`; `list_checkpoints` yalnız `taken` filtreliyor, `pruned` ref'leri
  dışlamıyor (`:100-113`); `max_total_mb` yalnız raporlanıyor (`:350-351`); take `repo=cwd`
  (`ci_executor.py:1740`), restore `_REPO_ROOT` (`:1758,486`). Sonuncusu risk olarak doğru.
- #10 ✔: autonomy_orchestrator ve cycle `control`'ü hiç import etmiyor.
- #11 ✔: `capability_gap.py:153,183,257-270,592,82-89`.
- #12 ✔: `emit_change_planned`'da `enforce_profile_for_action` yok. İlk/son farkı pratikte önemsiz,
  çünkü validated idempotent ve change_id başına tek satır (`:505-523`).
- #13 ✔: `_is_reachable` herhangi bir çağrı bulunca True dönüyor (`:126-141`).
- #15 ✔: `set_profile` geri alınmıyor (`:90-95`); iki farklı terminal tanımı (`:461` ve `:754`);
  skill/agent alt-dize araması (`:813-844`).
- #16 ✔ (çıkarım): plan önce kuruluyor, yazım satır satır.
- #17 ✔: `contract_digest.py:10-14`; `context_budget_gate.py:122-124` artık docs/aria ve
  docs/adr'yi de sayıyor.

**"Hiçbir dokümanda olmayan" ve RAG notları**

- checkpoint, containment_probe (`GIT_PROVEN_FLOOR=(2,43)` `:113`), command_policy/claude_settings,
  control (SIGTERM→SIGKILL `claude_runtime.py:1252,1336`), contention idempotensi ✔.
- Burn-in → merdiven köprüsü ✔: `observe_success` yalnız `record_burn_in_acceptance` (CLI `:6087`
  ve inline python) üzerinden gerçek ledger'a yazılıyor. `tools/aria-acceptance/burn_in_driver.py`
  `mode="mock"` ile ayrı demo ledger'ına yazıyor.
- context_compiler sıralaması (`:176-186`) ve `<derived_context>` sarmalı ✔.

## Birinci turun kaçırdıkları

1. **`halt_sequence` zinciri yanlış tetikleniyor (orta).** `_first_phase_failure`
   (`cycle.py:3841-3851`) `record_and_continue` fazlarının `failed` sonucunu da sayıyor
   (`:3663-3666`). Bu yüzden yalnızca kayıt amaçlı bir fazın hatası (örneğin preflight'taki
   `mission_reconcile`) cycle'ın algı çekirdeğini (memory, pressure, mission_ingest,
   consensus/HR/judge_calibration, proactive, reflection) `upstream_failure` ile atlatıyor. Kod
   yorumu (`:295-297`) ve MIMARI Ş2 bunu söylemiyor.
2. **Capability resolver canlıda 40/40 `request`.** `extend` yolu da yapısal olarak ölü (bkz. #2).
   Kök neden `map_agent_priors`'ın çağıranının olmaması: `latest_agent_priors` hep boş.
3. **Canlı değişim zinciri planned'da durdu** (2 planned, 0 committed). §8.1-C'nin "committed'da
   durur" etkisi canlıda henüz sınanmadı (bkz. #7).
4. **`checkpoint.restore_checkpoint(files=…)` yol kaçışına açık.** `candidate = workspace / rel`
   resolve edilmiyor ve kök içinde olup olmadığı denetlenmiyor (`checkpoint.py:285-291`). CLI
   `restore --file ../x` checkpoint'te olmayan ve workspace dışında kalan bir dosyayı `unlink`
   edebilir. Operatör CLI yolu (`cli.py:6214`), otonom yol değil. B03 yalnız "checkpoint'te olmayan
   yol" dedi, kök dışını anmadı.
5. **closure_reachability'nin CI kapısı birim testinde** (bkz. #1). B03 "kapı yok" dedi.
6. **circuit_breaker docstring'indeki pencere değeri yanlış.** Docstring 72 saat diyor, gerçek
   varsayılan 96 saat (`genesis_policy.minimum_window_hours(3)`). B03 "72h bayat" dedi ama doğru
   değeri vermedi.
