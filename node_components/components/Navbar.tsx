// Navbar.tsx
import styles from './Navbar.module.css';

export default function Navbar() {
  return (
    <header className={styles.navbar}>
      <span className={styles.title}>My Dashboard</span>
    </header>
  );
}

