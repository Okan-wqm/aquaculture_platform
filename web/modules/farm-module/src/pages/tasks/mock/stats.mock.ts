import { TaskStats } from '../types/task.types';

export const mockTaskStats: TaskStats = {
  totalToday: 8,
  completedToday: 2,
  overdueCount: 1,
  upcomingCount: 5,
  completionRate: 78,
  avgCompletionMinutes: 52,
};
