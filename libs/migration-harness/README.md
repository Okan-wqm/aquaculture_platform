# migration-harness

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build migration-harness` to build the library.

## Running unit tests

Run `nx test migration-harness` to execute the unit tests via [Jest](https://jestjs.io).
