[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aria-kernel"
version = "0.2.0"
description = "Local ARIA adapter and skill health governance kernel"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
aria-kernel = "aria_kernel.cli:main"

[tool.setuptools.packages.find]
where = ["."]
