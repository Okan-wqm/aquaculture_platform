{
  "name": "outbox",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "platform/libs/outbox/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:lib"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "platform/libs/outbox/jest.config.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["platform/libs/outbox/src/**/*.ts"]
      }
    }
  }
}
