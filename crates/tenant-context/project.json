{
  "name": "tenant-context",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "crates/tenant-context/src",
  "tags": ["scope:rust", "type:crate", "layer:context"],
  "targets": {
    "build": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "build", "package": "tenant-context", "release": false },
      "outputs": ["{workspaceRoot}/target/debug/libtenant_context.rlib"],
      "cache": true
    },
    "build-release": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "build", "package": "tenant-context", "release": true },
      "outputs": ["{workspaceRoot}/target/release/libtenant_context.rlib"],
      "cache": true
    },
    "test": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "test", "package": "tenant-context", "allFeatures": true },
      "cache": true
    },
    "lint": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "clippy", "package": "tenant-context", "args": ["--all-targets", "--all-features", "--", "-D", "warnings"] },
      "cache": true
    },
    "fmt-check": {
      "executor": "@aqua/cargo:run",
      "options": { "command": "fmt", "package": "tenant-context", "args": ["--", "--check"] },
      "cache": true
    }
  }
}
