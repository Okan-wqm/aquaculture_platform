[package]
name = "event-contracts-rs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Rust binding for @platform/event-contracts. JSON Schema → typed Rust struct codegen (typify). Enforces ADR-006 flat event pattern + branded EventId at compile time."

[lib]
path = "src/lib.rs"

# [dependencies] intentionally empty during Faz 0 PR-A scaffold. The
# typify build script + generated serde structs land in Faz 2.

[lints]
workspace = true
