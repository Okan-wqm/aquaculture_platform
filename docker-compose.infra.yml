# =============================================================================
# Aquaculture Platform - Infrastructure Only (Development)
# Use this for local development with NX serve
# =============================================================================

services:
  # ===========================================================================
  # Database
  # ===========================================================================
  postgres:
    image: timescale/timescaledb:2.17.2-pg16
    container_name: aqua-postgres
    environment:
      POSTGRES_USER: aquaculture
      POSTGRES_PASSWORD: ${DB_PASSWORD:-devpassword}
      POSTGRES_DB: aquaculture
      # Removed POSTGRES_HOST_AUTH_METHOD: trust — password auth is required.
      # The dev password is set via DB_PASSWORD env var (defaults to 'devpassword').
      # SEC-015: Per-service DB passwords (passed to 00-init-schemas.sh)
      AUTH_SERVICE_DB_PASS: ${AUTH_SERVICE_DB_PASS:-}
      FARM_SERVICE_DB_PASS: ${FARM_SERVICE_DB_PASS:-}
      SENSOR_SERVICE_DB_PASS: ${SENSOR_SERVICE_DB_PASS:-}
      BILLING_SERVICE_DB_PASS: ${BILLING_SERVICE_DB_PASS:-}
      HR_SERVICE_DB_PASS: ${HR_SERVICE_DB_PASS:-}
      ALERT_SERVICE_DB_PASS: ${ALERT_SERVICE_DB_PASS:-}
      ADMIN_SERVICE_DB_PASS: ${ADMIN_SERVICE_DB_PASS:-}
      GATEWAY_SERVICE_DB_PASS: ${GATEWAY_SERVICE_DB_PASS:-}
      NOTIFICATION_SERVICE_DB_PASS: ${NOTIFICATION_SERVICE_DB_PASS:-}
      HYDROPONICS_SERVICE_DB_PASS: ${HYDROPONICS_SERVICE_DB_PASS:-}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/docker/init-scripts:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aquaculture"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aqua-network

  # ===========================================================================
  # Cache
  # ===========================================================================
  redis:
    image: redis:7-alpine
    container_name: aqua-redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-devpassword}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      # SEC-014: Use REDISCLI_AUTH env var to avoid password in process list
      test: ["CMD-SHELL", "REDISCLI_AUTH=${REDIS_PASSWORD:-devpassword} redis-cli ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aqua-network

  # ===========================================================================
  # Message Broker
  # ===========================================================================
  nats:
    image: nats:2.10-alpine
    container_name: aqua-nats
    # ARCH-010/ARCH-018: Use config file for JetStream limits + store_dir
    command: ["--config", "/etc/nats/nats.conf"]
    ports:
      - "4222:4222"
      - "127.0.0.1:8222:8222"
    volumes:
      - nats_data:/data
      - ./infrastructure/docker/nats/nats.conf:/etc/nats/nats.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 4222 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aqua-network

  # ===========================================================================
  # Object Storage
  # ===========================================================================
  minio:
    image: minio/minio:RELEASE.2025-01-20T15-46-22Z
    container_name: aqua-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - aqua-network

  # ===========================================================================
  # Development Tools
  # ===========================================================================
  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: aqua-mailhog
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - aqua-network

  adminer:
    image: adminer:4.8.1
    container_name: aqua-adminer
    # SEC-024: Bind to localhost only — not accessible from LAN
    ports:
      - "127.0.0.1:8081:8080"
    depends_on:
      - postgres
    networks:
      - aqua-network

networks:
  aqua-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  nats_data:
  minio_data:
