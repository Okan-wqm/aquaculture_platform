<!-- ARIA-CURRENT-STATE-NOTICE: Tam-okuma ajan raporu (2026-09-25, main @ 19efd07f);
docs/aria/CURRENT_STATE.md ve executable contracts'a tabidir. Ham kanıt kaydıdır, runtime
otoritesi değildir. -->

# B10 — Tam okuma raporu (aria_kernel: state_store_lifecycle_arcs … workflow_contract_registry)

Repo `main @ 19efd07f`. Tüm yollar `aria-kernel/aria_kernel/` altında (aksi belirtilmedikçe).

## Dosya envanteri

Okunan: **39 / 39** dosya (16 799 satır), hepsi baştan sona Read ile.

| Dosya                             | Satır | Ne yapar (1 cümle)                                                                                    | Üretimde bağlı mı?                                                                                | Şema                   |
| --------------------------------- | ----- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------- |
| state_store_lifecycle_arcs.py     | 470   | aria/state kilit tutucularının git adımlarını kaydeder, canlılık sınırlarını (5400 s / 900 s) türetir | bağlı — `state_store.py:139-152,282`, `ledger.py:18,725`                                          | 6 (yok)                |
| state_tree_contract.py            | 328   | yayınlanan ağaçta beyan dışı girdileri index-only düşürerek publish'i iyileştirir                     | bağlı — `state_store.py:1957-1968`, `autonomy_evidence.py:31`                                     | 6 (yok)                |
| status_probe.py                   | 245   | sağlayıcı auth sondası için 3 değerli karar + yeniden deneme saati                                    | bağlı — `native_admission.py:35`, `tools/aria-poc/{claude,codex,zai}_runtime.py`                  | 5                      |
| strict_jsonl_reader.py            | 200   | JSONL satır-geçerlilik okuyucu (strict/tolerant), zincir doğrulamaz                                   | bağlı — 18 modül                                                                                  | 6                      |
| suppression_scanner.py            | 313   | diff eklenen satırlarında bastırma desenleri (skip, as any, empty catch…)                             | bağlı — `apply_engine.py:251`, `auto_merge.py:273`                                                | 17 (dolaylı)           |
| surface_manifest_validator.py     | 541   | ajan frontmatter/whitelist/registry senkron doğrulayıcıları                                           | **yalnız test**                                                                                   | 19                     |
| surface_reachability.py           | 241   | kapalı sözlük üyelerinin üretimde yazılıp yazılmadığını AST'den ölçer                                 | **yalnız test** (kapı testi)                                                                      | 19                     |
| surface_waivers.py                | 248   | tarihli waiver manifestlerinin tek okuyucusu / lapse yüklemi                                          | bağlı — `deadlines.py:54-150`                                                                     | 19                     |
| task.py                           | 436   | döngü görev adaylarını üretip admissible/panel/blocked bölümler                                       | bağlı — `mission.py:66,1065`                                                                      | 20                     |
| telemetry.py                      | 240   | Prometheus/OTEL metrik dışa aktarımı                                                                  | bağlı — `cli.py:4197`, `gateway/scheduler.py:211`                                                 | 15                     |
| text_safety.py                    | 144   | prompt sınırı: bidi/kontrol temizleme, HTML-encode, base64 sarma                                      | bağlı — `cross_review_bridge.py:499`, `plan_synthesizer.py:1028`, `implementation_safety.py:2662` | 3                      |
| token_economy.py                  | 186   | kabul-başına token + efor düşürme önerisi/uygulaması                                                  | bağlı — `gateway/scheduler.py:239`, `ci_executor.py:2206`                                         | 15 (yok)               |
| tool_degradation.py               | 347   | bozuk/karantinadaki araç serisini kaydeder, 3. gecede HUMAN_REQUIRED                                  | bağlı — `cycle.py:1366`, `doctor.py:477`                                                          | 10 (yok)               |
| tool_health.py                    | 844   | koşu kaydı, sağlık kararı, anında karantina, oto-CALIBRATE, glob eşleme                               | bağlı — `tool_runner.py:23`, cycle                                                                | 10                     |
| tool_registry.py                  | 1517  | tools-root çözümleme/bağlama, registry, governance, yaşam döngüsü geçişleri                           | bağlı — ~100 modül                                                                                | 10/6                   |
| tool_runner.py                    | 704   | adaptörü subprocess olarak koşar, çıktı/kapsam/mutasyon denetler                                      | bağlı — cycle `_phase_tools`, `spine_orchestrator`, `fixture_runner`                              | 10                     |
| tool_sit_out.py                   | 205   | karantinadaki aracın oturduğu döngüleri cycles.jsonl'dan sayar                                        | bağlı — `tool_degradation.py:44`                                                                  | 10 (yok)               |
| tools_binding.py                  | 254   | geri yüklenen aria/state ağacını hosta bağlar (migrasyonsuz)                                          | bağlı — `cli.py:3596` ← `restore-aria-state/action.yml:223`                                       | 6/19 (yok)             |
| trailer_scan.py                   | 237   | `Closes-/Addresses-Pressure` trailer'larıyla baskı kapatır                                            | bağlı — `learning.py:29,94`                                                                       | 4/9 (okuma tarafı yok) |
| triage.py                         | 375   | baskıyı kanıt yoluna göre şeride ayırır, fitness tavanı uygular                                       | bağlı — `cycle.py:1377`, `learning.py:100`                                                        | 9                      |
| trust.py                          | 199   | ≥3 kaynaklı gap'i "trusted" yapar, ref bayatlığını blame ile ölçer                                    | bağlı — `learning.py:98-99`, `pressure.py:1036`, `reverify.py:37`                                 | 20                     |
| turn_budget.py                    | 288   | cycle deadline + uygulayıcı tur kapı gözlemi ve merge kanıtı                                          | bağlı — `hooks.py`, `claude_settings.py`, `implementation_safety`, `cycle.py`                     | yok                    |
| turn_budget_policy.py             | 204   | tur kapının politika bloğu (varsayılan 120, tavan 400)                                                | bağlı — `turn_budget.py:105`                                                                      | yok                    |
| twin.py                           | 1015  | repo haritası (proje grafiği, TESTED_BY, churn, co-change) + intent + self_features pilotu            | bağlı — cycle twin_refresh, `agent_invocations.py:178,427`                                        | 5                      |
| typed_judgment.py                 | 766   | tipli yargı soruları/yanıtları, batch ayrıştırıcı, alıntı doğrulama                                   | bağlı — `judgment_bridge.py:42`, `ci_executor_judge_batch.py:212`                                 | 11 (yok)               |
| upcasters/**init**.py             | 14    | paket dışa aktarımı                                                                                   | bağlı (cycles kısmı)                                                                              | 19                     |
| upcasters/cycles.py               | 82    | v2 cycles satırına okuma anında status ekler                                                          | bağlı — yalnız `autonomy_evidence.py:643`                                                         | 19                     |
| upcasters/service_map_v1_to_v2.py | 133   | SERVICE_MAP v1↔v2 web kovası dönüşümü                                                                | **yalnız test** (ölü)                                                                             | 19                     |
| usage_ledger.py                   | 109   | spawn başına token kullanım satırı                                                                    | bağlı — `claude_runtime.py:1018` (yalnız Claude)                                                  | 15                     |
| validation.py                     | 1036  | izinli doğrulama komutlarını koşar, validation-runs defterine yazar                                   | bağlı — `apply_engine.py:746,986`, `experiment.py:366` …                                          | 17                     |
| validation_env.py                 | 176   | doğrulama çocuğu için süzülmüş ortam inşası                                                           | bağlı — `validation.py:509`                                                                       | yok                    |
| validation_matrix_gate.py         | 783   | risk-türü tabanlı 3 katmanlı doğrulama matrisi + regresyon çapası                                     | bağlı — `cycle.py:2905`, `cli.py:3725`                                                            | 17                     |
| validation_runs_ledger.py         | 580   | validation-runs tek yazıcı + log hash bağlama/doğrulama                                               | bağlı — validation, matrix gate, auto_merge                                                       | 17/7                   |
| validation_suite.py               | 144   | kanonik 4 komutluk suite SSoT + eşleme kuralı                                                         | bağlı — `command_policy`, `implementation_safety`, `auto_merge`                                   | 16/17                  |
| verification_gate.py              | 435   | worker sonucu submit (lease) + doğrulama kapısı                                                       | bağlı — `worker_dispatch_hook.py:414`, `worker_executor.py:240`                                   | 4                      |
| watchdog_freeze.py                | 142   | açık dış-watchdog incident issue'su varken ARIA merge'ünü dondurur                                    | bağlı — `merge_authority.py:59`                                                                   | 7                      |
| worker_dispatch.py                | 1181  | dispatch/claim defteri, reducer, lease reaper, prune                                                  | bağlı (kısmen; bkz. ölü kod) — hook, cycle, promotion_controller                                  | 4                      |
| worker_dispatch_hook.py           | 548   | worker daemon tiki: claim → executor → verify → dallanma                                              | bağlı — `autonomous_worker_scheduler.py`                                                          | 4                      |
| workflow_contract_registry.py     | 889   | CI workflow iş sözleşmeleri SSoT + wall-clock tavanı                                                  | bağlı — `workflow_contracts.py`, `preflight.py`, `docs_ssot.py`                                   | 19/21                  |

## Doküman tutarsızlıkları (yalnız YENİ)

| #      | Doküman iddiası (doc + şema/§)                                                                                                                                                                                                                                       | Kod gerçeği                                                                                                                                                            | Kanıt file:line                                                                                                    | Sınıf                    |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------ |
| B10-1  | Şema 4: `V & FV -.terminal + 7 gün.-> prune_worktrees`                                                                                                                                                                                                               | prune yalnız `{completed,cancelled,expired}`'i budar; reducer bu durumları hiç üretmez; yalnız CLI'dan çağrılır → hiçbir worktree budanmaz                             | `worker_dispatch.py:403,868-937`; `cli.py:4351`                                                                    | TUTARSIZ                 |
| B10-2  | Şema 4: `FV … retry bütçesi: 3 → human_required`                                                                                                                                                                                                                     | 3. hatada yalnız governance satırı; `record_human_required` yok, claim bırakılmaz                                                                                      | `worker_dispatch_hook.py:507-525`                                                                                  | TUTARSIZ                 |
| B10-3  | Şema 4: `EX başarısız → released → pending retry < 3`                                                                                                                                                                                                                | executor non-zero çıkışı retry sayacına girmez (sayaç yalnız `verification_gate_failed`) → sınırsız yeniden deneme                                                     | `worker_dispatch_hook.py:391-401`; `worker_dispatch.py:760-781`                                                    | TUTARSIZ                 |
| B10-4  | Şema 4: `converged plan terfisi -create_dispatch_request-> requests.jsonl`                                                                                                                                                                                           | `create_dispatch_request` (v1, triage-tabanlı) yalnız CLI; otonom yol `promotion_controller` satırı kendisi yazar (v2)                                                 | `cli.py:4216`; `promotion_controller.py:154-184`                                                                   | KAYMA                    |
| B10-5  | Şema 4 dispatch → worker merge; hook docstring "merged — merge_if_green"                                                                                                                                                                                             | merge sonucu sabit `{"decision":"blocked"}` literal; `merged` ulaşılamaz                                                                                               | `worker_dispatch_hook.py:450-470`                                                                                  | EKSİK-KOD                |
| B10-6  | Şema 10: `Q -->                                                            \| operatör + kök neden + fixture                                                                                                                                         \| C` tek çıkış | `transition_tool` genel matris uygulamaz; `QUARANTINED→SHADOW` (CLI `tool promote --target-status SHADOW`) kök neden/onay olmadan geçer                                | `tool_registry.py:1388-1491`; `cli.py:1038,3638-3647`; `promotion.py:108-114`                                      | TUTARSIZ                 |
| B10-7  | Şema 10: `DRAFT/SANDBOX → SHADOW`; Şema 5/13 `EVAL_WINDOW → ACTIVE`                                                                                                                                                                                                  | Kernel'in kendi reachability kapısı bu durumlara **yazan üretim yolu yok** diyor (waiver 2026-10-16)                                                                   | `aria-kernel/surface-reachability.unwritten.json` (tool_registry_status.DRAFT/SANDBOX, genesis ACTIVE/EVAL_WINDOW) | EKSİK-KOD                |
| B10-8  | Şema 7: `WD[aria_watchdog 600 sn stall] -.donma.-> M2`                                                                                                                                                                                                               | merge dondurma kaynağı dış watchdog'un açtığı GitHub issue'su (başlık öneki+etiket), kernel aria_watchdog değil                                                        | `watchdog_freeze.py:62-133`; `.github/manifests/aria-state-watchdog.json`                                          | TUTARSIZ                 |
| B10-9  | Şema 5: prompt v3 "`<untrusted_*>` etiketli"                                                                                                                                                                                                                         | twin intent katmanı (commit subject+why) prompt'a sarmasız/sanitize'sız düz Markdown girer                                                                             | `twin.py:948-1002`; `agent_invocations.py:653-686`                                                                 | TUTARSIZ                 |
| B10-10 | Şema 3: çapraz inceleme köprüsü plan metnini `<untrusted_*>`'a sarar                                                                                                                                                                                                 | sarma **base64** (Mode B) ile yapılır                                                                                                                                  | `text_safety.py:87-119`; `cross_review_bridge.py:499-502`                                                          | EKSİK-DOKÜMAN            |
| B10-11 | Şema 15: "~25 aria\_\* metriği"                                                                                                                                                                                                                                      | ~43 farklı metrik adı                                                                                                                                                  | `telemetry.py:26-210`                                                                                              | KAYMA                    |
| B10-12 | Şema 19: "9 lane için kayıtlı iş sözleşmesi"                                                                                                                                                                                                                         | 10 workflow / 11 job + 4 audited exclusion                                                                                                                             | `workflow_contract_registry.py:232-806`                                                                            | KAYMA                    |
| B10-13 | Şema 19 R3: surface_reachability "kaynak·durum·rol…"                                                                                                                                                                                                                 | 5 yüzey (genesis ileri geçiş, plan event, tool status, pressure source, request role); ters yön (`undeclared_written_members`) de ölçülür                              | `surface_reachability.py:96-210`                                                                                   | EKSİK-DOKÜMAN            |
| B10-14 | Şema 20 TK: "next_action ZORUNLU"                                                                                                                                                                                                                                    | anahtar türetilemezse **hiç konmaz**; adopter `no_derivable_next_action` ile reddeder                                                                                  | `task.py:15-35,376-385`                                                                                            | EKSİK-DOKÜMAN            |
| B10-15 | Şema 9: `human_only: infra · workflows…`                                                                                                                                                                                                                             | desen `infra/**`; asıl altyapı `infrastructure/**` eşleşmez → `blocked`                                                                                                | `triage.py:209`                                                                                                    | TUTARSIZ (küçük)         |
| B10-16 | Şema 17 C3 "enforced: matris kapısı"                                                                                                                                                                                                                                 | EXISTENCE/PATTERN katmanları diff'e değil tüm repoya bakar (vacuous); schema_change cmd korelasyonu alt-dize ile oyunlanabilir; risk-siz yolda başarısız koşu da geçer | `validation_matrix_gate.py:328-361,474-478,641-653`                                                                | TUTARSIZ (zayıf kapı)    |
| B10-17 | Şema 4 VG: "izinli komutlar geçti"                                                                                                                                                                                                                                   | komutlar ajan worktree'sinde tam `os.environ` ile, sandbox'sız, timeout yakalanmadan koşar                                                                             | `verification_gate.py:303-309`                                                                                     | EKSİK-DOKÜMAN (güvenlik) |
| B10-18 | Şema 7 QN "sandbox kaçışı → karantina"                                                                                                                                                                                                                               | adaptörler sandbox'sız, tam host env ile; kaçış yalnız sonradan git-status farkıyla tespit                                                                             | `tool_runner.py:132-147,171-179`                                                                                   | EKSİK-DOKÜMAN            |
| B10-19 | (yok)                                                                                                                                                                                                                                                                | tur bütçesi (120, tavan 400) + cycle deadline kapısı + HARD_FAIL `cycle_and_turn_budget_cap`                                                                           | `turn_budget.py`; `turn_budget_policy.py:64-83`                                                                    | EKSİK-DOKÜMAN            |
| B10-20 | (yok)                                                                                                                                                                                                                                                                | ekonomi: kabul-başına token >400k veya 0 kabul → efor 1 basamak düşürülür (Claude yolunda uygulanır)                                                                   | `token_economy.py:109-179`; `ci_executor.py:2206-2212`                                                             | EKSİK-DOKÜMAN            |
| B10-21 | (yok)                                                                                                                                                                                                                                                                | 3 ardışık bozuk/karantinalı gece → HIGH HUMAN_REQUIRED (panel oylayamaz)                                                                                               | `tool_degradation.py:65,223-296`                                                                                   | EKSİK-DOKÜMAN            |
| B10-22 | (yok)                                                                                                                                                                                                                                                                | publish kilidi bekleyicisi 90 dk; state_transaction 15 dk; tüm git 300 s                                                                                               | `state_store_lifecycle_arcs.py:60,335-362`                                                                         | EKSİK-DOKÜMAN            |

## Bağlı olmayan / ölü / stub kod

- `surface_manifest_validator.run_all_validators` + 7 doğrulayıcı: yalnız test; docstring'deki CI
  spec `tests/invariants/aria-surface-manifest.spec.ts` repoda **yok**
  (`surface_manifest_validator.py:16-18`).
- `surface_reachability.survey` vb.: yalnız test kapısı (tasarım gereği).
- `upcasters/service_map_v1_to_v2.upcast/downcast`: yalnız test; üretimde sadece docstring referansı
  (`discovery.py:208`).
- `task.explain_task`, `task.latest_tasks` (`task.py:182-200`): hiçbir çağıran yok (test dahil).
- `tool_degradation.consecutive_degraded_cycles` (`:154-160`): üretim çağıranı yok.
- `tool_runner._workspace_snapshot`, `_workspace_snapshot_raw` (`:430-484`): `_workspace_snapshots`
  ile yer değiştirmiş, çağrılmıyor.
- `worker_dispatch._latest_request_states` (`:818-832`), `recover_orphan_governance` (`:940-978`):
  çağıran yok.
- `worker_dispatch.cancel_dispatch_request` / `mark_dispatch_picked_up`: çağrılıyor (CLI) ama
  **etkisiz** — reducer governance okumaz (`:835-937`).
- `worker_dispatch.prune_worktrees`: reducer'ın hiç üretmediği durumları bekler → no-op (`:403`).
- `worker_dispatch_hook` merge dalı: sabit `"blocked"` stub (`:454-459`).
- `suppression_scanner` `aria_suppression_honor` dedektörü: hedeflediği
  `aria-tools/*suppression*.json` yüzeyi yok / gitignore'lu → pratikte ölü (`:102-105`).
- `suppression_scanner` docstring'deki `if: always()` dedektörü kodda yok (`:20` vs `:73-78`).
- `twin.self_features`: 2 sabit özellikli "pilot" (`twin.py:291-305,375`).
- `verify_workflow_registry` (workflow_contracts.py) ve `validate_file`: dormant, waiver 2026-10-09
  (`control-reachability.dormant.json`).
- `batch-containment.waivers.json`: registry'de kayıtlı, dosya yok (`surface_waivers.py:106-112`).

## Hata / risk gözlemleri

1. **Adaptör yanlış-karantinası**: `tool_runner` her koşuda tüm `aria-tools/` içeriğini hash'ler;
   eşzamanlı daemon yazımı → `scope_out_mutations` → anında karantina (`tool_runner.py:487-510`;
   `tool_health.py:356-360`).
2. **Doğrulama kapısında sandbox/env yok**: `verify_worker_result` ajan kodunu tam ortam +
   sandbox'sız koşar (`verification_gate.py:303-309`), oysa `validation.py` aynı sınıf için env
   inşası + bwrap uyguluyor.
3. **Lane-A izin listesi**: `npx ts-node -e <kod> tools/aria-adapters/x.test.ts` geçer;
   `python3 -m unittest` kısıtsız (`validation.py:968-975`).
4. **Kanonik suite "daraltma"**: sondaki herhangi bir argüman kabul (`--base=HEAD --head=HEAD`,
   `--exclude=*`) (`validation_suite.py:84-89`).
5. **Öz-onay reddi ana yolda kapalı**: `apply_engine` `change_author_identity` geçmez
   (`validation_runs_ledger.py:280-290`; `apply_engine.py:746-755,986-995`).
6. **Matris kapısı risk-siz yolda başarısız koşuları kabul eder** (yalnız log hash doğrulanır)
   (`validation_matrix_gate.py:641-653`; `validation_runs_ledger.py:526-552`).
7. **Karantinadan kaçış**: `tool promote --target-status SHADOW` QUARANTINED aracı doğrudan SHADOW'a
   alır (`tool_registry.py:1397-1403` yalnız `→CALIBRATE`'i kısıtlar).
8. **ACTIVE kapısında `evidence_chains_valid=True` sabit** (`promotion.py:106`) — tool_registry
   docstring'indeki "son cümle, hiçbir otorite atlayamaz" garantisi üretimde işlevsiz.
9. **Worker executor sonsuz döngüsü**: çöken executor her 30 s tikte yeniden claim edilir
   (`worker_dispatch_hook.py:391-401`).
10. **Reaper sunulmuş işi yeniden kuyruğa alabilir**: `submitted` canlı sayılır, claim submit'te
    kapanmaz, executor timeout'u `lease+60` > lease (`worker_dispatch_hook.py:302`;
    `worker_dispatch.py:53-58,1107`).
11. **Trailer ile baskı kapatma yetkisiz**: herhangi bir commit'in mesajı baskıyı kapatır
    (`trailer_scan.py:83-94`).
12. **Trust yükseltmesi bütünlüksüz okumadan** (`trust.py:15`; `feedback.py:315-319`).
13. **Tipli yargı alıntısı 1 karaktere kadar düşebilir** (`typed_judgment.py:533,551`).
14. **`twin._git` timeout'suz** (`twin.py:885-888`).
15. **`HARD_FORBIDDEN_READ_GLOBS` yalnız kök** (`.env`, `secrets/**`) (`tool_health.py:70-75`).
16. **Oto-CALIBRATE `transition_tool`'u atlar** (`tool_health.py:270-283`) —
    `surface_reachability.py:130-133`'teki "tek yazıcı" iddiasını bozar; `register_tool` da
    SHADOW→ARCHIVED/QUARANTINED'e governance satırsız izin verir (`tool_registry.py:985-1013`).
17. **Tur kapı politika değişikliği** önceki cap ile koşmuş tüm uygulamaları merge'de reddettirir
    (`turn_budget.py:229-233,251-252`).

## Hiçbir dokümanda olmayan önemli davranışlar

- Kilit/liveness hesabı tamamen git adım kaydından türetilir; publish kilidi 90 dk bekleyebilir
  (`state_store_lifecycle_arcs.py:335-353`).
- Publish, ebeveyn ağaçtaki beyan dışı girdileri (lock side-car, `repo_identity.json`) governance
  satırıyla index'ten düşürerek kendini iyileştirir (`state_tree_contract.py:240-314`).
- `tools_contract.json` + `bind-tools-root`: restore = bağlama, migrasyon değil; yabancı repo deposu
  reddedilir (`tools_binding.py:60-127,219-244`).
- Sağlayıcı sondası 3 değerli; UNDECIDED asla failover nedeni değil (`status_probe.py:64-78`).
- Tur bütçesi + cycle deadline kapısı (hook düzeyinde) (`turn_budget.py`).
- Token ekonomisi efor düşürme (`token_economy.py`).
- Araç bozulma serisi → HUMAN_REQUIRED; manifest sync `last_transition`'ı siler
  (`tool_degradation.py`, `tool_sit_out.py:19-27`).
- Doğrulama çocuğu ortam süzgeci (`validation_env.py`) ve süreç-grubu SIGKILL
  (`validation.py:436-483`).
- Tipli yargı primitifleri (choice/score/noul) ve alıntı-index kanıt modeli (`typed_judgment.py`).
- Twin intent katmanı (commit WHY satırları prompt'a) (`twin.py:938-1002`).
- Workflow abort-gate sözleşmesi ve 510 dk executor tavanı
  (`workflow_contract_registry.py:47-83,340`).

## RAG / kanıt modeli ile ilgili not

- `typed_judgment` kanıt modelini güçlendirir: model dosya yolu yazamaz, yalnız istek ref'lerine
  **index** + sabitlenmiş excerpt'ten **verbatim alıntı** verir; `confidence_source` rota tarafından
  damgalanır (`typed_judgment.py:16-32`). Ancak alıntı alt-dize kontrolü (min 1 karakter) zayıf, ve
  tipli format tekil yanıtta opsiyonel (`judgment_bridge.py:103-114`).
- `twin.intent_context_for_files` git geçmişinden **bağlam** üretir ("not evidence" başlığıyla) —
  RAG-benzeri bağlam enjeksiyonu; güvenilmeyen etiketle sarılmadığı için kanıt/talimat sınırı yalnız
  düzyazı uyarısına dayanır (`agent_invocations.py:667-671`).
- `trust.py` "trusted" statüsü benzerlik/doğrulama değil, **farklı kaynak etiketi sayımı** (≥3) —
  bütünlük doğrulanmamış okuma ile.
- Doğrulama kanıtı (`validation_runs_ledger`) log içeriğinin hash'ine bağlıdır; bağımsız tanık
  yoktur (yazan süreç = kaydeden süreç).

## Notlar (dosya dosya, ara kayıt)

### 1. state_store_lifecycle_arcs.py (470)

- Yaprak modül: git adım kaydı + kilit "arc"ları + canlılık sınırları. `GIT_TIMEOUT_SECONDS=300`
  (:60), `PUBLISH_MAX_ATTEMPTS=3` (:66).
- Türetilmiş sınırlar: publish arc = pending(3) + 3×attempt(5) = 18 adım × 300 s = **5400 s**;
  checkout restore 9 adım = 2700 s; lifecycle kilit bekleyicisi `max` = **5400 s (90 dk)**
  (:335-353). `STATE_LOCK_LIVENESS_SECONDS` = 900 s (:360-362) → `ledger.state_transaction`
  varsayılan bekleme (ledger.py:18,725), `human_required.py:42`, `notify.py:69`.
- Runtime zorlaması: `lifecycle_step_active`/`state_transaction_held` thread-local işaretçileri
  (:374-416); `require_step_operation` adımla argv uyuşmazlığında ValueError (:419-426).
- Üretim: `state_store.py:139-152,282-290`, `ledger.py:18`. Şema: 6 (dokümanda hiç yok).

### 2. state_tree_contract.py (328)

- Yayınlanan `aria/state` ağacında neyin bulunabileceği; ebeveyn committe beyan dışı girdi (kilit
  side-car'ları, `repo_identity.json`, `integrity_index.json`) varsa **index-only
  `git rm --cached`** ile iyileştirme (:240-314). Governance satırı
  `state_publish_inherited_entries_dropped` ÖNCE yazılır, yazılamazsa hiçbir şey düşürülmez
  (:271-311).
- Gerekçe: 2026-09-04 → 09-11 arası hiçbir kernel publish'i inmedi; bakım şeridi `git add -A` ile 16
  `*.lock` + 2 dosya commitlemişti (:13-17).
- Üretim: `state_store.py:1957-1968`; `autonomy_evidence.py:31` (STATE_BOOTSTRAP_EMPTY_MARKERS).
  Şema 6 (yok).

### 3. status_probe.py (245)

- Sağlayıcı durum sondası 3 değerli karar: AVAILABLE/UNAVAILABLE/UNDECIDED (:64-78); UNDECIDED asla
  failover nedeni değil; 3 deneme, geri çekilme (2 s, 5 s), deneme başına `recheck_timeout_seconds`
  (20 s) (:50-56).
- Gerekçe (:3-9): 2026-09-12'de `claude auth status` 20 s takıldı → auth reddi gibi okundu → primary
  planner **openai/gpt-6-astra**'da koştu.
- Üretim: `native_admission.py:35-41,136`; `tools/aria-poc/claude_runtime.py:462`,
  `codex_runtime.py:43,81`, `zai_runtime.py:48`. Şema 5.

### 4. strict_jsonl_reader.py (200)

- Satır-JSON geçerliliği okuyucu (hash-zinciri DOĞRULAMAZ — o `load_jsonl_verified`),
  strict/tolerant. 18 üretim modülü kullanıyor; `read_strict_jsonl(` çağrılarının 11'i `tolerant`.
- Tutarsız davranış: beyanlı yüzeyde tüm dosya önce decode edilir (atomik, non-object satır
  reddedilir :119-132); **beyansız yolda** jeneratör satırları tek tek yield eder (strict hata
  öncesi satırlar zaten tüketilmiş) ve `dict` olmayan JSON'u (dizi/sayı) **reddetmeden yield eder**
  (:177-197).
- Her okumada `path.read_text` ile tüm dosya belleğe (:94).

### 5. suppression_scanner.py (313)

- Diff eklenen satırlarında 5 kategori (test_skip, ci_masking, ts_masking, runtime_masking,
  aria_suppression_honor). Üretim: `apply_engine.py:251-253`, `auto_merge.py:273-274`, CLI
  `cli.py:5156`.
- Docstring `if: always()` desenini vaat ediyor (:20) — **regex yok** (:73-78).
- Satır-yerel regex: en yaygın Python biçimi `except …:\n    pass` (iki satır) ve çok satırlı
  `catch (e) {\n}` **yakalanmaz** (:93-96).
- `aria_suppression_honor` yalnız `aria-tools/…suppression….json` yolunu arar (:102-105);
  `aria-tools/` gitignore'lu ve kernelde böyle bir yüzey yok → dedektör pratikte ölü; gerçek
  muafiyet dosyaları (`aria-kernel/control-reachability.dormant.json`,
  `surface-reachability.unwritten.json`, surface waivers) kapsanmıyor.
- Ayrıştırıcı: hunk içinde silinen `-- sql yorumu` satırı `---` (ardından boşluk) ile başladığından
  metadata sayılır ve dedektörlere verilir (:254); eklenen `++ x` içerikli satır `+++` (ardından
  boşluk) başlığı gibi dosya yolunu değiştirir (:261-289).

### 6. surface_manifest_validator.py (541)

- 7 doğrulayıcı (ajan frontmatter, whitelist varlığı, ROLE_TARGET_PAIRING, registry argv yolu,
  plan-doc tazeliği, \_maintenance izolasyonu, registry↔manifest senkronu).
- **Yalnız test**: `run_all_validators` hiçbir CLI/cycle/workflow'dan çağrılmıyor (grep: yalnız
  `aria-kernel/tests/test_surface_manifest_validator.py` vb.). Docstring'in dayandığı "CI invariant
  test `tests/invariants/aria-surface-manifest.spec.ts`" (:16-18) **repoda yok**.
- Docstring "name+description+model+tools" der (:6), `REQUIRED_FRONTMATTER_FIELDS` `model` içermez
  (:93). Özet "Run all 6 validators" der, 7 koşar (:477,492).
- `validate_plan_doc_freshness` yalnız ilk geçişe bakar (`content.find`) ve `"open"` alt dizesi
  `openai/opened`'ı da eşler (:291-293).
- `registry-stub-allowlist.json` yolu `base_dir`'i yok sayar, sabit `aria-tools` (:423).

### 7. surface_reachability.py (241)

- Kapalı sözlüklerin "yazılan üye"lerini `literal_provenance` ile AST'den çözer. Docstring "four
  surfaces" der (:26), `declared_surfaces()` **5** yüzey döndürür (genesis ileri geçiş, plan event
  type, tool status, pressure source, agent request role) (:96-169).
- Ters yön (`undeclared_written_members`, :184-210) VAR — MIMARI Şema 19 "ters yönde vazgeçme YOK"
  diyor; bu doğru (waiver yok) ama "ters yön ölçümü yok" okunabilir; kod ters yönü de ölçüyor.
- **Yalnız test**: üretim çağıranı yok; `tests/test_surface_reachability.py` kapı olarak koşar.
  Canlı unwritten waiver'ları (`aria-kernel/surface-reachability.unwritten.json`, hepsi 2026-10-16):
  `genesis_lifecycle_forward_transition.ACTIVE/EVAL_WINDOW` (ORPHAN-HIGH-689),
  `tool_registry_status.DRAFT/SANDBOX` (ANALYSIS-LOW-692), `ARCHIVED` (ORPHAN-MEDIUM-691),
  `plan_convergence_event_type.lock_reaped`, `agent_surface_request_role.gap_closure/gap_finding`. →
  Şema 10'daki `DRAFT/SANDBOX → SHADOW` ve Şema 5/13'teki `EVAL_WINDOW → ACTIVE` basamaklarına
  **hiçbir üretim yazıcısı geçiş yapmıyor** (kodun kendi kapısı söylüyor).

### 8. surface_waivers.py (248)

- 3 tarihli waiver manifestinin tek okuyucusu/lapse yüklemi (`expires_on < utc_today`)
  (:92-122,207-211). Üretim: `deadlines.py:54-150` (7 gün önceden duyuru). Şema 19.
- `batch-containment.waivers.json` kayıtlı (:106-112) ama dosya **yok** (yok = boş manifest,
  :167-168). Kontrol waiver'ları: `validate_file`, `verify_workflow_registry` **2026-10-09**; diğer
  6'sı 2026-10-20 (ORPHAN-HIGH-573).

### 9. task.py (436)

- Döngülük aday üretimi: pressure + open finding + capability gap + proactive (≥60) → `tasks` /
  `routed_to_panel` / `blocked` bölümlemesi, schema v2 (:104-179). Üretim: `mission.py:66,1065` (tek
  çağıran). Şema 20 (TK) — doküman "next_action ZORUNLU" diyor; kod anahtarı **yoksa hiç koymaz**,
  adopter `no_derivable_next_action` ile reddeder (:15-35,376-385) → EKSİK-DOKÜMAN (ince).
- `shadow_run_summary` üreticisi emekliye ayrıldı (:83-94); panel-sahipli adaylar mission'a hiç
  gitmez (:156-165).
- Kod yorumu "canlı mağazadaki 5 mission'ın hepsi bu yoldan geldi" (:29-30) — review §1
  `missions/mission-events.jsonl = 0` diyor; "canlı mağaza" aria/state değil (droplet) olmalı → iki
  "canlı" kaynağı çelişiyor.
- `explain_task`, `latest_tasks` (:182-200): **ölü** (repo genelinde çağıran yok, test dahil).
- Finding adayı `validation_commands: ["npm run test","npm run lint"]` sabit (:296) — kök script'ler
  `nx affected` (tüm repo), aday kapsamına özgü değil.

### 10. telemetry.py (240)

- Prometheus/OTEL dışa aktarımı; üretim: `cli.py:4197`, `gateway/scheduler.py:211-214`. Şema 15.
- MIMARI Şema 15 "~25 aria\_\* metriği" → kodda **~43 farklı metrik adı** (governance 18 + tools 6 +
  store 19) → KAYMA.
- `aria_agent_fitness_score` (gauge) tüm tarihsel satırlar için `_prometheus` içinde **toplanır**
  (:111-112,225-229) → anlamsız toplam skor.
- Her export tüm governance/runs/dispatch defterlerini baştan okur (:23-24,99-113);
  `_guarded_metrics` hatayı metrik yapar (:116-121). HELP/TYPE satırı yok.

### 11. text_safety.py (144)

- `sanitize_untrusted_text` (bidi/kontrol sil + HTML-encode + kısalt),
  `encode_untrusted_delimited_payload` (base64 Mode B), `contains_bidi_or_control`. Üretim:
  `cross_review_bridge.py:499-502`, `plan_synthesizer.py:1028-1043`,
  `implementation_safety.py:2662`.
- MIMARI Şema 3 "çapraz inceleme köprüsü her iki plan metnini `<untrusted_*>` etiketlerine sarar" —
  kod **base64 kodlu** sarar (`encoding="base64"`), düz metin değil → EKSİK-DOKÜMAN (ayrıntı).
- Bidi seti yalnız U+202A-202E, U+2066-2069; U+200E/200F (LRM/RLM), U+061C, sıfır genişlikli
  karakterler (U+200B-200D, U+FEFF) kapsanmıyor (:47-49).

### 12. token_economy.py (186)

- Kabul edilen sonuç başına token; efor düşürme önerisi (bir basamak, taban `medium`), TTL 7 gün
  (:24-35,159-179). Yazan: `gateway/scheduler.py:239-242` (günlük 05:50,
  `default_schedules.py:77-81`), CLI `cli.py:6243-6250`. Uygulayan: **yalnız**
  `tools/aria-poc/ci_executor.py:2206-2212` (`invoke_claude_cli` içinde) — native codex/zai yolları
  `effective_effort` çağırmaz.
- MIMARI'de hiç yok (EKSİK-DOKÜMAN): makine kendi efor seviyesini ölçüme göre otomatik düşürür.
- `lower_effort("low")` → `"medium"` döndürür (taban altındaki efor **yükseltilir**) (:101-106).

### 13. tool_degradation.py (347) + 14. tool_sit_out.py (205)

- Her döngü non-ok koşu + oturup kalan QUARANTINED araç için `tool_run_degraded` governance satırı;
  **3 ardışık** bozuk döngü → HIGH HUMAN_REQUIRED `tool-degraded:<tool>:<ilk_cycle>` (panel
  oylayamaz) (:65,223-296). Üretim: `cycle.py:1366-1368`, `doctor.py:477-479`. Şema 10/20'de yok →
  EKSİK-DOKÜMAN.
- Önemli belgelenmemiş davranış: gecelik `tool_manifest_sync` (`register_tool`) registry satırını
  **tümden değiştirip `last_transition`'ı düşürür**; karantina tarihi/nedeni bu yüzden quarantine
  defterinden okunur (tool_sit_out.py:19-27,73-104).
- Karantina tetikleyicileri: `evidence_error/schema_error/scope_violation/tool_unhealthy`
  (tool_sit_out.py:3-6).
- `consecutive_degraded_cycles` (:154-160): üretim çağıranı yok (yalnız test/ölü).

### 15. tool_health.py (844)

- `record_run`: zarf doğrula → ACTIVE değilse emitted\_\* varsa **GovernanceError** (:115-119) →
  kapsam ihlali/kanıt hatası status'u yeniden yazar → v2 artefakt → runs.jsonl → raw/finding kaydı →
  `evaluate_health` → health.jsonl (:109-196). Üretim: `tool_runner`, cycle tools fazı. Şema 10.
- Anında karantina: scope_violation, schema_error, evidence_error/self_output,
  **scope_out_mutations**, repository_mutation_attempt, crash+ledger_corruption, tool_unhealthy,
  **integrity_failed**, kritik FP (:344-374). `environment_unavailable` bilerek hariç
  (:41-45,367-369).
- Otomatik CALIBRATE (≥10 yargılı örnekte precision<min, 30 günde ≥3 non-critical FP, son 10'da ≥2
  çelişki, crash oranı, 7 günde 2 bütçe aşımı) `_update_tool_internal` ile yazılır —
  **`transition_tool`'u atlar** (:264-294). Bu, `surface_reachability.py:130-133`'teki
  "transition_tool tek denetlenen yazıcıdır, yapı gereği" iddiasını çürütür.
- Konsensüs satırları (anchor yükseltmesi) `(run,finding,group)` üzerinden katlanır → precision
  şişmesi önlenir (:398-431). `precision_status` 5 değer (:530-541).
- `HARD_FORBIDDEN_READ_GLOBS` = `.git/**, secrets/**, .env, .env.*` — **yalnız kök**; `apps/x/.env`,
  `**/secrets/**` kapsanmaz (:70-75). (Tüm 11 manifest allow listesi beyan ettiği için legacy
  "kapsam yok" yolu pratikte kapalı.)

### 16. tool_registry.py (1517)

- Registry, tools-root çözümleme (arg → `ARIA_TOOLS_DIR` → cwd'den yukarı yürüyüş → hata)
  (:164-216), bağlama (canonical identity) (:363-477), governance ekleme, yaşam döngüsü. Üretim:
  ~100 modül.
- `TOOL_STATUSES` 7 durum (:31-39); ilk kayıt yalnız DRAFT/SANDBOX/SHADOW (:46,1022-1030).
- **`transition_tool` genel bir geçiş matrisi uygulamaz** (:1388-1491): yalnız
  `QUARANTINED→CALIBRATE` (kök neden+fixture), `CALIBRATE→SHADOW` (fixture), `→ACTIVE` kısıtlı.
  `QUARANTINED→SHADOW`, `ARCHIVED→SHADOW`, `ACTIVE→DRAFT` vb. **hiç kontrolsüz**. CLI
  `tool promote --target-status SHADOW` (`cli.py:1038,3638-3647`) → `promotion.promote_tool` →
  `transition_tool` yolu karantinadaki bir aracı **kök neden/fixture/operatör onayı olmadan**
  SHADOW'a döndürür → MIMARI Şema 10 "Q → operatör + kök neden + fixture → C" tek çıkış iddiası
  TUTARSIZ.
- `register_tool` yeniden kayıtta yalnız QUARANTINED→≠Q, {DRAFT..CALIBRATE}→ACTIVE,
  {ACTIVE,CALIBRATE}→erken durum reddeder (:985-1010); **SHADOW→ARCHIVED/QUARANTINED/CALIBRATE
  manifest yeniden yüklemesiyle geçer** (governance satırı yok).
- `→ACTIVE` kapısı: precision, kritik FP ve `evidence_chains_valid` **çağıran tarafından verilen
  değerler** (:1336-1341,1469). Tek üretim çağıranı `promotion.py:97-107`
  `evidence_chains_valid=True`'yu **sabit** geçer ve zero-finding şeridinde `precision=1.0` yazar →
  docstring'in "evidence_chains_valid son cümle, hiçbir otorite atlayamaz" (:1379-1386) iddiası
  üretimde **her zaman True** (asıl kapı `readiness.adapter_active_readiness`).
- Sağlık eşiği aralık kısıtları: precision_min ∈[0.5,1], critical_FP **tam 0**, crash ∈[0,0.5],
  nc_FP ∈[1,100] (:1127-1167). Runner argv komut politikasından geçer
  (`verify_bash_command_allowed`, :941); `npx ts-node` → `node ./node_modules/ts-node/dist/bin.js`'e
  yeniden yazılır (:932-933).
- `tools.lock` ≥120 s ve pid ölü ise silinir + `lock_reaped` governance (:753-760) — ama
  surface_reachability waiver'ı `plan_convergence_event_type.lock_reaped`'i "yazılmıyor" diye
  listeliyor (bu ayrı sözlük; karışıklık notu).
- `append_tools_governance_once`: her çağrıda **tüm governance.jsonl'ı** baştan okur (:698-703) —
  defter büyüdükçe O(n).
- `ensure_tools_dir` her çağrıda `_prepare_tools_dirs` (manifestteki tüm tools ledger yollarını
  `touch`) + `update_tools_index` çalıştırır (:292-293) — ~536 çağrı noktası (kod yorumu :515).

### 17. tool_runner.py (704)

- `run_tool` (:37) — MIMARI Şema 10 R1-R6 **DOĞRU** (profil kapısı :55, QUARANTINED reddi :57-58,
  komut politikası :65-70, stdout ≤12 MB :34,151, zorunlu 4 alan :27,376-387, yalnız ACTIVE
  `emitted_*` :198,213-214, öncesi/sonrası `git status` :96,171-179). Üretim: `cycle._phase_tools`,
  `spine_orchestrator`, `fixture_runner`.
- **Adaptörler sandbox'sız koşar**: düz `subprocess.run`, `env=dict(os.environ)` (tüm host ortamı —
  token/anahtar değişkenleri dahil adaptöre geçer) (:132-147). "Sandbox kaçışı" yalnız **sonradan**
  git-status farkıyla tespit edilir; ağ/dosya kısıtı yok. (Şema 7 QN "sandbox kaçışı → karantina"
  ifadesi önleme değil tespit.)
- **Risk — eşzamanlı yazıcı yanlış-karantinası**: "raw" görünüm `aria-tools/` altındaki HER dosyanın
  sha256'sını alır (:487-510); `aria-tools/**` her aracın `DEFAULT_DENY` kapsamı dışında → koşu
  sırasında başka süreç (daemon'lar: planner-dispatch/worker-scheduler/gateway, `aria-tools`'a
  yazar) bir satır eklerse `scope_out_mutations` dolar → `tool_health.immediate_quarantine_reason`
  "scope-out mutation" ile **masum adaptörü anında karantinaya alır** (tool_health.py:356-360).
  Ayrıca her koşuda iki kez tüm `aria-tools` (yüzlerce MB) hash'lenir.
- Yazma kapsamı OKUMA glob'larıyla sınıflanır (`_mutation_path_in_tool_scope` →
  `find_scope_violations`) (:588-594); kapsam içi yazım da `repository_mutation_attempt` ile
  karantina (:179,185).
- `_workspace_snapshot`, `_workspace_snapshot_raw` (:430-484): üretimde çağrılmıyor
  (ORPHAN-MEDIUM-526 sonrası `_workspace_snapshots` kullanılıyor) → ölü kopya.
- Eksik ts-node → `environment_unavailable` (karantina değil) (:112-122); Node heap varsayılan 2048
  MB (:133).

### 18. tools_binding.py (254)

- Geri yüklenen `aria/state` ağacına host kimliği bağlama (migrasyon değil); yabancı canonical
  identity → `tools_root_foreign_store` reddi (:219-244). Üretim: `cli.py:3595-3600` ←
  `.github/actions/restore-aria-state/action.yml:223`, `aria-state-maintenance.yml`. Şema 6/19'da
  yok → EKSİK-DOKÜMAN.
- Kod yorumu (:25-33): eski yolda `allow_legacy` izni restore sırasında **0 kez** danışılıyordu
  (kapı `repo_identity.json` yokken kör) — belgelenmemiş tarihsel kör pencere.
- `tools_contract.json`'da identity boşsa kontrol **atlanır** (:233-237).

### 19. trailer_scan.py (237)

- Son tamamlanmış cycle'ın HEAD'inden bu yana commit'lerde `Closes-Pressure:`/`Addresses-Pressure:`
  trailer'ı → baskıyı `closed` yapar (:15-124). Üretim: `learning.py:29,94`. MIMARI Şema 4 "PR
  açılır trailer: Closes-Pressure" yalnız yazım tarafını gösterir; okuma/kapama tarafı
  belgelenmemiş.
- **Risk**: kapatma yalnız trailer metnine dayanır — commit'in yazarı, ARIA'nın kendi PR'ı olup
  olmadığı veya değişen dosyaların baskının kanıtıyla ilişkisi **doğrulanmaz** (:83-94,127-171);
  herhangi bir commit mesajı baskıyı kapatır. (Kanıt modeline aykırı: kapanış kanıtı
  `git:commit:<sha>` = self-declared.)
- `git log prev..HEAD` (:27) `guard_item` dışında: sığ klon/force-push'ta tüm tarama hata verir.

### 20. triage.py (375)

- Baskıyı kanıt yoluna göre 5 şeride ayırır; ajan fitness tavanı (QUARANTINED→blocked,
  CALIBRATE/bayat→needs_review, fitness satırı yoksa needs_review tavanı) (:22,56-185,203-215).
  Üretim: `cycle.py:1377-1379` **ve** `learning.py:100` (aynı cycle'da iki yol; karar anahtarı ile
  tekilleştirilir :105,166). Şema 9 büyük ölçüde DOĞRU.
- `human_only` deseni `infra/**` — repodaki asıl altyapı dizini `infrastructure/` (NATS, docker)
  eşleşmez → bu yollar `human_only` yerine `blocked:unresolved_policy` olur (:209); `docker/**` diye
  dizin yok. Doküman "human_only: infra" der → ince TUTARSIZ.
- `auto_fix_safe` kümesi `tools/**`, `scripts/**`, `*.md` (fnmatch'te `*` `/` geçer →
  `.claude/agents/aria-*.md`, `CLAUDE.md`, `docs/adr/**`) içerir (:213) → ARIA'nın kendi kapıları
  (`tools/gates/**`, `tools/aria-poc/**`) ve ajan tanımları "düşük risk" sınıflanır. (Uygulama
  kapıları `implementation_safety` READONLY ile ayrıca korur; sınıflama ile koruma arasında
  çelişki.)
- `explain_triage`, `list_triage_decisions` yalnız CLI.

### 21. trust.py (199)

- `trust_escalation_derive`: bir capability gap için feedback'te **≥3 farklı `source` etiketi** →
  `feedback_escalated_to_trusted`; `trusted_gap_keys` bunu baskının `trusted_effective` alanına
  taşır (`pressure.py:871-899,1036-1038`) (:13-47,94-107). Kaynak etiketleri kapalı küme {self,
  operator, external_scanner, ai_judge, manual_audit} (`feedback.py:35`) ama **self-declared**;
  okuma `list_feedback_without_integrity` ile **hash zinciri doğrulanmadan**
  (`feedback.py:315-319`). Güven yükseltmesi bütünlüğü doğrulanmamış satırlardan türetilir → Şema 7
  kanıt güven modeliyle uyumsuz, EKSİK-DOKÜMAN.
- `ref_staleness_check`: feedback ref'lerinde `git blame -L` ile satırın son commit'i gözlenen
  commit'ten farklıysa `stale` (:50-91,139-162); ≤100 örnek. `ref_status_by_feedback_id` →
  `reverify.py:37`, `pressure.py:1036`. Üretim: `learning.py:98-99`.

### 22. turn_budget.py (288) + 23. turn_budget_policy.py (204)

- İki bağlayıcı kap: (a) **cycle kapı** = `ARIA_JOB_DEADLINE_EPOCH` − 120 s kapanış payı; (b) **tur
  kapı** = uygulayıcı isteği başına budgeted (Bash/Edit/Write/MultiEdit/NotebookEdit) `allow` hook
  verdict sayısı, varsayılan **120**, tavan `2×MAX_AFFECTED_PATHS = 400` (turn_budget.py:70-83;
  turn_budget_policy.py:64-83; `plan_convergence.py:138`). Sayım `hook_decisions` defterinden;
  aşımda `implementer_turn_budget_exhausted` (:186-207).
- Merge öncesi kanıt: `turn_budget_evidence` →
  `implementation_safety._check_cycle_and_turn_budget_cap`; satır yoksa
  `native_turn_budget_evidence_unavailable`, kap politikadan farklıysa
  `native_turn_budget_cap_mismatch` (:217-268). **Operatör politika değerini değiştirirse, önceki
  cap ile koşmuş tüm uygulamalar merge'de reddedilir** (:229-233).
- USD burada kasıtlı olarak kabul kriteri değil (managed_subscription = telemetry) (:8-13).
- Kod içi KAYMA: turn_budget_policy.py docstring "`budgeted_turns` = 60" (:17), kod ve default JSON
  **120** (:73; `data/genesis_policy_default.json:73-75`).
- Hiçbir dokümanda yok (MIMARI/NASIL grep: 0) → EKSİK-DOKÜMAN (HARD_FAIL `cycle_and_turn_budget_cap`
  Şema 16/17'ye eklenmeli).

### 24. twin.py (1015)

- Şema 5 "Twin: bağımlılık grafiği + TESTED_BY + churn + eş-değişim; `twin/map.json`, 400 komit"
  **DOĞRU** (:9-13,46,51). Ek kurallar belgelenmemiş: >50 dosyalı commit'ler churn/co-change'den
  **tamamen çıkarılır** (:58,805-807) → büyük squash-merge PR'ları haritada görünmez; churn eşiği
  ≥2, co-change ≤2000 çift (:59-67).
- Sığ klon / okunamayan git log → `twin_history_unavailable[_shallow]` ile yayın reddi
  (:760-794,851-874) — 2026-09-12'ye kadar gece şeridi `fetch-depth:1` ile boş harita yayınlıyordu
  (:771-775).
- **Intent katmanı** (`intent_context_for_files`, :938-1002): dosya başına son 3 commit'in subject +
  ilk "WHY" satırı + ADR/finding ref'leri; mint anında zarfa mühürlenir
  (`agent_invocations.py:427-429`). `_render_recent_intent` bunu prompt'a **`<untrusted_*>` sarmalı
  ve sanitizasyon olmadan** düz Markdown olarak basar (`agent_invocations.py:653-686`) → commit
  mesajı (herhangi bir katkıcının) ajan prompt'una talimat gibi girer. MIMARI Şema 5 "prompt v3 …
  `<untrusted_*>` etiketli" iddiası bu blok için TUTARSIZ.
- **`self_features` "pilot"u**: yalnız 2 sabit kodlu özellik
  (`runtime_artifacts.autonomy_output_summary`, `knowledge_graph.conventions_for_paths`) ve sabit
  test adları (:291-305,333-356,375) — genel bir özellik çıkarıcı değil, pilot/stub;
  `_qualified_twin_context` mint'te yeniden niteler (:509-590, `agent_invocations.py:178-180`).
- `_git` **timeout'suz** `subprocess.run` (:885-888) — state_store'un her git çağrısını sınırlama
  disiplinine aykırı; büyük repo/NFS'te cycle fazı asılabilir (faz SIGALRM'ı dışında).
- Üretim: cycle `twin_refresh` fazı, `agent_invocations.py:178,427`, CLI `cli.py:3527-3553`.

### 25. typed_judgment.py (766)

- Saf modül: 3 tipli primitif (`choice`/`score`/`noul`), batch kabı, adla reddeden ayrıştırıcı (25
  neden kodu) (:49-87,666-723). Model `confidence_source` ve dosya yolu **yazamaz**; kanıt
  `{index, quote≤120}` olarak verilir ve quote mint'te sabitlenmiş excerpt'e karşı kontrol edilir
  (:16-32,527-560). `choice` güveni <0.5 → red (:610-612). Gerekçe'de `BANNED_PHRASES_DEFAULT` → red
  (:568-571).
- Üretim: `judgment_bridge.py:42-141` (tekil yanıtta **tipli blok opsiyonel**, eski düzyazı
  `verdict` bloğu da geçerli :103-114) ve `tools/aria-poc/ci_executor_judge_batch.py:212-416`
  (batch, varsayılan kapalı). MIMARI Şema 11'de hiç yok → EKSİK-DOKÜMAN.
- **Zayıf alıntı doğrulaması**: `quote in content` alt-dize testi, min uzunluk 1 (:533,551) → tek
  karakterlik (ör. `"e"`, `"{"`) alıntı her excerpt'te "doğrulanmış" sayılır. Excerpt
  sabitlenmemişse `verified=False` ama kabul edilir (:527-532,555-559).
- Excerpt gövdesi **kaçışsız** `<untrusted_evidence_excerpt>` içine basılır (:454-456; bilerek,
  alıntı doğrulaması için) → repo dosyasında `</untrusted_evidence_excerpt>` dizisi etiketi erken
  kapatabilir (text_safety'deki base64 Mode B burada kullanılmıyor).

### 26-28. upcasters/**init**.py (14), cycles.py (82), service_map_v1_to_v2.py (133)

- `upcast_cycle_row`: v<3 satıra `event`'ten `status` türetir, bilinmeyen event → GovernanceError,
  kopya döner (cycles.py:47-87). Şema 19 E1 **DOĞRU**. Tek üretim tüketicisi
  `autonomy_evidence.py:643-646,892,3018,3267` — genel cycles okuyucuları (ör.
  `tool_sit_out.cycles_started_since` `event` alanını doğrudan okur) upcaster'dan geçmez.
- `service_map_v1_to_v2.upcast/downcast`: **yalnız test** (`tests/test_phase3_discovery_mfe.py`);
  üretimde tek referans `discovery.py:208` docstring'i → ölü. Docstring "hiç bilgi kaybolmaz /
  round-trip korunur" (:32-36) iddiası, `upcast`'in `web/modules`, `web/apps` placeholder
  satırlarını **attığı** (:97-98) için yanlış.

### 29. usage_ledger.py (109)

- Spawn başına token kullanımı → `knowledge-graph/context-usage.jsonl` (`context_usage`). Tek yazıcı
  `tools/aria-poc/claude_runtime.py:1018-1023` → **yalnız Claude spawn'ları** kaydedilir; codex/zai
  yok.
- Docstring "Record-only; tüketici yok; bir satır asla bir eylemi yetkilendirmez" (:14-17) —
  **bayat**: `token_economy.usage_per_accepted_result` bu defteri okuyup efor düşürme önerisi
  üretir, `ci_executor.py:2206-2212` spawn eforunu buna göre düşürür → satırlar dolaylı olarak
  davranışı değiştirir (kod içi TUTARSIZ).

### 30. validation.py (1036)

- Lane-A doğrulama komut koşucusu: `change_id` change ledger'da çözülmeli, `commit_sha` **workspace
  HEAD'e eşit** olmalı (ORPHAN-CRITICAL-728), temiz worktree; her komut
  `validation_runs_ledger.record_validation_run` ile tek yazıcıya gider; süreç grubu timeout'ta
  SIGKILL (:141-311,436-483,584-659). Üretim: `apply_engine`, `implementation_delivery`,
  `experiment`, `experiment_author`, `fitness`, `pr_manager`. Şema 17 (apply gate) ile uyumlu.
- **İzin listesi açığı**: `npx ts-node` yalnız argv'nin _herhangi bir_ öğesinin
  `tools/aria-adapters/*.test.ts` olmasını ister (:968-970) →
  `npx ts-node -e "<keyfi kod>" tools/aria-adapters/x.test.ts` geçer. `python3 -m unittest` **hiç
  kısıtsız** (:974-975; `discover -s <dizin>` dahil). `npx nx` hedef kontrolü birleştirilmiş argv'de
  alt-dize (`--target=test…`) (:965-967). `cargo --config` reddi, ajan yazdığı ağaçta
  `.cargo/config.toml` ile aşılabilir (aynı etki). Sandbox (bwrap) yalnız `spawn_wrapper`
  verildiğinde (ajan ağacı) uygulanır (:568-574); cycle baseline'ı sarmasız.
- `evaluate_validation_gate` → `ready_for_pr`/`blocked` (:345-389); `_regression_status` 4 değer.

### 31. validation_env.py (176)

- Doğrulama çocuğunun ortamı **inşa edilir**: baseline +
  `HOME/USER/XDG_*/CI/GIT_CONFIG_{GLOBAL,SYSTEM,NOSYSTEM}` + `PYTHON*/NODE_*/NX_*/CARGO_*/RUST*`
  (secret biçimli hariç); ARIA store bağları, `GH_TOKEN`, `GIT_CONFIG_VALUE_n`,
  `ARIA_JOB_DEADLINE_EPOCH` düşer (:64-80,121-166). Üretim: `validation.py:509`. Belgelenmemiş
  güvenlik sınırı (EKSİK-DOKÜMAN).
- Karşıtlık: `tool_runner` adaptörleri **tüm** `os.environ` ile koşturur (tool_runner.py:132) — aynı
  kernelde iki farklı ortam disiplini.

### 32. validation_suite.py (144)

- Kanonik suite 4 komut: `nx affected --target=test`, `--target=lint`, `npm run type-check`,
  `node tools/quality/quality.mjs format check-changed` (:62-67) — tüm kapıların tek SSoT'u
  (`command_policy`, `auto_merge`, `implementation_safety`).
- **Zayıflık**: `canonical_command_satisfied_by` kanonik komuta **her türlü sondaki argümanı**
  "meşru daraltma" sayar (:84-89; bash deseni :130-135) →
  `nx affected --target=test --base=HEAD --head=HEAD` veya `--exclude=*` hiçbir projeyi test etmeden
  kanonik suite'i "karşılar".
- Kod yorumu ORPHAN-MEDIUM-436'yı "owner okan, deadline 2026-09-06" diye izliyor (:36-38) — son
  tarih geçmiş (bugün 2026-09-25).

### 33. validation_matrix_gate.py (783)

- 4 risk türü (auth/tenant/schema/event) yol sezgisiyle; 3 katman EXISTENCE → PATTERN → RUN-PASS +
  cmd korelasyonu; `historical_attestation` kapıyı atlar; risk yoksa ≥1 doğrulanmış validation_run
  şart; Plan 031 regresyon-çapası (:72-205,540-772). Üretim: `cycle.py:2905-2913`
  (`require_regression_anchor=True`), CLI `cli.py:3725`, `change_ledger`. Şema 17 C3 "enforced:
  matris kapısı" DOĞRU ama ayrıntı yok.
- **EXISTENCE/PATTERN katmanları değişikliğe değil tüm repoya bakar**
  (`repo_root.glob(spec["path_glob"])`, :328-361): ör. `apps/auth-service/**/*.spec.ts` içinde
  `@UseGuards` geçen herhangi bir mevcut dosya, event-contracts için `validate|schema` regex'i —
  main'de zaten sağlanır → iki katman pratikte **boş geçer** (vacuous); diff'in yeni test içerip
  içermediğini ölçmez.
- **schema_change korelasyonu oyunlanabilir**: `expected_cmd_substring="schema-invariants"` nx dışı
  olduğu için **alt-dize** kuralıyla eşlenir (:474-478). Gerçek betik
  `npm run test:schema-invariants` Lane-A izin listesinde **yok** (`validation._allowed_npm_script`:
  yalnız test/lint/build/type-check/format:check/gates:_/invariants:_, validation.py:1001-1013) →
  dürüst yoldan karşılanamaz; buna karşılık
  `npx nx affected --target=test --exclude=schema-invariants` gibi exit-0 bir komut alt-dizeyi
  içerip kapıyı geçer.
- Regresyon çapası deseni `fixture` (yolun herhangi bir yerinde, büyük/küçük harf duyarsız) ve
  `(^|/)tests?/` (:216-224) → ilgisiz bir test/fixture dosyasına dokunmak "her otonom düzeltme
  regresyon testi bırakır" garantisini karşılar.
- nx kimlik eşlemesi (`nx test P` ≡ `nx run P:test` ≡ `nx run-many --target=test --projects=P`)
  (:412-492) — önceki "hiçbir çalıştırılabilir komut karşılayamaz" hatasının düzeltmesi, kod
  yorumunda belgelenmiş.

### 34. validation_runs_ledger.py (580)

- `validation/validation-runs.jsonl` tek yazıcı/şema v2 (status türetilir: ok/failed/timeout), log
  dosyası sha256 ile bağlanır; `verify_validation_run` yeniden hash'ler (:82-343,526-552). Üretim:
  `validation._run_one`, `validation_matrix_gate`, `auto_merge` (üçlü kapı), `observability`,
  `finding`, `experiment`.
- **Öz-onay reddi üretimde devre dışı**: `runner_identity == change_author_identity` kontrolü yalnız
  `change_author_identity` verilirse çalışır (:280-290); ana üretim yolu
  `apply_engine.py:746-755,986-995` bu argümanı **hiç geçmez** (yalnız `experiment.py:328-372` ve
  testler geçer). `runner_identity` de serbest string (`lane_runner_identity(fallback=...)`).
  Docstring'in "runner planner'dan farklı OLMALI" (:20-24) garantisi ana yolda yok.
- **Matris kapısı ile birleşince hata**: `validation_matrix_gate` risk türü yoksa yalnız
  `verify_validation_run` başarısına bakar (validation_matrix_gate.py:641-653) — bu fonksiyon yalnız
  log hash eşitliğini doğrular, **`status`/`exit_code` kontrol etmez** → yalnız **başarısız**
  (exit≠0) koşuları olan risk-siz bir değişiklik `no_risk_types_verified` ile geçer.
- Log hash yalnız sonradan değişikliğe karşı korur; log'u yazan süreç satırı da yazar (bağımsız
  tanıklık yok).

### 35. verification_gate.py (435)

- `submit_worker_result` (lease hash + aktif claim + süre dolumu, §G.3) ve `verify_worker_result`
  (worktree var · head kayması yok · temiz · base ancestor · diff hash · büyük diff artefakt hash ·
  trailer · izinli komutlar geçti). Şema 4 VG listesi **DOĞRU**. Üretim:
  `worker_dispatch_hook.py:414`, `tools/aria-poc/worker_executor.py:240,435`, CLI
  `cli.py:5821-5833`.
- **Güvenlik tutarsızlığı**: `verify_worker_result` doğrulama komutlarını **ajanın yazdığı
  worktree'de** `subprocess.run(shlex.split(cmd), cwd=worktree, timeout=120)` ile koşar (:303-309) —
  `validation_env.build_validation_env` **yok** (tüm `os.environ`: `GH_TOKEN`, `ARIA_TOOLS_DIR`…
  çocuğa geçer), **bwrap/spawn_wrapper yok**, süreç grubu öldürme yok; `TimeoutExpired` yakalanmaz
  (doğrulama satırı yazılmadan istisna). Aynı risk sınıfını `validation.py`/`validation_env.py`
  (ARIA-MEDIUM-066, ARIA-HIGH-124) kapatmıştı; bu yol kapsam dışı kalmış.
- Docstring "tokensız çağıranlar reddedilir … CLI fail-open kalmasın" (:161-163) ama CLI
  `worker-result submit --allow-legacy-no-token` bayrağını sunar (`cli.py:2852,5827`) → lease
  kontrolü tamamen atlanır.
- Koşullu atlamalar: `expected_trailer` boşsa trailer kontrolü, `diff_hash` boşsa diff kontrolü,
  `base_sha` boşsa ancestor kontrolü atlanır (:284-301); trailer eşleşmesi `trailer in log`
  alt-dize.
- `_resolve_active_claim_for_submit` yorumu "recorded_at'e göre sırala" der, sıralama yapılmaz
  (defter sırası) (:66-68).

### 36. watchdog_freeze.py (142)

- ARIA merge'ünü, **dış watchdog'un açtığı GitHub incident issue'ları** (etiket + başlık öneki
  `.github/manifests/aria-state-watchdog.json`) açıkken veya okunamazken reddeder (fail-closed)
  (:62-133). Üretim: `merge_authority.py:29,59`. Cycle ve insan merge'leri bilerek dondurulmaz
  (:15-35).
- MIMARI Şema 7 `WD[aria_watchdog: 600sn olay yoksa stall …] -.donma.-> M2` çizer → kodda merge
  dondurmanın kaynağı kernel `aria_watchdog` değil, `aria-external-watchdog.yml`'ın açtığı **GitHub
  issue**'su (aria/state tipi ≥50 saat hareketsiz) → TUTARSIZ (kaynak yanlış).
- Manifest yorumu "producer 01:13'te koşar" der; `aria-auto-cycle.yml:66` cron `13 17 * * *` (17:13)
  → manifest yorumu KAYMA (eşik 50 saat yine de iki geceyi kapsar).

### 37. worker_dispatch.py (1181)

- Dispatch defteri + claim/lease + reducer + reaper. `DEFAULT_LEASE_SECONDS=1800`,
  `DEFAULT_MAX_LEASE_REQUEUES=2` (:38,65) — MIMARI Doğrulama Düzeltmesi #1 DOĞRU. Durum **yalnız**
  `requests ∪ claims ∪ worker-results ∪ verification-results` katlamasından türetilir; governance
  "yalnız denetim izi" (:835-937).
- **`cancel_dispatch_request` ve `mark_dispatch_picked_up` etkisiz**: yalnız governance
  `dispatch_request_state_changed` yazarlar (:250-260,298-308), reducer governance'ı okumaz → iptal
  edilen atama `pending` kalır, `next_pending_assignment` onu yine döndürür, worker daemon'u claim
  eder. `already_cancelled` dalı (:289-290) hiçbir zaman tetiklenemez.
- **`prune_worktrees` hiçbir şey budayamaz**: yalnız `{completed, cancelled, expired}` durumlarını
  budar (:403), reducer bu üç durumu **hiç üretmez** (üretilenler:
  pending/picked_up/submitted/submit_rejected/verified/verification_failed/stale/human_required/multiple_active_claims_corruption).
  Ayrıca yalnız CLI'dan çağrılır (`cli.py:4351`), hiçbir workflow koşturmaz → `aria-worktrees/`
  birikir. MIMARI Şema 4 `V & FV -.terminal + 7 gün.-> prune_worktrees` → TUTARSIZ.
  (`worker_executor.py:411` yorumu da "prune_worktrees reaps" der.)
- Şema 4 `A -->|create_dispatch_request| Q1` — `create_dispatch_request`/`auto_batch_dispatch` (v1,
  triage-tabanlı, `auto_fix_safe|needs_review`) **yalnız CLI** (`cli.py:4206,4216`); otonom yol
  `promotion_controller.py:154-184` satırı kendisi yazar (`aria/dispatch-request/v2`) ve
  `git worktree add` yapmaz → KAYMA (fonksiyon adı yanlış).
- **Risk**: reaper (`reap_expired_assignment_claims`, cycle fazı `cycle.py:1579`) `submitted`
  durumundaki atamayı da "canlı" sayar (:1107, ACTIVE set :53-58); submit claim'i kapatmadığı için
  doğrulama lease süresi (1800 s) içinde olmazsa claim `released` yazılır → reducer `pending`'e
  döndürür → **zaten sunulmuş iş yeniden dağıtılır**. Reaper okuma-karar-yazma'yı
  `state_transaction` dışında yapar (:1098-1152).
- Ölü: `_latest_request_states` (:818-832), `recover_orphan_governance` (:940-978) — üretim çağıranı
  yok.
- `_derive_assignment_state` docstring'i "completed doğrulamadan sonra set edilir" der (:527-528) —
  reducer `verified` üretir, `completed` değil.

### 38. worker_dispatch_hook.py (548)

- Worker daemon tiki: bir pending atama → retry bütçesi ön-kontrol → provider cooldown ön-kontrol →
  claim → `worker_executor.py` alt süreci (lease token env ile) → `verify_worker_result` → dallanma
  (:115-548). Üretim: `autonomous_worker_scheduler.py`. `DEFAULT_MAX_RETRIES=3` (:65) — MIMARI
  düzeltme #1 DOĞRU.
- **Merge dalı sabit kodlu stub**: `auto_fix_safe` + PR varsa
  `merge_result = {"decision": "blocked", "reasons": ["enterprise_readiness_claim_id_required"]}`
  **literal** atanır, hiçbir merge çağrısı yapılmaz (:450-470) → `"merged"` statüsü **yapısal olarak
  ulaşılamaz**; zorunlu `github_adapter` yalnız tip adı loglamak için kullanılır (:492). Docstring
  "merged — merge_if_green kararı merged" (:33-34) EKSİK-KOD.
- **Retry bütçesi → human_required değil**: 3. doğrulama hatasında yalnız
  `worker_dispatch_max_retries_exceeded` governance satırı yazılır, claim bırakılmaz (:507-525);
  `record_human_required` çağrısı **yok** → atama `verification_failed`'da kalır, reaper'ın canlı
  kümesinde de değildir (worker_dispatch.py:53-58). MIMARI Şema 4
  `FV … retry bütçesi: 3 → human_required` → TUTARSIZ.
- **Sınırsız executor yeniden denemesi**: `worker_executor` non-zero çıkışında claim
  `worker_executor_failed` ile bırakılır (:391-401) ama `assignment_retry_count` yalnız
  `verification_gate_failed` sayar (worker_dispatch.py:760-781) → çöken bir executor her tikte (30
  s) aynı atamayı yeniden claim/spawn eder; üst sınır yok (Şema 4 "başarısız → released → pending
  retry < 3" iddiası bu dal için yanlış).
- Head-of-line: `next_pending_assignment` her zaman en eski pending'i döndürür
  (worker_dispatch.py:557-566); provider cooldown / bütçe tükenmişse aynı atamada döner, sonrakilere
  geçmez.
- Executor'a `**os.environ` geçer (:297-301).

### 39. workflow_contract_registry.py (889)

- ADR-036 iş-başına workflow sözleşmesi SSoT: izinler, yazma yolu regex'leri, artefakt adı/yolu,
  retention, token kaynağı, ağ politikası, zorunlu adımlar + adım sırası + abort gate
  (`WorkflowAbortGate`: lease kapısından sonraki her adım `blocked != 'true'` taşımalı)
  (:47-157,232-755). Üretim: `workflow_contracts.py`, `preflight.py`, `docs_ssot.py`,
  `readiness_proofs.py`, `ci_executor` (wall-clock).
- **Sayılar**: `WORKFLOW_CONTRACTS` = **10 workflow / 11 job** (7 `aria-*` +
  `finding-closure-reconcile`, `finding-state-sweep`, `rule-health-report`) + **4** audited
  exclusion (`aria-kernel`, `aria-state-maintenance`, `aria-external-watchdog`,
  `aria-runner-capability-probe`, hepsi `9999-12-31`). MIMARI Şema 19 "9 lane için kayıtlı iş
  sözleşmesi" → KAYMA. Tüm 11 `aria-*` workflow ya sözleşmeli ya muaf.
- Modül docstring "3 `aria-kernel*` test workflow'u audited-exclusion" (:20-36) — artık yalnız 1
  (`aria-kernel-fast` emekli, :758-762) → kod içi KAYMA.
- Şema 19 W4 "duvar saati: job timeout − 5 dk" **DOĞRU** (`WALL_CLOCK_RESERVE_MINUTES=5`, :852-872).
  Belgelenmemiş: `aria-agent-executor` job timeout **510 dk (8,5 saat)** — reserve 9000 s =
  `state_store_lifecycle_arcs` 2700+5400+900 s'den türetilir (:321-340; YAML
  `aria-agent-executor.yml:148`); `aria-auto-cycle` 360 dk (:459).
- `verify_workflow_registry` (tüm envanter kararı, `workflow_contracts.py:93`)
  `control-reachability.dormant.json`'da **hiç çağrılmayan kontrol** olarak waiver'lı, son tarih
  **2026-10-09** (ORPHAN-HIGH-573) → envanter bütünlüğü kontrolü üretimde bağlı değil.
