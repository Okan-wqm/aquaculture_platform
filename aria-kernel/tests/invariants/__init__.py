"""Plan ARIA-V3 — invariant subpackage.

Subpackage name ``invariants`` (under ``aria-kernel/tests/``) so it
participates in the existing ``python3 -m unittest discover aria-kernel``
sweep but groups V3 invariants cleanly for cross-phase audit.
"""
