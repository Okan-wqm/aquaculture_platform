/**
 * External Entities Index
 *
 * Read-only entity references for cross-service analytics.
 * These entities are owned by other services but accessible
 * through the shared database for analytics queries.
 */

export * from './tenant.entity';
export * from './user.entity';
export * from './subscription.entity';
export * from './invoice.entity';
