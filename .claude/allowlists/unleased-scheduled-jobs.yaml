# Scheduled methods that still use a raw NestJS scheduler decorator instead of
# the kernel `@ScheduledJob` (ADMIN-HIGH-013). Every raw `@Cron` / `@Interval` /
# `@Timeout` runs on every replica of its service with no lease and no
# heartbeat. tests/invariants/scheduled-jobs-leased.spec.ts requires every
# scheduled method in apps/, libs/ and platform/libs/ to be `@ScheduledJob`, or
# to appear here with an owner, a future expiry, the finding that owns the
# conversion and a reason. `ceiling` is the maximum number of entries and only
# decreases; an entry whose site is gone or converted fails the gate.
#
# admin-api-service — the finding's surface — is fully converted and may not
# appear here. The remaining services are the fleet half of the same finding.
version: 1
ceiling: 31
entries:
  - site: apps/farm-service/src/feeding-protocol/services/feeding-cron-v2.service.ts#hourlyTick
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-108
    reason: farm-service consolidated the feeding sweeps into one hourly tick; it converts with the rest of the service
  - site: apps/farm-service/src/feeding-protocol/services/feeding-cron-v2.service.ts#mealWindowSweep
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/feeding-protocol/services/feeding-cron-v2.service.ts#retentionCleanup
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/infrastructure/watchdog-cron.service.ts#runScheduledScan
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/regulatory/services/report-scheduler.service.ts#deadlineSweep
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/regulatory/services/report-scheduler.service.ts#monthlyRollover
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/regulatory/services/report-scheduler.service.ts#retrySweep
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/regulatory/services/report-scheduler.service.ts#weeklyRollover
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#checkLowStock
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#checkOverdueMaintenance
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#checkOverdueWorkOrders
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#cleanupOldData
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#generateMaintenanceWorkOrders
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#minioOrphanCleanup
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#monthlyComplianceReport
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#refreshAnalyticsViews
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/cron-jobs.service.ts#weeklyMaintenanceSummary
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#analyzeFCR
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#checkFeedStock
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#dailyFeedingSummary
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#generateDailyFeedingPlan
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#sendFeedingReminders
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/scheduler/feeding-scheduler.service.ts#weeklyFeedForecast
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/task/services/auto-rule-trigger.service.ts#processScheduleRules
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/task/services/recurring-task.service.ts#generateDueTasks
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/task/services/task.service.ts#detectOverdueTasks
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/weather/services/environment-cron.service.ts#retainCanonicalObservations
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: apps/farm-service/src/weather/services/environment-cron.service.ts#syncDueProviders
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in farm-service; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: libs/backend-common/src/database/retention/retention-enforcement.service.ts#enforceAll
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in backend-common; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: platform/libs/outbox/src/outbox-worker.service.ts#cleanupPublished
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in outbox; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
  - site: platform/libs/outbox/src/outbox-worker.service.ts#pollAndPublish
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: raw @Cron in outbox; runs on every replica with no lease and no heartbeat until converted to @ScheduledJob
# Files that call CronHeartbeatService directly instead of through
# ScheduledJobRunner. Same governance; converting the file's scheduled methods
# to @ScheduledJob removes the direct dependency.
directHeartbeatFiles:
  - site: apps/farm-service/src/infrastructure/watchdog-cron.service.ts
    owner: okan
    expiry: 2026-12-31
    findingId: ADMIN-HIGH-013
    reason: heartbeat adopted before the runner existed; its @Cron sites are raw entries above and convert together
