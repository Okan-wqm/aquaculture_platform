{"parentUuid":"346e122a-8653-4061-8f70-9f2885432f6c","isSidechain":true,"agentId":"a30ecb6e5b19562e6","message":{"model":"claude-opus-4-6","id":"msg_019LsqQFAWbsMNUTZCyJ8oGA","type":"message","role":"assistant","content":[{"type":"text","text":"I now have a comprehensive understanding of the entire sensor-service. Let me compile the exhaustive feature inventory.\n\n---\n\n# SENSOR-SERVICE COMPLETE FEATURE INVENTORY\n\n---\n\n## DOMAIN 1: SENSOR REGISTRATION & MANAGEMENT\n\n### Entities\n\n**1. Sensor** (`sensors` table)\n- Fields: `id` (uuid PK), `name`, `serialNumber` (unique), `type` (enum), `manufacturer`, `model`, `firmwareVersion`, `status` (enum), `tenantId`, `pondId`, `farmId`, `tankId`, `siteId`, `departmentId`, `systemId`, `equipmentId`, `description`, `location`, `metadata` (jsonb), `configuration` (jsonb), `calibrationData` (jsonb), `protocolId` (FK->SensorProtocol), `protocolConfiguration` (jsonb), `connectionStatus` (jsonb), `typeDefinitionId` (FK->SensorTypeDefinition), `registrationStatus` (enum), `lastSeenAt`, `lastCalibratedAt`, `isActive`, `createdAt`, `updatedAt`, `createdBy`, `parentId` (FK->Sensor self-ref), `isParentDevice`, `dataPath`, `sensorRole` (enum), `unit`, `minValue`, `maxValue`, `calibrationEnabled`, `calibrationMultiplier`, `calibrationOffset`, `alertThresholds` (jsonb), `displaySettings` (jsonb)\n- Relationships: ManyToOne SensorProtocol, ManyToOne SensorTypeDefinition, ManyToOne Sensor (self-ref parent), OneToMany Sensor (children)\n- Decorated with `@Auditable()`\n- Indexes: `[tenantId, status]`, unique on `serialNumber`, `[tenantId, siteId]`, `[tenantId, departmentId]`, `[tenantId, systemId]`, `[tenantId, equipmentId]`, `tenantId`, `pondId`, `tankId`, `protocolId`, `parentId`\n\n**Enums:**\n- `SensorType`: TEMPERATURE, PH, DISSOLVED_OXYGEN, SALINITY, AMMONIA, NITRITE, NITRATE, TURBIDITY, WATER_LEVEL, MULTI_PARAMETER, FLOW_RATE, CONDUCTIVITY, ORP, CHLORINE, CO2\n- `SensorStatus`: ACTIVE, INACTIVE, MAINTENANCE, ERROR, OFFLINE\n- `SensorRegistrationStatus`: DRAFT, PENDING_TEST, TESTING, TEST_FAILED, ACTIVE, SUSPENDED\n- `SensorRole`: PARENT, CHILD\n\n**Status Transitions (SensorRegistrationStatus):**\n- DRAFT -> PENDING_TEST (registerSensor with connection test)\n- PENDING_TEST -> TESTING (testSensorConnection)\n- TESTING -> TEST_FAILED | ACTIVE (test result)\n- ACTIVE -> SUSPENDED (suspendSensor)\n- SUSPENDED -> ACTIVE (reactivateSensor)\n- Any -> DRAFT (update)\n\n**2. SensorProtocol** (`sensor_protocols` table)\n- Fields: `id`, `code` (unique), `name`, `description`, `category` (enum), `subcategory` (enum), `connectionType` (enum), `configurationSchema` (jsonb - JSON Schema), `defaultConfiguration` (jsonb), `documentationUrl`, `iconName`, `isActive`, `requiresGateway`, `gatewayProtocol`, `supportsDiscovery`, `supportsBidirectional`, `supportsPolling`, `supportsSubscription`, `defaultPort`, `defaultBaudRate`, `defaultTimeout`, `maxConnectionsPerInstance`, `requiredPermissions`, `supportedDataTypes`, `sortOrder`, `createdAt`, `updatedAt`\n\n**Enums for SensorProtocol:**\n- `ProtocolCategory`: INDUSTRIAL, IOT, SERIAL, WIRELESS\n- `ProtocolSubcategory`: MODBUS, FIELDBUS, ETHERNET_INDUSTRIAL, PLC_NATIVE, PLC, BUILDING_AUTOMATION, REALTIME_ETHERNET, MESSAGE_QUEUE, REQUEST_RESPONSE, REALTIME, WIRED_SERIAL, SOCKET, BUS, SERIAL_PORT, NETWORK, LPWAN, MESH, SHORT_RANGE\n- `ConnectionType`: TCP, UDP, SERIAL, USB, WIRELESS, HYBRID, ETHERNET, I2C, ONE_WIRE, SPI, BLUETOOTH\n\n**3. SensorTypeDefinition** (`sensor_type_definitions` table)\n- Fields: `id`, `tenantId`, `typeKey` (unique per tenant), `displayName`, `description`, `icon`, `category`, `industry`, `isSystem`, `defaultChannels` (jsonb), `metadata` (jsonb), `createdAt`, `updatedAt`\n- Business rule: `isSystem=true` types are global/immutable; tenant types are tenant-scoped\n\n**4. IndustryTemplate** (`industry_templates` table)\n- Fields: `id`, `templateKey` (unique), `displayName`, `description`, `icon`, `sensorTypes` (jsonb), `dashboardLayout` (jsonb), `alertPresets` (jsonb), `isActive`, `createdAt`\n\n**5. ChannelDetectionLog** (`channel_detection_log` table)\n- Fields: `id`, `tenantId`, `sensorId` (FK->Sensor), `rawSample` (jsonb), `aiAnalysis` (jsonb), `proposedChannels` (jsonb), `userAction` (APPROVED|REJECTED), `finalChannels` (jsonb), `createdAt`\n\n### GraphQL API - Sensor\n\n**Queries:**\n- `sensor(id: ID!)` -> Sensor\n- `sensors(page, limit, pondId?, status?)` -> [Sensor]\n- `latestReading(sensorId: ID!)` -> SensorReading\n- `latestReadingsBatch(sensorIds: [ID!]!)` -> [SensorReading]\n- `readings(sensorId, startTime, endTime, limit?)` -> [SensorReading]\n- `aggregatedReadings(sensorId, startTime, endTime, interval?)` -> AggregatedReadingsResponse\n- `sensorsByProtocol(protocolCode)` -> [RegisteredSensorType]\n- `sensorStats()` -> SensorStatsType (total, active, inactive, testing, failed, byType, byProtocol)\n- `parentDevice(id)` -> ParentDeviceType\n- `childSensors(parentId)` -> [ChildSensorType]\n- `parentDevices(filter?, pagination?)` -> SensorListType\n\n**Mutations:**\n- `createSensor(input: CreateSensorInput)` [TENANT_ADMIN, MODULE_MANAGER] -> Sensor\n- `updateSensor(input: UpdateSensorInput)` [TENANT_ADMIN, MODULE_MANAGER] -> Sensor\n- `ingestReading(input: IngestReadingInput)` -> SensorReading\n- `batchIngestReadings(input: BatchIngestInput)` -> Int\n- `registerSensor(input: RegisterSensorInput)` [TENANT_ADMIN, MODULE_MANAGER] -> SensorRegistrationResultType\n- `testSensorConnection(sensorId)` [TENANT_ADMIN, MODULE_MANAGER] -> ConnectionTestResultType\n- `activateSensor(sensorId)` [TENANT_ADMIN, MODULE_MANAGER] -> RegisteredSensorType\n- `suspendSensor(sensorId, reason?)` [TENANT_ADMIN, MODULE_MANAGER] -> RegisteredSensorType\n- `reactivateSensor(sensorId)` [TENANT_ADMIN, MODULE_MANAGER] -> RegisteredSensorType\n- `updateSensorProtocol(input)` [TENANT_ADMIN, MODULE_MANAGER] -> SensorRegistrationResultType\n- `updateSensorInfo(input)` [TENANT_ADMIN, MODULE_MANAGER] -> RegisteredSensorType\n- `deleteSensor(sensorId)` [TENANT_ADMIN] -> Boolean\n- `registerParentWithChildren(input)` [TENANT_ADMIN, MODULE_MANAGER] -> ParentWithChildrenResultType\n- `testParentConnection(parentId)` [TENANT_ADMIN, MODULE_MANAGER] -> ConnectionTestResultType\n- `deleteParentWithChildren(parentId)` [TENANT_ADMIN] -> Boolean\n\n**Business Rules:**\n- Serial number must be unique (globally via entity-level unique index)\n- Duplicate serial number within tenant check on createSensor\n- Federation reference resolver always filters by tenantId (security)\n- createSensor auto-creates default channels if typeDefinitionId is provided\n- Parent-child: parent sensor has `isParentDevice=true`, children have `parentId` FK, `sensorRole=CHILD`\n\n### GraphQL API - Sensor Types\n\n**Queries:**\n- `sensorTypes()` -> [SensorTypeDefinition] (includes system types)\n- `industryTemplates()` -> [IndustryTemplate]\n- `pendingChannelProposals(sensorId)` [TENANT_ADMIN, MODULE_MANAGER] -> [ChannelDetectionLog]\n\n**Mutations:**\n- `createSensorType(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `updateSensorType(id, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deleteSensorType(id)` [TENANT_ADMIN]\n- `applyIndustryTemplate(templateKey)` [TENANT_ADMIN]\n- `detectSensorChannels(sensorId, samples)` [TENANT_ADMIN, MODULE_MANAGER] -> ChannelDetectionLog (AI channel detection)\n- `approveChannelProposal(proposalId, modifications?)` [TENANT_ADMIN, MODULE_MANAGER]\n- `rejectChannelProposal(proposalId)` [TENANT_ADMIN, MODULE_MANAGER]\n\n---\n\n## DOMAIN 2: DATA CHANNELS\n\n### Entity\n\n**SensorDataChannel** (`sensor_data_channels` table)\n- Fields: `id`, `sensorId` (FK->Sensor), `tenantId`, `channelKey`, `displayLabel`, `description`, `dataType` (enum), `unit`, `unitSymbol`, `physicalMin`, `physicalMax`, `operationalMin`, `operationalMax`, `dataPath`, `minValue`, `maxValue`, `calibrationEnabled`, `calibrationMultiplier` (default 1.0), `calibrationOffset` (default 0.0), `lastCalibratedAt`, `nextCalibrationDue`, `calibrationPolynomial` (jsonb), `protocolConfig` (jsonb), `alertThresholds` (jsonb), `displaySettings` (jsonb), `discoveredAt`, `discoverySource` (enum), `sampleValue`, `isEnabled`, `displayOrder`, `createdAt`, `updatedAt`\n- Unique constraint: `[sensorId, channelKey]`\n- Helper methods: `applyCalibration(rawValue)`, `isWithinRange(value)`, `isWithinPhysicalBounds(value)`, `isWithinOperationalBounds(value)`, `validateValue(value)`, `getAlertLevel(value)` -> 'normal'|'warning'|'critical'\n\n**Enums:**\n- `ChannelDataType`: NUMBER, BOOLEAN, STRING, ENUM\n- `DiscoverySource`: AUTO, MANUAL, TEMPLATE\n\n### GraphQL API\n\n**Queries:**\n- `allDataChannels()` -> [DataChannelType]\n- `dataChannelsBySensor(sensorId)` -> [DataChannelType]\n- `enabledChannelsBySensor(sensorId)` -> [DataChannelType]\n- `dataChannel(channelId)` -> DataChannelType\n\n**Mutations:**\n- `discoverDataChannels(input: DiscoverChannelsInput)` [TENANT_ADMIN, MODULE_MANAGER]\n- `createDataChannel(sensorId, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `updateDataChannel(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `bulkUpdateDataChannels(input)` [TENANT_ADMIN, MODULE_MANAGER] (max 100 items, atomic transaction)\n- `deleteDataChannel(channelId)` [TENANT_ADMIN]\n- `saveDiscoveredChannels(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `reorderDataChannels(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deleteAllChannelsForSensor(sensorId)` [TENANT_ADMIN]\n\n**Business Rules:**\n- Calibration: polynomial takes precedence over linear (multiplier+offset)\n- Alert levels: critical checked first, then warning\n- Physical bounds violation -> invalid; operational bounds violation -> valid but flagged\n\n---\n\n## DOMAIN 3: TIME-SERIES DATA (READINGS / METRICS)\n\n### Entities\n\n**SensorReading** (`sensor_readings` table - legacy wide format)\n- Fields: `id` (uuid PK), `sensorId`, `tenantId`, `timestamp` (timestamptz), `readings` (jsonb: {temperature?, ph?, dissolvedOxygen?, salinity?, ammonia?, nitrite?, nitrate?, turbidity?, waterLevel?}), `pondId`, `farmId`, `quality` (decimal 0-100), `source` (mqtt|http|batch), `createdAt`\n- Indexes: `[sensorId, timestamp]`, `[tenantId, timestamp]`, `[pondId, timestamp]`\n\n**SensorMetric** (`sensor_metrics` table - narrow EAV format, TimescaleDB hypertable)\n- Composite PK: `[time, sensorId, channelId]`\n- Fields: `tenantId`, `siteId`, `departmentId`, `systemId`, `equipmentId`, `tankId`, `pondId`, `farmId`, `rawValue` (double precision), `value` (calibrated double precision), `qualityCode` (OPC-UA: 0-63 bad, 64-127 uncertain, 192-255 good), `qualityBits` (bitmask: interpolated, extrapolated, manual, calibrating, rate-exceeded, clamped), `sourceProtocol`, `sourceTimestamp`, `ingestionLatencyMs`, `batchId`\n- NOT registered in TypeORM forFeature() - managed via migrations only\n- Relationships: lazy ManyToOne Sensor, lazy ManyToOne SensorDataChannel\n\n**AggregatedMetric** (GraphQL-only type from continuous aggregates)\n- Fields: `bucket`, `sensorId`, `channelId`, `avgValue`, `minValue`, `maxValue`, `stddevValue`, `firstValue`, `lastValue`, `sampleCount`, `goodCount`, `qualityPct`\n\n### TimescaleDB Features\n\n**HypertableService:**\n- `getChunkCount(hypertable)` -> number\n- `getSize(hypertable)` -> {totalBytes, compressedBytes, uncompressedBytes}\n- `getChunkInterval(hypertable)` -> string\n\n**ContinuousAggregateService:**\n- Known aggregates: `metrics_1min`, `metrics_1hour`, `metrics_1day`\n- `getRefreshStatus()` -> [{viewName, lastRefresh, behindBy}]\n- `refresh(viewName, startTime, endTime)` - manual refresh\n\n**RetentionPolicyService** - manages automatic data expiry policies\n\n### Aggregation Interval Enum (for `aggregatedReadings` query)\n- Auto-selection based on time range:\n  - 1h -> 1min (60 points)\n  - 6h -> 5min (72 points)\n  - 24h -> 15min (96 points)\n  - 3d -> 1h (72 points)\n  - 7d -> 4h (42 points)\n  - 30d -> 1d (30 points)\n\n### Quality Codes (OPC-UA aligned)\n- GOOD: 192, GOOD_LOCAL_OVERRIDE: 193\n- UNCERTAIN: 64, UNCERTAIN_LAST_USABLE: 65, UNCERTAIN_SENSOR_NOT_ACCURATE: 66, UNCERTAIN_EU_EXCEEDED: 67, UNCERTAIN_SUBNORMAL: 68\n- BAD: 0, BAD_CONFIG_ERROR: 1, BAD_NOT_CONNECTED: 2, BAD_DEVICE_FAILURE: 3, BAD_SENSOR_FAILURE: 4, BAD_COMM_FAILURE: 5, BAD_OUT_OF_SERVICE: 6, BAD_WAITING_INITIAL: 7\n\n---\n\n## DOMAIN 4: MQTT INGESTION\n\n### Topic Subscriptions\n- `sensors/#` - All sensor data\n- `aquaculture/+/sensors/#` - Tenant-specific sensors\n- `+/+/+/temperature-array` - Array sensor pattern\n- `tenants/+/devices/+/telemetry` - Edge device telemetry (CPU/RAM/Disk/Temp)\n- `tenants/+/devices/+/status` - Device online/offline\n- `tenants/+/devices/+/response` - Command response (legacy)\n- `tenants/+/devices/+/responses` - Command response (v2.0+)\n- `tenants/+/devices/+/io_data` - I/O tag live values\n- `tenants/+/devices/+/alarms` - I/O alarm events\n- `tenants/+/devices/+/capabilities` - Boot-time hardware capabilities\n- `tenants/+/devices/+/lora_events` - LoRaWAN events\n- Legacy (opt-in): `edge/+/heartbeat`, `edge/+/birth`, `edge/+/death`, `edge/+/response`, `edge/+/responses`\n\n### Ingestion Pipeline\n1. MQTT message received\n2. Topic parsed -> tenantId + sensorId extracted\n3. Sensor lookup (with negative caching for unknown topics)\n4. Data validation\n5. Calibration applied (if channel has calibration enabled)\n6. Written to `sensor_metrics` (narrow) and `sensor_readings` (legacy wide)\n7. NATS event published: `SensorReading`\n\n### Services\n- **MqttListenerService** - subscribes, routes messages, handles heartbeats/births/deaths\n- **BatchProcessorService** - batches writes for performance\n- **DataProcessorService** - main processing pipeline\n- **ValidationService** - range/type validation\n- **SensorTopicCacheService** - in-memory cache of sensor-to-topic mappings\n\n---\n\n## DOMAIN 5: CALIBRATION\n\n### Entity\n\n**Calibration entity** (`calibration/calibration.entity.ts` - empty file, calibration data stored inline in Sensor and SensorDataChannel)\n\n### Calibration Logic (in SensorDataChannel)\n- **Linear**: `value = rawValue * calibrationMultiplier + calibrationOffset`\n- **Polynomial**: `value = sum(coefficient[i] * rawValue^i)` (takes precedence)\n- **CalibrationService** (`sensor/services/calibration.service.ts`) - applies channel-level calibration during ingestion\n\n### DriftDetectionService\n- Detects calibration drift over time\n- Located at `calibration/drift-detection.service.ts`\n\n### GraphQL (via Sensor entity)\n- Calibration fields on Sensor: `calibrationData`, `calibrationEnabled`, `calibrationMultiplier`, `calibrationOffset`, `lastCalibratedAt`\n- Calibration fields on SensorDataChannel: `calibrationEnabled`, `calibrationMultiplier`, `calibrationOffset`, `lastCalibratedAt`, `nextCalibrationDue`, `calibrationPolynomial`\n\n---\n\n## DOMAIN 6: DATA QUALITY & CLEANING\n\n### Services\n- **OutlierDetectionService** (`cleaning/outlier-detection.service.ts`) - IQR and z-score methods\n- **InterpolationService** (`cleaning/interpolation.service.ts`) - fills gaps in time series\n- **DataCleanerService** (`cleaning/data-cleaner.service.ts`) - orchestrates pipeline\n- **DataQualityService** (`sensor/services/data-quality.service.ts`) - validates readings for NaN, Infinity, range\n\n### Stream Processing\n- **AnomalyDetectorService** (`stream-processing/anomaly-detector.service.ts`) - real-time anomaly detection\n- **RealTimeAnalyzerService** (`stream-processing/real-time-analyzer.service.ts`) - streaming analysis\n- **KafkaStreamsService** (`stream-processing/kafka-streams.service.ts`) - Kafka integration\n\n### Aggregation\n- **StatisticalAggregatorService** - min, max, avg, stddev\n- **TimeBucketService** - wraps TimescaleDB `time_bucket()`\n- **RollupService** - downsamples to lower resolution\n\n---\n\n## DOMAIN 7: VFD (VARIABLE FREQUENCY DRIVES)\n\n### Entities\n\n**VfdDevice** (`vfd_devices` table)\n- Fields: `id`, `name`, `brand` (enum), `model`, `serialNumber`, `protocol` (enum), `protocolConfiguration` (jsonb - union type), `connectionStatus` (jsonb), `status` (enum), `tenantId`, `farmId`, `tankId`, `location`, `description`, `metadata`, `customRegisterMappings` (jsonb), `pollIntervalMs` (default 1000), `isPollingEnabled`, `createdAt`, `updatedAt`, `createdBy`, `updatedBy`\n- Decorated with `@Auditable()`\n\n**VfdReading** (`vfd_readings` table)\n- Fields: `id`, `vfdDeviceId` (FK->VfdDevice CASCADE), `tenantId`, `parameters` (jsonb: outputFrequency, motorSpeed, motorCurrent, motorVoltage, dcBusVoltage, outputPower, motorTorque, powerFactor, energyConsumption, runningHours, powerOnHours, startCount, driveTemperature, motorThermal, controlCardTemperature, ambientTemperature, statusWord, faultCode, warningWord, alarmWord, speedReference, frequencyReference), `statusBits` (jsonb), `rawValues` (jsonb), `latencyMs`, `isValid`, `errorMessage`, `timestamp`, `createdAt`\n\n**VfdRegisterMapping** (`vfd_register_mappings` table)\n- Fields: `id`, `brand`, `modelSeries`, `parameterName`, `displayName`, `description`, `category` (enum), `registerAddress`, `registerCount`, `functionCode`, `dataType` (enum), `scalingFactor`, `offset`, `unit`, `byteOrder` (enum), `wordOrder` (enum), `isBitField`, `bitDefinitions` (jsonb), `isReadable`, `isWritable`, `recommendedPollIntervalMs`, `displayOrder`, `isActive`, `isCritical`, `minValue`, `maxValue`, `metadata`, `createdAt`, `updatedAt`\n- Unique: `[brand, modelSeries, parameterName]`\n\n**Enums:**\n- `VfdBrand`: DANFOSS, ABB, SIEMENS, SCHNEIDER, YASKAWA, DELTA, MITSUBISHI, ROCKWELL\n- `VfdProtocol`: MODBUS_RTU, MODBUS_TCP, PROFIBUS_DP, PROFINET, ETHERNET_IP, CANOPEN, BACNET_IP, BACNET_MSTP\n- `VfdParameterCategory`: STATUS, MOTOR, ENERGY, THERMAL, FAULT, CONTROL\n- `VfdDeviceStatus`: DRAFT, PENDING_TEST, TESTING, TEST_FAILED, ACTIVE, SUSPENDED, OFFLINE\n- `VfdCommandType`: START, STOP, REVERSE, SET_FREQUENCY, SET_SPEED, FAULT_RESET, QUICK_STOP, EMERGENCY_STOP, JOG_FORWARD, JOG_REVERSE, COAST_STOP\n- `VfdDataType`: UINT16, INT16, UINT32, INT32, FLOAT32, CONTROL_WORD, STATUS_WORD\n- `ByteOrder`: BIG, LITTLE\n\n### Brand Configs\n- 8 brand-specific register mapping configs: ABB, Danfoss, Delta, Mitsubishi, Rockwell, Schneider, Siemens, Yaskawa\n- Each defines CiA402/PROFIdrive control word commands\n\n### VFD Adapters\n- `BaseVfdAdapter` (abstract)\n- Protocol-specific: `VfdModbusTcpAdapter`, `VfdModbusRtuAdapter`, `VfdBacnetAdapter`, `VfdCanopenAdapter`, `VfdEthernetIpAdapter`, `VfdProfibusDpAdapter`, `VfdProfinetAdapter`\n\n### GraphQL API\n\n**Queries:**\n- `vfdDevice(id)` -> VfdDevice\n- `vfdDevices(filter?, pagination?)` -> PaginatedVfdDeviceListDto\n- `vfdStats()` -> VfdStatsDto\n- `vfdDevicesByFarm(farmId)` -> [VfdDevice]\n- `vfdDevicesByTank(tankId)` -> [VfdDevice]\n- `vfdDeviceCountByStatus()` -> String (JSON)\n- `vfdLatestReading(vfdDeviceId)` -> VfdReading\n- `vfdReadings(vfdDeviceId, from?, to?, limit?)` -> [VfdReading]\n- `vfdReadingStats(vfdDeviceId, period?, from?, to?)` -> VfdReadingStatsByPeriodDto\n- `vfdBrands()` -> JSON\n- `vfdProtocols()` -> JSON\n- `vfdProtocolSchema(protocol)` -> JSON\n- `vfdProtocolDefaultConfig(protocol)` -> JSON\n- `vfdRegisterMappings(brand, modelSeries)` -> [VfdRegisterMapping]\n- `vfdRegisterMappingsByCategory(brand, category)` -> [VfdRegisterMapping]\n- `vfdBrandCommands(brand)` -> JSON\n- `validateVfdConfig(protocol, configuration)` -> VfdValidationResult\n\n**Mutations:**\n- `registerVfdDevice(input)` [TENANT_ADMIN, MODULE_MANAGER] -> VfdRegistrationResultDto (optionally tests connection)\n- `updateVfdDevice(id, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deleteVfdDevice(id)` [TENANT_ADMIN]\n- `testVfdConnection(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `activateVfdDevice(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deactivateVfdDevice(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `sendVfdCommand(vfdDeviceId, command)` [TENANT_ADMIN, MODULE_MANAGER]\n- `startVfd(vfdDeviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `stopVfd(vfdDeviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `setVfdFrequency(vfdDeviceId, frequencyHz)` [TENANT_ADMIN, MODULE_MANAGER]\n- `setVfdSpeed(vfdDeviceId, speedPercent)` [TENANT_ADMIN, MODULE_MANAGER]\n- `resetVfdFault(vfdDeviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `emergencyStopVfd(vfdDeviceId)` [ALL AUTHENTICATED - safety exception]\n- `readVfdParameters(vfdDeviceId, parameters?)` -> VfdReadResultDto\n- `readVfdCriticalParameters(vfdDeviceId)` -> VfdReadResultDto\n- `deleteOldVfdReadings(olderThan)` -> Int\n\n**ResolveField:** `latestReading` on VfdDevice\n\n---\n\n## DOMAIN 8: AUTOMATION (IEC 61131-3)\n\n### Entities\n\n**AutomationProgram** (`automation_programs` table)\n- Fields: `id`, `tenantId`, `deviceId`, `processTemplateId`, `programCode` (unique per tenant), `programName`, `description`, `programType` (enum), `category`, `sfcDefinition` (jsonb), `structuredTextCode`, `transpiledJs`, `executionMode` (enum), `scanCycleMs`, `priority` (1-10), `triggerConfig` (jsonb), `version`, `status` (enum), `deployedVersion`, `deployedAt`, `deployedBy`, `approvedAt`, `approvedBy`, `isLocked`, `lockedBy`, `lockedAt`, `deployTarget` (enum), `targetPlcAddress`, `targetPlcPort`, `targetPlcModel`, `targetPlcProtocol`, `tags` (jsonb), `metadata`, `createdAt`, `updatedAt`, `createdBy`\n- Decorated with `@Auditable()`\n\n**ProgramStep** (`program_steps` table)\n- Fields: `id`, `programId`, `stepCode` (unique per program), `stepName`, `stepType` (enum), `description`, `positionX`, `positionY`, `entryAction`, `exitAction`, `timeoutMs`, `onTimeout` (enum), `timeoutTargetStep`, `stepOrder`, `createdAt`, `updatedAt`\n\n**StepAction** (`step_actions` table)\n- Fields: `id`, `stepId`, `actionName`, `description`, `qualifier` (enum), `actionType` (enum), `actionCode`, `targetRef`, `params` (jsonb), `delayMs`, `durationMs`, `actionOrder`, `isActive`, `createdAt`, `updatedAt`\n\n**ProgramTransition** (`program_transitions` table)\n- Fields: `id`, `programId`, `transitionCode` (unique per program), `transitionName`, `description`, `fromStepId`, `toStepId`, `fromStepCode`, `toStepCode`, `conditionType` (enum), `conditionExpression`, `transpiledCondition`, `priority`, `controlPoints` (jsonb), `timeoutMs`, `eventType`, `isActive`, `createdAt`, `updatedAt`\n\n**ProgramVariable** (`program_variables` table)\n- Fields: `id`, `programId`, `varName` (unique per program), `displayName`, `description`, `dataType` (enum), `scope` (enum), `initialValue`, `ioConfigId` (FK->DeviceIoConfig), `ioTagName`, `equipmentNodeId`, `equipmentProperty`, `sensorChannelId`, `minValue`, `maxValue`, `engUnit`, `alarmHH`, `alarmH`, `alarmL`, `alarmLL`, `metadata`, `varOrder`, `createdAt`, `updatedAt`\n\n**DeploymentLog** (`deployment_logs` table)\n- Fields: `id`, `tenantId`, `programId`, `deviceId`, `commandId` (unique), `version`, `status` (enum), `edgeScript` (jsonb), `deployedBy`, `deployedAt`, `completedAt`, `edgeAckAt`, `errorMessage`, `updatedAt`\n\n**Enums:**\n- `ExecutionMode`: MANUAL, CONTINUOUS, SCHEDULED, TRIGGERED\n- `ProgramStatus`: DRAFT, PENDING_REVIEW, APPROVED, DEPLOYING, DEPLOYED, ARCHIVED\n- `ProgramType`: SFC, FBD, ST, LD\n- `DeployTarget`: RUST_ENGINE, CODESYS_PLC, PLC_SETPOINT\n- `StepType`: INITIAL, NORMAL, FINAL\n- `TimeoutBehavior`: ABORT, SKIP, ALARM, GOTO\n- `ConditionType`: EXPRESSION, TIMEOUT, ALWAYS, EVENT\n- `VariableDataType`: BOOL, INT, DINT, UINT, UDINT, REAL, LREAL, STRING, TIME, DATE, TOD, DT\n- `VariableScope`: LOCAL, INPUT, OUTPUT, INOUT, RETAIN, CONSTANT\n- `ActionQualifier`: N, R, S, L, D, P, P0, P1, SD, DS, SL\n- `ActionType`: SET_OUTPUT, CALL_FB, ASSIGN, LOG, ALARM, TIMER, CUSTOM_ST\n- `DeploymentStatus`: PENDING, DEPLOYING, SUCCESS, FAILED, ROLLED_BACK\n\n**Status Transitions (ProgramStatus):**\n- DRAFT -> PENDING_REVIEW (submitProgramForReview)\n- PENDING_REVIEW -> APPROVED (approveProgram)\n- PENDING_REVIEW -> DRAFT (rejectProgram)\n- APPROVED -> DEPLOYING (deployProgram)\n- DEPLOYING -> DEPLOYED (edge device ack)\n- DEPLOYED -> ARCHIVED (archiveProgram)\n- Any -> DRAFT (after rejection)\n\n### GraphQL API\n\n**Queries:**\n- `automationProgram(id)` -> AutomationProgram\n- `automationProgramByCode(code)` -> AutomationProgram\n- `automationPrograms(filter?, page?, limit?)` -> AutomationProgramConnection\n- `automationProgramsConnection(filter?, page?, limit?)` -> AutomationProgramConnection\n- `automationProgramStats()` -> ProgramStats\n- `programSteps(programId)` -> [ProgramStep]\n- `programTransitions(programId)` -> [ProgramTransition]\n- `programVariables(programId)` -> [ProgramVariable]\n- `stepActions(stepId)` -> [StepAction]\n- `deploymentHistory(deviceId?, page?, limit?)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deploymentLog(id)` [TENANT_ADMIN, MODULE_MANAGER]\n\n**Mutations:**\n- `createAutomationProgram(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `updateAutomationProgram(id, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `deleteAutomationProgram(id)` [TENANT_ADMIN]\n- `cloneAutomationProgram(id, newCode)` [TENANT_ADMIN, MODULE_MANAGER]\n- `addProgramStep(input)` / `updateProgramStep(id, input)` / `removeProgramStep(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `addStepAction(input)` / `updateStepAction(id, input)` / `removeStepAction(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `addProgramTransition(input)` / `updateProgramTransition(id, input)` / `removeProgramTransition(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `addProgramVariable(input)` / `updateProgramVariable(id, input)` / `removeProgramVariable(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `syncProgramVariables(input)` [TENANT_ADMIN, MODULE_MANAGER] - bulk sync from parsed ST code\n- `submitProgramForReview(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `approveProgram(id)` [TENANT_ADMIN]\n- `rejectProgram(id, reason)` [TENANT_ADMIN]\n- `lockProgram(id)` / `unlockProgram(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `archiveProgram(id)` [TENANT_ADMIN]\n- `deployProgram(input: DeployProgramInput)` [TENANT_ADMIN, MODULE_MANAGER]\n- `rollbackDeployedProgram(deviceId)` [TENANT_ADMIN]\n- `validateStructuredText(code)` [TENANT_ADMIN, MODULE_MANAGER] -> ValidationResult\n\n**ResolveFields:** `stepCount`, `transitionCount`, `variableCount` on AutomationProgram; `actionCount` on ProgramStep\n\n### ST Compiler\n- Full IEC 61131-3 Structured Text compiler: Lexer -> Parser -> Semantic Analyzer -> Type Checker\n- NATS request-reply handlers for IDE features: analyze, complete, hover, format, outline, definition, references\n- Worker thread pool (1-4 threads, 10s task timeout)\n- Security limits: 100KB max source, 50 depth, 5s parse timeout, 500K max tokens\n\n---\n\n## DOMAIN 9: PLC CONTROL (OPC UA)\n\n### Entities\n\n**PlcConnection** (`plc_connections` table)\n- Fields: `id`, `tenantId`, `siteId`, `tankId`, `name`, `description`, `endpointUrl`, `securityMode` (enum), `securityPolicy`, `authMode` (enum), `username`, `password` (encrypted), `clientCertificate` (encrypted), `clientPrivateKey` (encrypted), `serverCertificate`, `status` (enum), `lastConnectedAt`, `lastError`, `publishingIntervalMs`, `samplingIntervalMs`, `sessionTimeoutMs`, `connectTimeoutMs`, `requestTimeoutMs`, `autoReconnect`, `maxReconnectAttempts`, `reconnectDelayMs`, `maxReconnectDelayMs`, `keepAliveIntervalMs`, `failoverEndpointUrl`, `parametersNodeId`, `telemetryNodeId`, `alarmsNodeId`, `statusNodeId`, `isActive`, `createdAt`, `updatedAt`\n- Decorated with `@Auditable()`\n\n**FeedingParameter** (`feeding_parameters` table)\n- Fields: `id`, `tenantId`, `plcConnectionId` (FK->PlcConnection), `tankId`, `name`, `description`, `version`, `biomassKg`, `fcr`, `targetDailyFeedKg`, `schedule` (jsonb: [{time, feedType, amountKg, durationSeconds, blowerSpeedPercent, doserSpeedPercent}]), `thresholds` (jsonb: {oxygenMin, oxygenCritical, tempMax, tempCritical, phMin, phMax}), `vfdSettings` (jsonb: {blowerMinSpeed, blowerMaxSpeed, doserMinSpeed, doserMaxSpeed}), `status` (enum), `sentAt`, `acknowledgedAt`, `activatedAt`, `errorMessage`, `checksum`, `createdAt`, `updatedAt`, `createdBy`\n- Decorated with `@Auditable()`\n\n**PlcAlarm** (`plc_alarms` table)\n- Fields: `id`, `tenantId`, `plcConnectionId`, `tankId`, `alarmCode`, `severity` (enum), `source` (enum), `message`, `value`, `threshold`, `action`, `timestamp`, `acknowledged`, `acknowledgedAt`, `acknowledgedBy`, `clearedAt`, `notes`, `approvalLevel`, `requiredApprovalLevel`, `approvalChain` (jsonb), `escalatedAt`, `autoEscalateAfterMs`, `slaDeadline`, `slaBreached`, `createdAt`\n\n**PlcTelemetry** (`plc_telemetry` table)\n- Fields: `id`, `tenantId`, `plcConnectionId`, `tankId`, `timestamp`, `sensors` (jsonb), `actuators` (jsonb), `feeding` (jsonb), `plcStatus` (jsonb), `activeParameterId`, `createdAt`\n\n**Enums:**\n- `PlcConnectionStatus`: ONLINE, OFFLINE, CONNECTING, ERROR\n- `PlcSecurityMode`: NONE, SIGN, SIGN_AND_ENCRYPT\n- `PlcAuthMode`: ANONYMOUS, USERNAME, CERTIFICATE\n- `ParameterStatus`: DRAFT, PENDING, SENT, ACKNOWLEDGED, ACTIVE, SUPERSEDED, ERROR\n- `AlarmSeverity`: INFO, WARNING, CRITICAL, EMERGENCY\n- `AlarmSource`: OXYGEN_SENSOR, TEMPERATURE_SENSOR, PH_SENSOR, FLOW_SENSOR, BLOWER_VFD, DOSER_VFD, FEEDING_SYSTEM, PLC_SYSTEM, COMMUNICATION\n\n### GraphQL API\n\n**Queries:**\n- `plcConnection(id)`, `plcConnections(filter?, pagination?)`, `plcConnectionsBySite(siteId)`, `plcConnectionCountByStatus()`, `onlinePlcConnections()`\n- `discoverOpcUaEndpoints(endpointUrl)` [TENANT_ADMIN, MODULE_MANAGER]\n- `browseOpcUaNodes(plcConnectionId, parentNodeId?)` [TENANT_ADMIN, MODULE_MANAGER]\n- `readOpcUaHistoricalData(plcConnectionId, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- Feeding: `feedingParameters`, `feedingParameter`, `feedingParametersByPlc`, `activeParameter`\n- Alarms: `plcAlarms`, `plcAlarm`, `unacknowledgedAlarms`, `plcAlarmStats`, `alarmCountBySeverity`, `alarmCountBySource`\n- Telemetry: `plcTelemetryData`, `latestPlcTelemetry`, `plcTelemetryStats`, `feedingStats`, `actuatorUsageStats`, `latestTelemetrySummary`\n\n**Mutations:**\n- `createPlcConnection(input)` / `updatePlcConnection(id, input)` / `deletePlcConnection(id)` / `testPlcConnection(id)` / `activatePlcConnection(id)` / `deactivatePlcConnection(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `callOpcUaMethod(plcConnectionId, input)` [TENANT_ADMIN]\n- `writeOpcUaNode(plcConnectionId, input)` [TENANT_ADMIN]\n- Feeding: `createFeedingParameter(input)` / `updateFeedingParameter(id, input)` / `deleteFeedingParameter(id)` / `sendParameterToPlc(id)` / `activateFeedingParameter(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- Alarms: `acknowledgeAlarm(input)` / `bulkAcknowledgeAlarms(input)` [various roles]\n\n**Business Rules (Alarms):**\n- Enterprise approval chain: `approvalLevel` must reach `requiredApprovalLevel`\n- SLA tracking: `slaDeadline`, `slaBreached` for acknowledgement SLAs\n- Auto-escalation: `autoEscalateAfterMs` triggers escalation to higher level\n\n---\n\n## DOMAIN 10: EDGE DEVICE MANAGEMENT\n\n### Entities\n\n**EdgeDevice** (`edge_devices` table)\n- Fields: `id`, `tenantId`, `siteId`, `deviceCode` (unique), `deviceName`, `deviceModel` (enum), `serialNumber`, `description`, `lifecycleState` (enum), `commissionedAt`, `commissionedBy`, `mqttClientId` (unique), `certificateThumbprint`, `certificateExpiresAt`, `securityLevel` (IEC 62443), `provisioningToken`, `tokenExpiresAt`, `tokenUsedAt`, `mqttPasswordHash`, `fingerprint` (jsonb: cpuSerial, macAddresses, machineId, hostname), `agentVersion`, `lastSeenAt`, `isOnline`, `connectionQuality`, `ipAddress`, `firmwareVersion`, `firmwareUpdatedAt`, `targetFirmwareVersion`, `cpuUsage`, `memoryUsage`, `storageUsage`, `temperatureCelsius`, `uptimeSeconds`, `timezone`, `scanRateMs`, `config` (jsonb), `capabilities` (jsonb: modbus, gpio, analogIo, serial, canBus, profinet, lorawan), `tags` (jsonb), `createdAt`, `updatedAt`, `createdBy`\n- Decorated with `@Auditable()`\n\n**DeviceIoConfig** (`device_io_configs` table)\n- Fields: `id`, `deviceId` (FK->EdgeDevice CASCADE), `tagName` (unique per device), `description`, `ioType` (enum), `dataType` (enum), `moduleAddress`, `channel`, `rawMin`, `rawMax`, `engMin`, `engMax`, `engUnit`, `modbusFunction`, `modbusSlaveId`, `modbusRegister`, `gpioPin`, `gpioMode`, `busType`, `i2cBus`, `i2cAddress`, `spiBus`, `spiCs`, `uartPort`, `driverType`, `invertValue`, `alarmHH`, `alarmH`, `alarmL`, `alarmLL`, `deadband`, `isActive`, `createdAt`, `updatedAt`\n\n**DeviceEvent** (`device_events` table)\n- Fields: `id`, `tenantId`, `deviceId`, `eventType` (enum), `severity` (enum), `message`, `metadata` (jsonb), `createdAt`\n\n**LoRaDevice** (`lora_devices` table)\n- Fields: `id`, `edgeDeviceId` (FK->EdgeDevice CASCADE), `devEui` (unique), `appEui`, `appKey` (AES-256-GCM encrypted), `devAddr`, `activationMode` (enum), `deviceClass` (enum), `name`, `tagPrefix`, `codec`, `adrEnabled`, `fPort`, `lastSeenAt`, `lastRssi`, `lastSnr`, `frameCountUp`, `isJoined`, `joinedAt`, `tenantId`, `createdAt`, `updatedAt`\n\n**TenantProvisioningKey** (`tenant_provisioning_keys` table)\n- Fields: `id`, `tenantId`, `keyToken` (unique), `name`, `isActive`, `maxDevices`, `usedCount`, `autoApprove`, `defaultSiteId`, `expiresAt`, `createdBy`, `createdAt`, `updatedAt`\n\n**Enums:**\n- `DeviceLifecycleState`: REGISTERED, PROVISIONING, PENDING_APPROVAL, ACTIVE, OFFLINE, MAINTENANCE, ERROR, REVOKED, DECOMMISSIONED\n- `DeviceModel`: REVOLUTION_PI_CONNECT_4, REVOLUTION_PI_COMPACT, RASPBERRY_PI_4, RASPBERRY_PI_5, RASPBERRY_PI_4_LORA, RASPBERRY_PI_5_LORA, INDUSTRIAL_PC, CUSTOM\n- `SecurityLevel`: SL1(1), SL2(2), SL3(3), SL4(4) - IEC 62443\n- `IoType`: DI, DO, AI, AO\n- `IoDataType`: BOOL, INT16, INT32, UINT16, UINT32, FLOAT32, FLOAT64\n- `DeviceEventType`: SELF_REGISTERED, APPROVED, CONNECTED, DISCONNECTED, CONFIG_PUSHED, CONFIG_ACK, DEPLOYMENT, REBOOT, ERROR, ALARM, HEARTBEAT_LOST, DECOMMISSIONED\n- `DeviceEventSeverity`: INFO, WARNING, ERROR, CRITICAL\n- `LoRaActivationMode`: OTAA, ABP\n- `LoRaDeviceClass`: A, B, C\n\n### GraphQL API\n\n**Queries:**\n- `edgeDevice(id)`, `edgeDevices(siteId?, lifecycleState?, isOnline?, search?, page?, limit?)` -> EdgeDeviceConnection\n- `edgeDeviceStats()` -> EdgeDeviceStats\n- `deviceInstallCommands(deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `tenantProvisioningKeys()` [TENANT_ADMIN]\n- `deviceEvents(deviceId?, eventType?, page?, limit?)` -> DeviceEventConnection\n- `loraDevices(edgeDeviceId)` -> [LoRaDeviceType]\n- `availableFirmwareVersions()` -> [FirmwareVersionInfo]\n\n**Mutations:**\n- `registerEdgeDevice(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `updateEdgeDevice(id, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `approveEdgeDevice(id)` [TENANT_ADMIN]\n- `setDeviceMaintenanceMode(id, enabled)` [TENANT_ADMIN, MODULE_MANAGER]\n- `decommissionEdgeDevice(id, reason)` [TENANT_ADMIN]\n- `pingEdgeDevice(id)` -> PingResult\n- `rebootEdgeDevice(id, reason?)` [TENANT_ADMIN]\n- `createProvisionedDevice(input)` [TENANT_ADMIN, MODULE_MANAGER] -> ProvisionedDeviceResponse\n- `regenerateDeviceToken(deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `resetDeviceForReprovisioning(deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `createTenantProvisioningKey(input)` [TENANT_ADMIN]\n- `revokeTenantProvisioningKey(keyId)` [TENANT_ADMIN]\n- I/O: `addDeviceIoConfig(deviceId, input)`, `updateDeviceIoConfig(id, deviceId, input)`, `removeDeviceIoConfig(id, deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `pushIoConfigToDevice(deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `setDigitalOutput(input)` [TENANT_ADMIN, MODULE_MANAGER]\n- `scanEdgeDeviceHardware(deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `bulkAddDeviceIoConfigs(deviceId, inputs)` [TENANT_ADMIN, MODULE_MANAGER]\n- LoRa: `addLoRaDevice(edgeDeviceId, input)`, `removeLoRaDevice(edgeDeviceId, loraDeviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- `sendLoRaDownlink(edgeDeviceId, loraDeviceId, input)` [TENANT_ADMIN, MODULE_MANAGER]\n- OTA: `updateEdgeDeviceFirmware(id, targetVersion?)`, `bulkUpdateEdgeDeviceFirmware(deviceIds, targetVersion?)` [TENANT_ADMIN, MODULE_MANAGER]\n\n**ResolveFields:** `ioConfig`, `sensorCount`, `programCount`, `activeAlarmCount` on EdgeDevice\n\n### REST Endpoints (non-GraphQL)\n- `mqtt-auth.controller.ts` - MQTT broker authentication callbacks\n- `provisioning.controller.ts` - Zero-touch provisioning REST API\n\n---\n\n## DOMAIN 11: DEVICE GROUPS\n\n### Entities\n\n**DeviceGroup** (`device_groups` table)\n- Fields: `id`, `tenantId`, `name`, `description`, `type` (enum), `parentGroupId` (FK self-ref), `metadata`, `createdAt`, `updatedAt`\n- Self-referential parent-child hierarchy\n\n**DeviceGroupMember** (`device_group_members` table)\n- Fields: `id`, `groupId` (FK->DeviceGroup CASCADE), `deviceType` (enum), `deviceId`, `addedAt`\n- Unique: `[groupId, deviceType, deviceId]`\n\n**Enums:**\n- `DeviceGroupType`: CUSTOM, SITE, DEPARTMENT, SYSTEM, EQUIPMENT_TYPE\n- `DeviceMemberType`: SENSOR, EDGE_DEVICE, VFD_DEVICE, PLC_CONNECTION\n\n### GraphQL API\n- `deviceGroups()`, `deviceGroup(id)` queries\n- `createDeviceGroup(input)`, `updateDeviceGroup(id, input)`, `deleteDeviceGroup(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- `addDevicesToGroup(groupId, members)`, `removeDevicesFromGroup(groupId, memberIds)` [TENANT_ADMIN, MODULE_MANAGER]\n- `batchUpdateSensors(sensorIds, input)`, `batchActivateSensors(sensorIds)`, `batchDeactivateSensors(sensorIds)` [TENANT_ADMIN, MODULE_MANAGER]\n\n---\n\n## DOMAIN 12: PROCESS DIAGRAMS & SCADA\n\n### Entities\n\n**Process** (`processes` table)\n- Fields: `id`, `name`, `code`, `description`, `status` (enum), `nodes` (jsonb - ReactFlow nodes with equipment mappings), `edges` (jsonb), `tenantId`, `siteId`, `departmentId`, `metadata`, `isTemplate`, `templateName`, `createdAt`, `updatedAt`, `createdBy`, `updatedBy`\n\n**UnifiedTag** (`unified_tags` table)\n- Fields: `id`, `tenantId`, `fqn` (unique per tenant), `localName`, `displayName`, `description`, `ioType` (enum), `dataType` (enum), `direction` (enum), `engUnit`, `engMin`, `engMax`, `alarmHH`, `alarmH`, `alarmL`, `alarmLL`, `deadband`, `source` (jsonb), `hierarchy` (jsonb), `createdAt`, `updatedAt`\n\n**ScadaPackage** (`scada_packages` table)\n- Fields: `id`, `tenantId`, `name`, `description`, `version`, `processId`, `packageData` (jsonb), `status` (enum), `createdBy`, `updatedBy`, `createdAt`, `updatedAt`\n\n**ScadaDeployLog** (`scada_deploy_logs` table)\n- Fields: `id`, `tenantId`, `packageId`, `deviceId`, `commandId` (unique per tenant), `version`, `status` (enum), `sentAt`, `receivedAt`, `deployedAt`, `verifiedAt`, `healthCheckResults` (jsonb), `errorMessage`, `rolledBackTo`, `deployedBy`, `createdAt`, `updatedAt`\n\n**Enums:**\n- `ProcessStatus`: DRAFT, ACTIVE, INACTIVE, ARCHIVED\n- `TagDirection`: INPUT, OUTPUT, BIDIRECTIONAL\n- `TagIoType`: DI, DO, AI, AO\n- `TagDataType`: BOOL, INT16, INT32, UINT16, UINT32, FLOAT32, FLOAT64\n- `ScadaPackageStatus`: DRAFT, PUBLISHED, ARCHIVED\n- `ScadaDeployStatus`: PENDING, SENT, RECEIVED, DEPLOYING, VERIFYING, SUCCESS, FAILED, ROLLED_BACK\n\n### GraphQL API\n\n**Queries:**\n- `process(id)`, `processes(filter?, pagination?)`, `activeProcesses(siteId?)`, `processTemplates()`\n- `unifiedTag(id)`, `unifiedTags(filter?, pagination?)`, `searchTags(query, limit?)`\n- `scadaPackage(id)`, `scadaPackages(filter?, pagination?)`\n- `scadaDeployLogs(filter)`, `latestScadaDeployLog(deviceId)`\n\n**Mutations:**\n- Process: `createProcess(input)`, `updateProcess(input)`, `deleteProcess(id)`, `duplicateProcess(id, newName)`, `createProcessFromTemplate(templateId, name)`, `deployProcessToEdge(processId, deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- Tags: `createUnifiedTag(input)`, `updateUnifiedTag(input)`, `deleteUnifiedTag(id)`, `discoverTags(deviceId)`, `autoBindTags(processId, deviceId)` [TENANT_ADMIN, MODULE_MANAGER]\n- SCADA: `createScadaPackage(input)`, `updateScadaPackage(id, input)`, `deleteScadaPackage(id)` [TENANT_ADMIN, MODULE_MANAGER]\n- Deployment: `deployScadaPackageToEdge(packageId, deviceId)`, `deployScadaWithAutomation(input)` [TENANT_ADMIN, MODULE_MANAGER]\n\n---\n\n## DOMAIN 13: PROTOCOL ADAPTERS\n\n### Base Interface\n`BaseProtocolAdapter` (abstract) - defines connect/disconnect/read/write/subscribe\n\n### Adapter Registry (40+ adapters)\n\n**Industrial (20):** modbus-rtu, modbus-tcp, modbus-ascii, bacnet-ip, bacnet-mstp, canopen, cclink, devicenet, ethercat, ethernet-ip, knx-ip, mitsubishi-mc, profibus-dp, profinet, schneider-modicon, siemens-s7, allen-bradley-df1, allen-bradley-ethernet, omron-fins, opcua\n\n**IoT (6):** mqtt, amqp, coap, dds, http-rest, websocket\n\n**Serial (7):** rs232, rs485, i2c, one-wire, spi, tcp-socket, udp-socket\n\n**Wireless (6):** ble, esp-now, lorawan, thread-matter, zigbee, zwave\n\n### GraphQL API\n- `protocols(category?)`, `protocolSummaries()`, `protocolDetails(code)`, `protocolSchema(code)`, `protocolDefaults(code)`, `protocolCapabilities(code)`, `protocolCategoryStats()`, `protocolCodes()`\n- `validateProtocolConfig(input)`, `testProtocolConnection(input)`, `pingProtocol(protocolCode, config, count?)`, `applyProtocolDefaults(protocolCode, config)` [TENANT_ADMIN, MODULE_MANAGER]\n\n---\n\n## DOMAIN 14: DASHBOARD\n\n### Entity\n\n**DashboardLayout** (`dashboard_layouts` table)\n- Fields: `id`, `tenantId`, `userId`, `name`, `description`, `widgets` (jsonb), `processBackground` (jsonb), `gridConfig` (jsonb), `gridVersion`, `isDefault`, `isSystemDefault`, `createdAt`, `updatedAt`, `createdBy`\n\n### GraphQL API\n- `dashboardLayouts()`, `dashboardLayout(id)`, `myDefaultLayout()`, `systemDefaultLayout()`\n- `saveDashboardLayout(input)`, `saveSystemDefaultLayout(input)` [SUPER_ADMIN, TENANT_ADMIN], `setLayoutAsDefault(id)`, `deleteDashboardLayout(id)`\n\n---\n\n## DOMAIN 15: INFRASTRUCTURE\n\n### Audit System\n**AuditLog** (`sensor_audit_logs` table)\n- Fields: `id`, `tenantId`, `entityType`, `entityId`, `action` (INSERT|UPDATE|DELETE), `previousValue` (jsonb), `newValue` (jsonb), `changedFields` (jsonb), `changedBy`, `changedAt`\n- `@Auditable()` decorator on entities: Sensor, VfdDevice, AutomationProgram, EdgeDevice, PlcConnection, FeedingParameter\n- TypeORM subscriber (`audit.subscriber.ts`) automatically logs changes\n\n### Credential Vault\n- `CredentialVaultService` - AES-256-GCM encryption for sensitive fields\n- `EncryptedColumnTransformer` - TypeORM column transformer for transparent encrypt/decrypt\n- Used on: PlcConnection.password, PlcConnection.clientCertificate, PlcConnection.clientPrivateKey, LoRaDevice.appKey\n\n### Rate Limiting\n- `RateLimitGuard` (`guards/rate-limit.guard.ts`)\n\n### Health Check\n- `/health` endpoint via `HealthController`\n\n### Metrics\n- Prometheus metrics endpoint via `MetricsController`\n\n---\n\n## EVENTS PUBLISHED (NATS JetStream)\n\n1. **`SensorReading`** - Published on every ingested reading (consumed by alert-engine)\n2. **`events.AutomationProgramSaved.{tenantId}`** - When a program is saved\n3. **`events.AutomationProgramDeployed.{tenantId}`** - When a program is deployed\n4. **`events.AutomationTagsUpdated.{tenantId}`** - When tags are added/removed/updated\n5. **`events.AutomationFBDefinitionsChanged.{tenantId}`** - When function block definitions change\n6. **`SensorReadingIngested`** - For alert-engine rule evaluation\n7. **`AlertThresholdExceeded`** - When sensor reading crosses threshold\n8. **`SensorOffline`** - When sensor goes offline\n\n### NATS Request-Reply Subjects (ST Language Service)\n- `st.language.analyze` - Parse + semantic analysis\n- `st.language.complete` - IntelliSense completions\n- `st.language.hover` - Hover tooltip\n- `st.language.format` - Code formatting\n- `st.language.outline` - Document symbols\n- `st.language.definition` - Go-to-definition\n- `st.language.references` - Find references\n\n---\n\n## CROSS-ENTITY RELATIONSHIPS SUMMARY\n\n1. **Sensor** -> SensorProtocol (ManyToOne)\n2. **Sensor** -> SensorTypeDefinition (ManyToOne)\n3. **Sensor** -> Sensor (self-ref parent/child, ManyToOne/OneToMany)\n4. **SensorDataChannel** -> Sensor (ManyToOne CASCADE)\n5. **SensorMetric** -> Sensor (lazy ManyToOne), -> SensorDataChannel (lazy ManyToOne)\n6. **ChannelDetectionLog** -> Sensor (ManyToOne CASCADE)\n7. **VfdReading** -> VfdDevice (ManyToOne CASCADE)\n8. **DeviceIoConfig** -> EdgeDevice (ManyToOne CASCADE)\n9. **LoRaDevice** -> EdgeDevice (ManyToOne CASCADE)\n10. **DeviceGroupMember** -> DeviceGroup (ManyToOne CASCADE)\n11. **DeviceGroup** -> DeviceGroup (self-ref parent/child)\n12. **FeedingParameter** -> PlcConnection (ManyToOne)\n13. **ProgramVariable** -> DeviceIoConfig (logical FK via ioConfigId)\n14. **ProgramVariable** -> SensorDataChannel (logical FK via sensorChannelId)\n15. **AutomationProgram** -> EdgeDevice (logical FK via deviceId)\n16. **AutomationProgram** -> Process (logical FK via processTemplateId)\n17. **ScadaPackage** -> Process (logical FK via processId)\n18. **ScadaDeployLog** -> ScadaPackage (logical FK via packageId)\n19. **ScadaDeployLog** -> EdgeDevice (logical FK via deviceId)\n20. **DeploymentLog** -> AutomationProgram, EdgeDevice (logical FKs)\n\n---\n\n## MIGRATIONS (chronological)\n\n1. `1735900000000-CreateSensorMetrics` - sensor_metrics hypertable\n2. `1735900001000-CreateContinuousAggregates` - materialized views\n3. `1736200000000-CreateReadingsAggregates` - readings aggregate views\n4. `1736800000000-CreateEdgeDevicesTable` - edge_devices table\n5. `1740000000000-AddSensorMetricsCompositeIndex` - performance index\n6. `1740200000000-CreateDynamicSensorTypes` - sensor_type_definitions, industry_templates\n7. `1740300000000-CreateProcessesTable` - processes table\n8. `1740300001000-CreateAutomationTables` - automation_programs, program_steps, step_actions, program_transitions, program_variables\n9. `1741000000000-CreateScadaTables` - unified_tags, scada_packages, scada_deploy_logs\n10. `1741100000000-AddEnterprisePlcConnectionFields` - PLC connection enhancements\n11. `1741200000000-EnterprisePerformanceOptimizations` - indexes and optimizations\n\n---\n\nThis is the complete feature inventory. Key files referenced:\n\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/database/entities/` - All core entities\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/sensor/resolvers/sensor.resolver.ts` - Main sensor GraphQL\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/vfd/entities/vfd.enums.ts` - All VFD enums\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/automation/entities/automation-program.entity.ts` - Automation enums/statuses\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/edge-device/entities/edge-device.entity.ts` - Edge lifecycle states\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/automation/compiler/compiler.constants.ts` - NATS subjects/events\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/ingestion/mqtt-listener.service.ts` - MQTT topics\n- `/var/aqua-saas/.claude/worktrees/agent-a3730a5c/apps/sensor-service/src/infrastructure/audit/audit-log.entity.ts` - Audit system"}],"stop_reason":"end_turn","stop_sequence":null,"usage":{"input_tokens":1,"cache_creation_input_tokens":3791,"cache_read_input_tokens":185938,"output_tokens":16123,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":3791},"inference_geo":"","iterations":[],"speed":"standard"}},"requestId":"req_011CZJ3RXVphjjWLzb5Sx1gj","type":"assistant","uuid":"b493badf-755e-474c-bf27-3c5fd5d8cb26","timestamp":"2026-03-22T11:34:46.605Z","userType":"external","entrypoint":"cli","cwd":"/var/aqua-saas/.claude/worktrees/agent-a3730a5c","sessionId":"c6f37750-70d4-4754-9414-4edb2cbde512","version":"2.1.81","gitBranch":"worktree-agent-a3730a5c","slug":"golden-honking-manatee"}
