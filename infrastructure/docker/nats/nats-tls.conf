# =============================================================================
# NATS TLS Configuration (disabled / no-op)
#
# This is the default (development) TLS configuration — TLS is disabled.
# For production, replace this file with nats-tls-enabled.conf by mounting
# the enabled version at /etc/nats/nats-tls.conf in docker-compose.
#
# See nats-tls-enabled.conf for the production TLS block.
# =============================================================================
# TLS disabled — no tls{} block
