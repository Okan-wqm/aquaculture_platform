<!-- ARIA-CURRENT-STATE-NOTICE: Tam-okuma ajan raporu (2026-09-25, main @ 19efd07f);
docs/aria/CURRENT_STATE.md ve executable contracts'a tabidir. Ham kanıt kaydıdır, runtime
otoritesi değildir. -->

# B02 — Tam okuma raporu (26 dosya, `aria-kernel/aria_kernel/agent_resolver.py` … `budget.py`)

Repo `main @ 19efd07f`, canlı durum `origin/aria/state @ e6f462fb`. 26/26 dosya okundu (bkz. sondaki
"Okuma kapsamı"). Önceki 6-ajan raporundaki (K1–K16, §3) bulgular tekrarlanmadı; yalnız yeni
bulgular / düzeltmeler.

## Dosya envanteri

| Dosya                            | Satır | Ne yapar                                                                                         | Üretimde bağlı mı?                                                                                                                | Şema                           |
| -------------------------------- | ----- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| agent_resolver.py                | 64    | Ajan .md yolunu root→_maintenance→product-audit sırasıyla bulur                                  | Evet: `context_budget_gate.py:62`, `surface_manifest_validator.py:67`, `service_agent_targeting.py:40`                            | 22 (dolaylı)                   |
| agent_routing.py                 | 129   | Orchestrator routing tablosundan servis→ajan eşlemesi, sahipsiz servis tespiti                   | Evet: `impact_graph.py:292-296`, `capability_gap.py:328`                                                                          | 14 / 13                        |
| agent_runtime_profile.py         | 312   | Ajan model/effort SSoT, model-kademe yazma koruması, authored_by_model damgası                   | Evet: `ci_executor.py:1708,1751,1997,3759`, `worker_dispatch_hook.py:238`, `agent_genesis.py:846-868`                             | 5 / 18 (O4)                    |
| agent_satisfaction.py            | 216   | `addresses_pressure` frontmatter'lı ajanlarla pressure'ı `satisfied` yapar, ajan indeksini tutar | Evet: learning hook `learning.py:95` → `cycle.py:617`                                                                             | yok                            |
| agent_surface.py                 | 251   | Rol/durum kapalı sözlükleri, rol↔hedef eşlemesi                                                 | Evet (çok tüketici)                                                                                                               | 5                              |
| apply_engine.py                  | 1056  | CONVERGED planı staging (change_planned, baseline, makine onayı), apply gate, onay doğrulama     | Evet: `cycle_phases/implementer.py:241`, `implementation_delivery.py:765`, `pr_manager.py:290`; `plan_apply_worktree` yalnız test | 17                             |
| architecture.py                  | 464   | Mimari karar incelemesi / seçenek / 5-kanıt paketi / ADR taslağı                                 | Yalnız operatör CLI (`cli.py:4830-4870`)                                                                                          | 14 (yalnız kapsam tablosu)     |
| architecture_spine_gate.py       | 854   | 5 değişmez baseline/postcheck, 5 regresyonda HUMAN_REQUIRED                                      | Evet: `cycle.py:2812-2836`, `must_satisfy.py:192`, CLI                                                                            | 14                             |
| aria_watchdog.py                 | 862   | Stall/bridge-warning/5xx dedektörleri, dedup'lı finding emisyonu, daemon                         | Cycle fazı `cycle.py:1150,3170`; daemon yalnız CLI (`cli.py:4335`)                                                                | 7 / 20                         |
| artifact_safety.py               | 86    | Secret temizleme, 1 MB sanitize JSON yazımı                                                      | Evet: 6 çağıran (`verification_gate.py:337`, `ci_executor.py:815` …)                                                              | 22 (X3)                        |
| auto_action_gate.py              | 321   | Genesis materialize ack-token kapısı                                                             | Yalnız CLI materialize yolu (`cli.py:5615,5777`)                                                                                  | 5 / 15                         |
| auto_merge.py                    | 1270  | Auto-merge değerlendirme, üçlü kapı, gh adaptörü                                                 | Evet: merge_authority, auto_merge_runners, worker hook                                                                            | 7 / 17                         |
| auto_merge_runners.py            | 685   | Profil→runner seçimi, aday PR döngüsü; ölü V9 bölümü                                             | Evet: `cli.py:5850-5928`; V9 bölümü ölü                                                                                           | 1 / 7                          |
| autonomous_host_lease.py         | 467   | Yerel host kirası + "remote CAS" kirası                                                          | Evet: `autonomy_orchestrator.py:663`, `gateway/daemon.py:51`, `readiness_proofs.py:450`, workflow                                 | 15 (HOST)                      |
| autonomous_planner_dispatcher.py | 286   | Planner claim döngüsü (3 rol)                                                                    | Orkestratör drainer (`autonomy_orchestrator.py:1665`); daemon CLI                                                                 | 4                              |
| autonomous_worker_scheduler.py   | 300   | Worker claim döngüsü                                                                             | Orkestratör drainer (`:2548`); daemon CLI                                                                                         | 4                              |
| autonomy_evidence.py             | 4166  | Hedef-SHA'ya bağlı yetenek kanıt durumu + **state publish doğrulayıcısı**                        | Publish: `state_store.py:1132,1759,3590`; status yalnız CLI (`cli.py:6164`)                                                       | 6 / 23-24 (yetenek modeli yok) |
| autonomy_ladder.py               | 209   | Temiz cycle / burn-in → `observe_success` köprüsü                                                | Evet: `aria-auto-cycle.yml:867-881`, CLI, `burn_in_driver.py`                                                                     | 7                              |
| autonomy_orchestrator.py         | 2832  | Gece tam-zincir sürücü                                                                           | Evet: `aria-auto-cycle.yml:810` → `cli.py:5890-6030`                                                                              | 1 / 16                         |
| autonomy_state.py                | 373   | autonomy_state.jsonl reducer                                                                     | Evet (orkestratör, evidence)                                                                                                      | 1 / 6                          |
| autonomy_unlock.py               | 417   | Kabul olayları, merdiven kararı, zamanlanmış profil çözümü                                       | Evet: `aria-auto-cycle.yml:599-615`, merge_authority                                                                              | 7                              |
| batch_containment.py             | 92    | `guard_item` öğe-başı hata kapsama                                                               | Evet (21 modül)                                                                                                                   | 20 (not)                       |
| belief_escalation.py             | 276   | Takılı çelişki → HR; panel kararı → inanç düzeltmesi                                             | Evet: `cycle.py:1419`, `human_required_adjudication.py:934`                                                                       | 9 / 12                         |
| bridge_exceptions.py             | 32    | `BridgeContractViolation` tipi                                                                   | Evet: `cross_review_bridge.py:316,794`, `agent_invocations.py:4171`                                                               | yok                            |
| bridge_status_ledger.py          | 431   | Köprü durumu ledger'ı + replay                                                                   | Evet: orkestratör bridge drainer                                                                                                  | 20 (BS)                        |
| budget.py                        | 1003  | Fiyat tabloları, native deneme rezervasyonu, duvar saati, maliyet atıfı, eski bütçe sistemleri   | Kısmen: fiyat/deneme/duvar saati/atıf canlı; `reserve_cycle_budget` ölü                                                           | 15                             |

## Doküman tutarsızlıkları (yalnız YENİ)

| #   | Doküman iddiası                                                                                                                                                                             | Kod gerçeği                                                                                                                                                                                         | Kanıt                                                                                             | Sınıf                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------- |
| N1  | Yakınsama "≤5 tur (MAX_CROSS_REVIEW_ROUNDS)" (MIMARI Şema 0/3/16; NASIL §9.3)                                                                                                               | Nightly `autonomy run` `--max-rounds` geçmez → CLI varsayılanı **2**, challenger timeout **300 s**; 5 yalnız manuel `plan advance`                                                                  | `cli.py:2377,2384-2389`; `aria-auto-cycle.yml:810-816`; `plan_convergence.py:92`                  | TUTARSIZ                  |
| N2  | 5 DI kancası (Plan/V9/Memory/CostTelemetry/ProfileGate), "seçim profil SSoT'sundan" (MIMARI Şema 22)                                                                                        | `cost_telemetry_hook` ve `profile_gate` çözülür ama hiç çağrılmaz; CLI'nın verdiği üretim cost hook'u yok sayılır                                                                                   | `autonomy_orchestrator.py:945-946,1035-1040` (başka kullanım yok)                                 | EKSİK-KOD                 |
| N3  | `assert_within_budget` → `reserve_cycle_budget` (token=ledger_hash) → reconcile → cost-attribution (MIMARI Şema 15)                                                                         | `reserve_cycle_budget` üretim çağıranı yok; `ARIA_BUDGET_RESERVATION_TOKEN` hiç set edilmiyor → reconcile daima atlanır; orkestratörün `max_budget_usd_per_cycle/run` parametreleri yalnız loglanır | `budget.py:474-520`; `ci_executor.py:1652-1654`; `autonomy_orchestrator.py:955-960,1159-1160`     | EKSİK-KOD                 |
| N4  | HOST: `autonomous-host.cas.json` CAS kirası orkestratör çoklu-host düzeni (MIMARI Şema 15)                                                                                                  | Orkestratör CAS değil `autonomous-host.lock` kullanır; CAS kirası yalnız readiness-proof üreticisinde; "CAS" oku→yaz, kilitsiz                                                                      | `autonomy_orchestrator.py:663-670`; `readiness_proofs.py:450`; `autonomous_host_lease.py:274-301` | TUTARSIZ                  |
| N5  | `aria_watchdog` → merge dondurma (`WD -.donma.-> M2`, MIMARI Şema 7)                                                                                                                        | Dondurma HARİCİ watchdog'un GitHub incident issue'larını okur; aria_watchdog bulguları merge'e bağlı değil                                                                                          | `merge_authority.py:29,59`; `watchdog_freeze.py:1-40`                                             | TUTARSIZ                  |
| N6  | aria_watchdog "5xx oranı" bekçisi (MIMARI Şema 7)                                                                                                                                           | `observability_base_url: null` → her cycle `source_unconfigured`                                                                                                                                    | `data/genesis_policy_default.json:99-104`; `aria_watchdog.py:727-728`                             | EKSİK-KOD                 |
| N7  | Spine B4/B5 auth/harness "adaptör koşusu" okur (MIMARI Şema 14)                                                                                                                             | Sabit `workspace_root/aria-tools/runs.jsonl` okunur; CI'da koşular store-resident `ARIA_TOOLS_DIR`'a yazılır → iki değişmez yapısal `pending`                                                       | `architecture_spine_gate.py:275,321`; `restore-aria-state/action.yml:84-88,171`                   | TUTARSIZ                  |
| N8  | Otonom şerit: "BASELINE doğrulama ÖNCE → change_planned → makine onayı" (MIMARI Şema 17)                                                                                                    | Sıra: `emit_change_planned` → baseline → proposal → makine onayı → staged action                                                                                                                    | `apply_engine.py:743,763,776,803,809`                                                             | KAYMA                     |
| N9  | Operatör şeridi `approved_for_apply + insan onayı → worktree aç` (MIMARI Şema 17 A1)                                                                                                        | `plan_apply_worktree` yalnız testlerden çağrılıyor, CLI'da da yok                                                                                                                                   | grep: `tests/test_enterprise_*`, `test_autopr_foundation.py`                                      | EKSİK-KOD                 |
| N10 | Belief paneli: "panel oybirliği yalnız aşağı yönlü düzeltme yapabilir" (MIMARI Şema 12)                                                                                                     | Aşağı düzeltmeyi her resolve quorum (2-1 dahil) yapar; oybirliği satırı ground-truth ANCHOR yapar                                                                                                   | `belief_escalation.py:131-141,150-153`                                                            | KAYMA                     |
| N11 | "Sıfır critical_violation" merdiven şartı (MIMARI Şema 7 GATE)                                                                                                                              | `critical_violation` olayı yazan üretim kodu yok (K10'a ek) → şart boş küme üzerinden hep doğru                                                                                                     | `autonomy_unlock.py:74-97`; grep yalnız `autonomy_ladder.py:84` (observe_success)                 | EKSİK-KOD                 |
| N12 | Merdiven "N ardışık temiz cycle / 30 observe" (NASIL §15, MIMARI Şema 7)                                                                                                                    | L1'in 30 observe'u tek burn-in iş koşusunda (`--cycles 30`) dakikalar içinde yazılabilir; açık-uç bayatlık (`now`) üretimde hiç geçirilmiyor                                                        | `aria-auto-cycle.yml:848-856,881`; `autonomy_unlock.py:167-177,229-247`                           | EKSİK-DOKÜMAN + EKSİK-KOD |
| N13 | Droplet daemon'ları: planner-dispatch · worker-scheduler (MIMARI Şema 0/8/16)                                                                                                               | Repoda bu ikisi için servis birimi yok (yalnız `aria-gateway`, `aria-egress-proxy`, `aria-telemetry`); fiilen orkestratör içi sınırlı drainer                                                       | `infrastructure/aria/`; `autonomy_orchestrator.py:1008-1016`                                      | TUTARSIZ                  |
| N14 | Deadline ≥1800 s → ARIA_STOP her döngüde (MIMARI Şema 1)                                                                                                                                    | Kontrol yalnız tam CONVERGED→review→merge yolunun sonunda; 4 `continue` dalı atlıyor                                                                                                                | `autonomy_orchestrator.py:2754-2781` vs `:1824-1852,1926-1978,1994-2030,2442-2473`                | KAYMA                     |
| N15 | artifact_safety "her artefakt yazımı öncesi + 1MB tavan" (MIMARI Şema 22)                                                                                                                   | 6 çağıran; verification_gate tavanı ≥2 MB'a çıkarır                                                                                                                                                 | `verification_gate.py:337`                                                                        | KAYMA                     |
| N16 | NASIL §11 maliyet "$0.50 / $5 / $100"                                                                                                                                                       | Bu üçlü iki ayrı sistemin karışımı: `budget.DEFAULT_BUDGET` (günlük 5 / aylık 100 / eylem 1) + `cost_budget` per-run 0.5                                                                            | `budget.py:185-191`                                                                               | KAYMA                     |
| N17 | (önceki rapor §3.5) "`maintenance_utility` dispatch edilemez"                                                                                                                               | Gece drain'i `maintenance_utility`'yi dağıtıyor; aria/state'te 24 accepted sonuç (2026-08-05 → 2026-09-21), 86 istek                                                                                | `ci_executor_drain.py:319-334`; `origin/aria/state:tools/agent-invocations/results.jsonl`         | Önceki rapora DÜZELTME    |
| N18 | Kod yorumları: `agent_resolver` "\_maintenance = aria-primary/challenger-planner"; spine "four invariants / auth stub"; batch_containment "16 hook"; auto_action_gate "`Gate.permit_merge`" | Planner'lar kökte; 5 değişmez, auth adaptör okur; 18 hook; `permit_merge` yok, merge bu kapıyı kullanmıyor                                                                                          | `agent_resolver.py:17-22`; `architecture_spine_gate.py:5-26`; `auto_action_gate.py:8-19`          | KAYMA (kod yorumu)        |

## Bağlı olmayan / ölü / stub kod

- `auto_merge_runners.evaluate_v9_implementation_merge` her zaman
  `eligible=False/v9_merge_path_disabled` (stub, `:640-685`); `verify_branch_tip`, `poll_pr_checks`,
  `compute_v9_idempotency_key` yalnız test.
- `apply_engine.plan_apply_worktree`, `latest_ready_apply_action`,
  `parse_plan_converged_approval_ref` yalnız test; `_read_diff_from_action`'ın staged/unstaged
  dalları: `allow_dirty_worktree`'yi hiçbir üretici yazmıyor → kirli ağaç reddedilir, temizde bu iki
  diff hep boş.
- `autonomy_orchestrator`: `cost_telemetry_hook`, `profile_gate`, `max_budget_usd_per_cycle`,
  `max_budget_usd_per_run` ölü parametreler (N2/N3).
- `budget.reserve_cycle_budget` + `reconcile_envelope_cost` zinciri üretimde ölü (N3);
  `remaining_reservation_budget` yalnız test.
- `auto_action_gate`: `AUTONOMOUS_AUTO_ACK_LANES = frozenset()` → `mint_auto_ack` dalı erişilemez;
  breaker/cost girdileri kararı değiştiremez.
- `autonomous_worker_scheduler.max_workers` kullanılmıyor; `merges_completed` sayacı ölü (hook
  merge'i sabit `blocked`, `worker_dispatch_hook.py:453-458`).
- `aria_watchdog.run_aria_watchdog_daemon` yalnız CLI, dağıtım birimi yok; runtime pull
  `source_unconfigured` (N6).
- `autonomy_state`: `human_required_count` hiç yazılmıyor (her zaman 0); `AUTONOMY_PHASES` hiçbir
  yerde okunmuyor.
- `agent_surface`: `"EXPIRED"` `REMINT_ELIGIBLE_DEAD_STATES`'te ama türetilen durumlarda yok;
  `gap_finding`/`gap_closure` rolleri mint eden yok.
- `architecture.py` tamamen operatör-CLI; otomasyondan çağrılmıyor.
- `autonomy_evidence`: `_mode_a_signed_readiness_live_proven` v3 sözleşme arar, CAPABILITY_SPECS'te
  yalnız v2 var → fonksiyon her zaman False (aşağıda R2).

## Hata / risk gözlemleri

- **R1 — Publish reddinin mimari kökü (B1'e ek, yeni).** `_verify_published_snapshot_commit` state
  publish'i kapılar ve 7 yeteneğin `count_surfaces` birleşimindeki her ledger'a ≤64 MB / ≤100 000
  satır / toplam ≤80 MB bütçesi uygular (`autonomy_evidence.py:593-596,2543-2554`). `raw_findings`
  yalnız `finding_funnel`'ın teşhis sayacı `raw_unique_fingerprints` için sayım yüzeyi
  (`:1027-1032,3031`). Yani salt teşhis amaçlı bir sayaç tüm aria/state yayınını reddettiriyor;
  compactor bu bağı değil yalnız boyutu hedefliyor. Satır tavanı 100 000 olduğundan ham bulgu
  büyümesi sürerse kapı kalıcı kapanır.
- **R2 — `enterprise_readiness` kalıcı `operator_blocked`.** `_apply_operator_prerequisites` her
  türetmede `_mode_a_signed_readiness_live_proven`'ı ister (`:3910-4008`); o da
  `aria/enterprise-readiness-claim/v3` sözleşmesi arar (`:3626-3653`), spec'te yalnız v2 var
  (`:1165-1178`). v3 üreticisi yok (grep: yalnız test + plan dokümanı). GitHub App kurulsa bile
  blokaj kalkmaz.
- **R3 — 4/7 yetenek hiçbir zaman `live_proven` olamaz.** `finding_funnel`, `fixture_calibration`,
  `autonomy_unlock` sözleşmelerinde `authoritative_sha_field=None` → her terminal satır
  `proof_target_sha_unavailable` (`:1287-1292`); `pre_merge_perimeter` `contracts=()`. Hiç `code`
  sözleşmesi yok → `code_proven` de erişilemez.
- **R4 — Auto-merge yol sınıflandırıcısı `.`-önekli yolları bozuyor.** `_normalize_path`
  `lstrip("./")` baştaki tüm noktaları siler (`auto_merge.py:1245-1246`) → `.github/workflows/**`,
  `.github/actions/**`, kök `.env*` hard-forbidden globları ölü. Canlı çalıştırma:
  `.claude/agents/aria-implementer.md` → `risk_class=low`, enterprise lane **L1**;
  `.github/workflows/README.md` → low/L1. `.github/*.yml` yalnız "unknown" olduğu için bloklanıyor.
- **R5 — Auto-merge "tüm check battery yeşil" sayfalamasız.** `GhCliGitHubAdapter.get_checks`
  `gh api …/check-runs` sayfalamıyor (`auto_merge.py:856-864`; API varsayılanı 30) → 30'dan fazla
  check-run olan bir PR'da sonraki sayfalardaki kırmızı non-required koşular görünmez.
- **R6 — Spine postcheck ya kör ya sürekli regresyon.** CI'da auth/harness hep `pending` (N7).
  Yollar çakışırsa `adapter_run_id`/`adapter_recorded_at` string farkı `detect_drift`'te
  `regression` sayılır (`architecture_spine_gate.py:303-307,656-659`) ve postcheck adaptörü önce
  tazelediği için her remediasyon sonrası regresyon → 5 turda HUMAN_REQUIRED.
- **R7 — Spine `schema_entity` ADR-011'e aykırı sayıyor.** Şemasız `@Entity('t')` ihlal sayılır
  (`:228-229`); repoda 194 meşru per-tenant varlık bu sayıya giriyor (farm 95, sensor 46, hr 29,
  messaging 15, ai 4, alert 4, hydroponics 1). Doğru yeni per-tenant tablo "regresyon" olur.
  `_SHARED_SCHEMA_TABLES` el-kopyası kaymış: `user_permissions` var, SSoT'taki `access_logs` yok
  (`:124-129` vs `libs/backend-common/src/constants/protected-tables.ts:101-104`).
- **R8 — aria_watchdog daemon modunda stall dedektörü kör.** Her iterasyon kendi governance satırını
  yazıp `since`'i ona taşır (`aria_watchdog.py:798-806,709-712`) → plan olayları pencereye girmez;
  ≥600 s yaş şartı ≤60 s pencerede sağlanamaz. Terk edilmiş planlar (`plan_abandoned`) terminal
  sayılmıyor (`:274-277`).
- **R9 — Strict/autonomous'ta tek bekleyen köprü satırı tüm geceyi durdurur.** `pending_after>0` →
  `bridge_replay_required` ile çıkılır, yakınsama/merge koşmaz
  (`autonomy_orchestrator.py:1700-1732`); drain gece başına ≤10 satır.
- **R10 — İdempotensi / kilit açıkları.** `autonomy_ladder._recorded_observe_reasons` her istisnayı
  yutup `set()` döner → okuma hatasında burn-in cycle'ları ikinci kez sayılır (`:112-118`). Host ve
  "CAS" kiraları kilitsiz oku→yaz (`autonomous_host_lease.py:274-301,352-398`); 5 dk kira,
  orkestratörde yalnız preflight'ta yenileniyor.
- **R11 — Ledger disiplini dışındaki yazıcılar.** `aria_watchdog_signatures.jsonl`
  (`aria_watchdog.py:486-488`) ve `budget-ledger.jsonl` (`budget.py:374-403`) ham `open("a")` ile
  yazılıyor; okuma bozuk satırı sessizce atlıyor; ikincisi kendi zincirini hiç doğrulamıyor (bozuk
  satır → "harcanan" düşer).
- **R12 — Queue projeksiyonu geniş kapsam.** next_cycle_queue öğesi
  `maintenance_utility`/`aria-autonomy-planner` isteğine
  `allowed_scope=["aria-kernel/**","aria-tools/**",".claude/**"]` ve kanıt yoksa
  `evidence_refs=[queue_item_id]` ile dönüşür (`autonomy_orchestrator.py:298,381-401`).
- **R13 — `verify_plan_converged_approval` zayıf eşleşme.** Makine onayındaki hash, CONVERGED olan
  revizyonla değil planın herhangi bir olayındaki `content_hash` ile eşleştiriliyor
  (`apply_engine.py:863-874`).
- **R14 — Staging `architectural_tier` beyanını zorunlu tutuyor.** Bu alanı taşımayan CONVERGED plan
  PR'a hiç ulaşamaz (`apply_engine.py:722-733`); dokümanda yok.
- **R15 — `autonomy_evidence` kanıt tazeliği kırılgan.** Ortak authority dosyalarına (ledger.py,
  tool*registry.py, workspace.py, state*\*, autonomy_evidence.py) ya da yetenek dosyalarına
  (cycle.py, autonomy_orchestrator.py, ci_executor.py…) dokunan her main commit o yeteneğin tüm
  geçmiş canlı kanıtını `proof_authority_changed` ile geçersiz kılıyor (`:830-845,1990-2000`).

## Hiçbir dokümanda olmayan önemli davranışlar

- State publish'in `autonomy_evidence` yetenek sayım bütçelerine bağlı olması (R1). Şema 6/23'e
  "publish verifier = kanıt sayım bütçesi" kutusu eklenmeli.
- 7 yetenekli kanıt modeli (declared / code_proven / live_proven / operator_blocked), kapanış
  politikası JSON'unun kodda sabit tabloyla birebir eşleşme zorunluluğu
  (`autonomy_evidence.py:73-583,3709-3907`).
- `glm-5.3` kademe sırasında opus'un ÜSTÜNDE (`agent_runtime_profile.py:63`): opus, glm'nin yazdığı
  ajanı değiştiremez, glm ajan yazabilir; bilinmeyen model hedefte en güçlü, aktörde en zayıf
  sayılır.
- Oybirlikli AI panel satırı ground-truth anchor olabiliyor (`belief_escalation.py:139-141`).
- Planner daemon yalnız `primary_plan/challenger_plan/cross_review` claim ediyor
  (`autonomous_planner_dispatcher.py:78-88`); diğer roller gece drain'inin rol-kota sırasıyla
  (`ci_executor_drain.py:319-334`).
- Aday PR listesi = readiness-claims ledger'ındaki tüm tarihsel PR'lar; kapalı PR'lar her koşuda
  yeniden `blocked` karar satırı üretiyor (`auto_merge_runners.py:322-339`).
- Pressure `satisfied` geçişi, genesis `pr-lanes.jsonl` `ready_for_pr` (merge değil) ile
  tetikleniyor (`agent_satisfaction.py:186-192`).

## RAG / kanıt modeli ile ilgili not

- Bu gruptaki modüllerde embedding / vektör yok. Kanıt, git blob / tree hash ve ledger hash
  zinciriyle bağlanıyor (`autonomy_evidence` authority hash'leri, `_git_tree_entry`).
- İstisnalar: (a) `architecture.py` `evidence_refs`'i hiç doğrulamıyor (`:317-318`). (b) Watchdog
  bulguları kanıt olarak ARIA'nın kendi `aria-tools/governance.jsonl`'ını gösteriyor
  (`aria_watchdog.py:322-326`). (c) Oybirlikli AI panel satırı ground-truth sayılabiliyor (R/N10).
  Bu üçü "LLM/ARIA çıktısı hakikat değil" ilkesinin kenar durumları.

## Okuma kapsamı

- 26/26 dosya okundu.
- Büyük dosyalarda (autonomy_evidence, autonomy_orchestrator, budget, bridge_status_ledger,
  belief_escalation, auto_merge_runners bölümleri) salt yorum satırları `grep -v '#'` ile süzülerek
  okundu; kod satırlarının tamamı okundu.
- `autonomy_evidence.py:200-583` (sabit kapanış-politikası tablosu) yapısal olarak tarandı; SHA
  değerleri tek tek doğrulanmadı.

---

## Ek: dosya dosya ara notlar

### B02 — tam okuma raporu (ara notlar; son hali en altta derlenecek)

## Ara notlar (dosya dosya)

### agent_resolver.py (64)

- `resolve_agent_md_path`: root → `_maintenance/` → `product-audit/` sırası. Üretim:
  `context_budget_gate.py:62`, `surface_manifest_validator.py:67`, `service_agent_targeting.py:40`.
- Docstring `agent_resolver.py:17-22` "\_maintenance/ = aria-primary-planner,
  aria-challenger-planner" diyor; gerçekte bu iki ajan `.claude/agents/` kökünde; `_maintenance/` =
  aria-drafter, aria-prompt-writer, gdpr-erasure-executor, implementation-planner, prompt-writer.
  (kod yorumu KAYMA)
- İkinci bir çözücü var: `agent_runtime_profile._find_agent_file`
  (`agent_runtime_profile.py:187-195`) root sonra `**/` glob (alfabetik ilk) — farklı arama sırası;
  docstring'in "kopya çözücü drift eder" uyarısının tam kendisi (iki SSoT).

### agent_routing.py (129)

- `.claude/shared/orchestrator-routing-table.md` markdown tablosunu parse eder; servis →
  primary/also_notify ajan; primary boş → coverage gap.
- Üretim: `impact_graph.py:292-296` (recommended_agents), `capability_gap.py:328`
  (`unowned_projects` → agent-genesis adayı). Şema 14 (service_examination) / Şema 13.
- Tablo biçimine kırılgan: yalnız `| File Pattern` başlığı atlanır, hücre ayrıştırması regex; dosya
  yoksa `[]` → her proje "unowned" sayılır (`agent_routing.py:110-111,184`) → tablo
  silinirse/taşınırsa tüm servisler capability gap olur (sessiz).

### agent_runtime_profile.py (312)

- Frontmatter/kernel `runtime_profiles.json` → (model, effort) SSoT. Üretim:
  `ci_executor.py:1708,1751,1997,3759,4710`, `worker_dispatch_hook.py:238`,
  `agent_genesis.py:846-868`, `belief_escalation.py:165`, `native_admission.py:25`.
- `MODEL_TIER_ORDER = (fable, glm-5.3, opus, sonnet, haiku)` (`:63`): glm-5.3 opus'tan GÜÇLÜ sayılır
  → opus, glm-5.3'ün yazdığı ajanı değiştiremez (`:114`); glm-5.3 ajan yazabilir. MIMARI Şema 18
  "ajan yazma seviyesi ≥ opus" doğru ama glm-5.3'ün opus üstü sıralaması hiçbir dokümanda yok.
- Bilinmeyen model: hedef olarak en güçlü (-1), aktör olarak en zayıf (`:67-85`) — asimetrik;
  dokümanda yok.
- `_read_profile_cached` `lru_cache(256)` (`:209`): uzun ömürlü daemon'da
  frontmatter/`runtime_profiles.json` değişikliği süreç yeniden başlayana kadar görülmez.
- `belief_escalation.py:168` `repo_root` vermeden çağırır → `__file__`'a göre kök (kurulu
  paket/worktree farkı riski).

### agent_satisfaction.py (216)

- Öğrenme kancası `agent_satisfaction_scan` (üretim: `learning.py:95` → `cycle.py:617`
  `run_learning_pre_cycle`/post-evidence). `.claude/agents/*.md`, `product-audit/*.md`,
  `agents/aria-*.md` tarar; frontmatter `addresses_pressure: [PE-…]` olan ajan + (genesis
  `pr-lanes.jsonl` `status==ready_for_pr` VEYA `pressure_addresses_recorded` commit-trailer olayı) →
  pressure `satisfied`.
- `_maintenance/` dizini TARANMAZ (`agent_satisfaction.py:52`) — `agent_resolver` tarar;
  aria-drafter/prompt-writer silinse `agent_removed` olayı çıkmaz.
- Genesis kanıtı `ready_for_pr` (merge DEĞİL) ile pressure'ı kapatır (`:186-192,170-171`).
- `_parse_addresses_pressure` bozuk frontmatter'da `ValueError` atar (`:77,111`) ve `_scan_agents`
  guard_item ile sarılı değil → tek hatalı ajan dosyası tüm kancayı düşürür (kanca düzeyinde
  yakalanır, `learning.py:~115`).
- Şema: MIMARI'da yok (learning hook'ları hiçbir şemada tek tek adlandırılmamış).

### agent_surface.py (251)

- Rol/durum kapalı sözlükleri SSoT (çok sayıda tüketici).
- `REMINT_ELIGIBLE_DEAD_STATES` içinde `"EXPIRED"` (`:211`) var ama `DERIVED_REQUEST_STATES`'te
  (`:168-187`) yok → reducer hiç üretmiyor; ölü üye (aynı ölü üye
  `human_required_adjudication.py:1017`).
- `REQUEST_ROLES` `gap_finding`, `gap_closure` (`:26-27`) içeriyor; DISPATCHABLE'da yok ve kernelde
  mint eden yok (grep: yalnız `context_budget_gate.py:92,105` rol tavanı) — E14'ün sildiği 5 rolle
  aynı sınıf "kimsenin doldurmadığı rol" (yorum `:36-48` bu sınıfı "yanlış" ilan ediyor).
- `DEFAULT_TARGET_AGENT_WHITELIST` (`:118-142`) `aria-cross-reviewer`'ı içermiyor; üretim
  cross_review mint'i `cross_review_bridge.py:64,219` bu ajanı hedefler. Whitelist yalnız
  `CONTRACT_ENFORCED_ROLES={"implementation"}` için uygulandığından (`agent_contract.py:120`,
  `agent_invocations.py:1568`) bugün kırılmıyor; kontrat genişletilirse cross_review kendiliğinden
  reddedilir.

### apply_engine.py (1056)

- Apply-action yüzeyi: `stage_converged_plan_for_pr` (üretim: `cycle_phases/implementer.py:241`),
  `run_apply_gate` (üretim: `implementation_delivery.py:765`, CLI `cli.py:5141`),
  `gate_apply_action`, `verify_plan_converged_approval` (üretim: `pr_manager.py:290`). Şema 17.
- **MIMARI Şema 17 sıra iddiası "BASELINE doğrulama ÖNCE koşulur → change_planned → makine onayı"**:
  kodda sıra `emit_change_planned` (`:743`) → baseline `run_validation_commands` (`:763`,
  change_id'ye ihtiyaç duyar) → `record_proposal` (`:776`) → `record_machine_approval` (`:803`) →
  apply action `staged_for_implementation` (`:809`). change_planned baseline'dan ÖNCE.
- **Operatör şeridi (Şema 17 A1) üretimde bağlı değil**: `plan_apply_worktree` (`:69`) yalnız
  testlerden çağrılıyor (CLI'da da yok; grep: `tests/test_enterprise_*`,
  `test_autopr_foundation.py`). Ayrıca `latest_ready_apply_action` (`:385`) ve
  `parse_plan_converged_approval_ref` (`:411`) yalnız test.
- Staging, planın `architectural_tier` beyanını ZORUNLU kılar (`:722-733`), yoksa GovernanceError —
  CONVERGED plan bu alanı taşımazsa PR'a hiç ulaşamaz; dokümanda yok.
- `_read_diff_from_action` docstring'i "base..branch + branch..HEAD + --staged" der (`:306-310`),
  kod `base..branch + --staged + git diff` koşar (`:361-365`); `branch..HEAD` sürüklenmesi kontrol
  edilmez. Kirli ağaç `allow_dirty_worktree` yoksa reddedilir (`:345-359`) ve bu alanı hiçbir
  üretici yazmıyor (grep) → üretimde staged/unstaged diff'ler her zaman boş; birleşimin 2/3'ü ölü.
- `verify_plan_converged_approval` (`:863-874`) makine onayının hash'ini planın HERHANGİ bir
  olayındaki `content_hash`'e karşı arar; CONVERGED olan revizyonla eşleşme şartı yok (doküman
  "revizyon içerik hash'i eşleşmeli" der — Şema 17 PRM).
- Profil kapıları: `plan_stage` (`:684`), `apply_gate` (`:908`).

### architecture.py (464)

- Mimari karar incelemesi (fix_in_place/harden/abstraction/replace_with_adr…), seçenek seti, 5-kanıt
  paketi, ADR taslağı. **Yalnız operatör CLI** (`cli.py:4830-4870`); cycle/orkestratör/workflow
  çağıranı yok. Şema 14/17 kapsam tablosunda "architecture\*" olarak geçer, davranışı hiçbir şemada
  yok.
- `evidence_refs` hiç doğrulanmaz (yalnız boş olmayan string, `architecture.py:317-318`);
  `adoption.level` kanıt sayısı/proje sayısından türetilir (`:331-345`) — kanıt modeli
  (`repo_verified`) burada uygulanmıyor.

### architecture_spine_gate.py (854)

- Üretim: cycle fazları `architecture_baseline` → `_take_cycle_baseline` (`cycle.py:2812-2820`),
  `architecture_postcheck` → `take_postcheck` (`cycle.py:2823-2836`, regresyon → faz `fail`);
  `_plan_comparison_obligation` → `must_satisfy.py:192`; CLI `cli.py:5334-5349`. Şema 14.
- **auth_security + harness_security ölçümleri üretimde yapısal olarak `pending`**: iki kontrol de
  `workspace_root/"aria-tools"/"runs.jsonl"` sabit yolunu okur (`:275,321`), ama
  `refresh_spine_adapters` koşuları `base_dir` (tools_dir) altına yazar (`:470-479`). CI'da
  `ARIA_TOOLS_DIR` store-resident (`.github/actions/restore-aria-state/action.yml:84-88,171`) →
  repo-içi `aria-tools/runs.jsonl` yok/boş → `stub:no_runs_ledger`. MIMARI Şema 14 "B4
  auth_security: adaptör koşusu · B5 harness_security: adaptör koşusu" → TUTARSIZ.
- **Yollar çakışırsa (yerel) tersine bozulur**: bu iki kontrolün `measurements`'ı
  `adapter_run_id`/`adapter_recorded_at` string'lerini içerir (`:303-307,354-358`); `detect_drift`
  sayısal olmayan her farkı `regression` sayar (`:656-659`); postcheck önce adaptörleri tazeler
  (değişen repo_state → yeni run_id) → remediasyon sonrası her postcheck en az 2 "regression" üretir
  → 5 turda HUMAN_REQUIRED (`:769-791`).
- `_SHARED_SCHEMA_TABLES = {audit_logs, gdpr_data_requests, user_consents, user_permissions}`
  (`:124-129`) — kanonik SSoT `libs/backend-common/src/constants/protected-tables.ts:101-104` =
  `{audit_logs, gdpr_data_requests, user_consents, access_logs}`. `user_permissions` ↔
  `access_logs` KAYMASI; CLAUDE.md "kopya tutma" kuralına aykırı el-kopyası.
- `_check_schema_entity` şemasız `@Entity('tablo')`'yu ihlal sayar (`:228-229`); ADR-011'e göre
  tenant-servislerindeki per-tenant tablolar şemayı BİLEREK atlar → repoda 194 meşru varlık (farm
  95, sensor 46, hr 29, messaging 15, ai 4, alert 4, hydroponics 1) "missing_schema_violation"
  sayılıyor; doğru yeni per-tenant varlık eklemek "regresyon" olur.
- Modül docstring'i hâlâ "four invariants" + auth "stub, Phase 6 dolduracak" (`:5-26`); kod 5
  değişmez ve auth adaptör okur. KAYMA (kod yorumu).

### aria_watchdog.py (862)

- Dedektörler: stall (600 s), tekrarlayan `agent_bridge_warning` (≥3/600 s), runtime `/metrics` 5xx
  payı + kritik güvenlik olayı. Üretim: cycle fazı `watchdog_sweep` (`cycle.py:1150,3170-3177`,
  `record_and_continue`); daemon `run_aria_watchdog_daemon` yalnız CLI (`cli.py:4335`) — hiçbir
  workflow/systemd/script başlatmıyor (grep boş).
- **Runtime pull üretimde ölü**: `genesis_policy_default.json:99-104`
  `observability_base_url: null`, repo override'ı yok → her cycle
  `runtime={"skipped":"source_unconfigured"}` (`aria_watchdog.py:727-728`). MIMARI Şema 7 GUARDS
  "5xx oranı" → EKSİK-KOD (bağlı değil).
- **Daemon modunda stall dedektörü yapısal olarak kör**: her iterasyon önce
  `aria_watchdog_iteration_started` governance satırı yazar (`:798-801`), sweep
  `latest_governance_ts`'yi bu satırı da içeren kümeden alır (`:709-712`), sonraki iterasyon yalnız
  `ts >= since` satırları okur (`:804,706`) → plan olayları pencereye hiç girmez; ≥600 s yaş şartı
  da ≤60 s pencerede sağlanamaz. Yalnız cycle-fazı modu (24 s pencere) çalışır.
- Daemon her 60 s'de 1 governance satırı (+ start/exit) yazar → daemon açılırsa governance.jsonl'a
  günde ~1440 gürültü satırı.
- Stall "plan" kimliğini governance satırlarından türetir (`:252-261`); plan olayları
  `plans/events.jsonl`'dadır; `plan_abandoned` terminali tanınmaz (yalnız
  `convergence_resolved/abandoned`, `human_required_recorded`, `:274-277`) → terk edilmiş planlar
  her cycle "stall" adayı (dedup tavanı 10/24 s imza, 30 global, `:450-455`).
- Dedup defteri `aria_watchdog_signatures.jsonl` ham `open("a")` + flock ile yazılır (`:486-488`) —
  hash-zincirsiz, `append_declared_jsonl` dışı; okuma bozuk satırı sessizce atlar (`:414-416`). "Her
  şey hash-zincirli deftere" değişmezine (MIMARI Değişmez 1) istisna.
- Stall kanıtları `aria-tools/governance.jsonl` / `plan:<id>` ref'leri (`:322-326`) — ARIA'nın kendi
  çıktısı; `emit_finding` reddederse sessizce `aria_watchdog_emit_rejected` (`:565-574`).
- **MIMARI Şema 7 `WD -.donma.-> M2`** (aria_watchdog → merge dondurma) TUTARSIZ: merge dondurma
  `watchdog_freeze.assert_merge_not_watchdog_frozen` (`merge_authority.py:29,59`) HARİCİ watchdog'un
  GitHub incident issue'larını okur; aria_watchdog bulguları merge'e hiç bağlanmaz
  (`watchdog_freeze.py:1-40`).

### artifact_safety.py (86)

- `secret_scrub` desenleriyle metin/JSON temizleme + `write_sanitized_json` (1 MB tavan,
  tmp→replace, fsync YOK). Üretim: `verification_gate.py:337` (tavanı `max(2 MB, 2×boyut)` ile
  EZER), `runtime_artifacts.py:16`, `tool_runner.py:22`, `agent_eval.py:330`,
  `tools/aria-poc/ci_executor.py:815-822`, `dispatch_failure.py:325`.
- MIMARI Şema 22 X3 "her artefakt yazımı öncesi temizleme + 1MB tavan" abartılı: yalnız 6 çağıran;
  verification_gate tavanı dinamik ≥2 MB yapıyor. KAYMA.

### batch_containment.py (92) · bridge_exceptions.py (32)

- `guard_item`/`with_item_failures` (21 modülde kullanılıyor); `LedgerIntegrityError` bilinçli
  kapsanmaz (MIMARI Şema 20 notu DOĞRU). Docstring "on altı learning hook" der; `learning.py` hook
  listesi 18 — kod yorumu KAYMA.
- `BridgeContractViolation`: üretim `cross_review_bridge.py:316,794`, `agent_invocations.py:4171`
  yeniden fırlatır. DOĞRU.

### auto_action_gate.py (321)

- Genesis materialize için tek-kullanım ack token kapısı. Üretim: yalnız operatör CLI
  (`cli.py:5615-5620,5777-5782`) → `agent_genesis.py:621`, `skill_genesis.py:697`.
- Docstring (`:8-19,76-78`) "hem materialize hem `merge_if_green` (`Gate.permit_merge`) aynı kapıyı
  tüketir" der; **`permit_merge` diye metot yok** ve auto_merge/merge_authority bu modülü import
  etmiyor (grep). Merge tarafı ayrı sınıflandırıcıyla kalmış → docstring'in "iki bağımsız
  sınıflandırıcı birleşti" iddiası EKSİK-KOD.
- `AUTONOMOUS_AUTO_ACK_LANES = frozenset()` (`:60`) → `human_ack_required` her zaman True;
  `mint_auto_ack` dalı (`:170-189`) erişilemez; `breaker_state`/`cost_state` girdileri kararı hiç
  değiştiremez (sonuç zaten True). MIMARI Şema 15 "maliyet trip → AutoActionGate human_ack_required"
  işlevsel olarak etkisiz (her durumda ack zaten zorunlu). Şema 5 "operatör onayı her şeritte
  zorunlu" DOĞRU.

### auto_merge.py (1270)

- Auto-merge değerlendirici (`evaluate_auto_merge`), üçlü kapı (`_evaluate_triple_gate`:
  head==commit, change_validated, validation_runs doğrulaması + Gate 5 tamamlanmışlık + Gate 4
  hijyen bataryası), `GhCliGitHubAdapter` (tek-kullanım merge token, `--match-head-commit`). Üretim:
  `merge_authority`, `auto_merge_runners`, cycle `pr_lifecycle`. Şema 7/17.
- **`_normalize_path` = `path.replace("\\","/").lstrip("./")` (`:1245-1246`) baştaki TÜM `.`
  karakterlerini siler** → `.github/workflows/x.yml` → `github/workflows/x.yml`;
  `hard_forbidden_globs` içindeki `.github/workflows/**`, `.github/actions/**`, `**/.env*` (kökte)
  hiç eşleşmez (ölü). Canlı doğrulama (`python3 -c classify_changed_files`):
  `.claude/agents/aria-implementer.md` → `risk_class=low`, enterprise lane **L1**
  (`docs_tests_low_risk`); `.github/workflows/README.md` → low/L1. Ajan prompt'ları (ARIA'nın kendi
  harness'i) merge-yolu sınıflandırmasında düşük-riskli L1 sayılıyor; `.github/**` yalnız "unknown"
  olduğu için bloklanıyor, "forbidden" olduğu için değil. (READONLY_PATHS başka katmanda
  koruyabilir; merge sınıflandırıcısı korumuyor.)
- `GhCliGitHubAdapter.get_checks` (`:856-864`) `gh api repos/.../commits/<sha>/check-runs` sayfalama
  yapmıyor (GitHub varsayılanı 30/sayfa) → ORPHAN-717 "tüm batarya yeşil" kontrolü (`:337-356`)
  30'dan fazla check-run'da ilk sayfayı görür; sonraki sayfalardaki kırmızı non-required koşular
  görünmez.
- `get_latest_head_sha` (`:853-854`) `get_pr`'yi yeniden çağırır — aynı kaynak; force-push tespiti
  yalnız birkaç saniyelik pencere. Gerçek koruma `gh pr merge --match-head-commit` (`:955-957`).
- `DEFAULT_POLICY.enabled=False` (`:21`) → policy override yoksa her karar "policy disabled" ile
  blocked (aria/state'teki 8/8 blocked ile uyumlu).
- `_safe_call` her istisnayı varsayılana yutar (`:1261-1265`) — okunamazlık fail-closed (readable
  False) olarak yansır; DOĞRU yönde.

### auto_merge_runners.py (685)

- Profil→runner: `strict`/`autonomous` → `RealAutoMergeRunner` (strict = dry_run değerlendirme),
  diğerleri NoOp. Üretim: `cli.py:5850-5928` (orkestratör girişi). Şema 1/7.
- Aday PR'lar = `enterprise/readiness-claims.jsonl`'daki TÜM tarihsel `pr_number`'lar (`:322-339`);
  açık/kapalı süzgeci yok → her koşu kapanmış PR'ları yeniden değerlendirip
  `auto-merge-decisions.jsonl`'a yeni `blocked` satırı ekler (aria/state'teki 8 kararın 4 PR'a ait
  olmasıyla uyumlu: #1335 ×2).
- Watchdog dondurma koşu başına 1 kez sorulur, tüm adaylar blocked (`:183-221`); `merge_pr_if_ready`
  istisnaları `guard_item` ile aday başına kapsanır (`:247-275`). DOĞRU.
- **Ölü V9 bölümü** (`:395-685`): `evaluate_v9_implementation_merge` her zaman `eligible=False` /
  `v9_merge_path_disabled` döner (`:640-685`, stub); `verify_branch_tip`, `poll_pr_checks`,
  `compute_v9_idempotency_key` üretimde çağrılmıyor (grep: yalnız testler; `own_pr_ci.py:5` bunu
  "ölü" diye anıyor). `__all__` hâlâ ihraç ediyor.
- Bölüm yorumu "sec CRIT-003 — `.github/workflows/**` DEFAULT_POLICY hard_forbidden ile bloklu"
  (`:420-423`) — `_normalize_path` hatası yüzünden bu glob hiç eşleşmez (bkz. auto_merge.py).

### autonomous_host_lease.py (467)

- İki ayrı kira: (a) `HostLease` `locks/autonomous-host.lock` — üretim: orkestratör preflight yalnız
  `autonomous` profilde (`autonomy_orchestrator.py:663-670`), gateway daemon
  (`gateway/daemon.py:51-54,89`), workflow ön-kontrolü `lease_state`
  (`aria-auto-cycle.yml:405-420`); (b) `RemoteCasLease` `locks/autonomous-host.cas.json` — TEK
  üretim çağıranı `readiness_proofs.produce_remote_cas_proof` (`readiness_proofs.py:450-454`), yani
  merge-hazırlık kanıtı üretirken.
- **MIMARI Şema 15 HOST kutusu** "locks/autonomous-host.cas.json: epoch/owner/ref+sha CAS kirası …
  başka host fresh → fail-closed" olarak ORKESTRATÖR çoklu-host düzenini anlatıyor; orkestratör CAS
  kirasını değil `autonomous-host.lock`'u kullanır. CAS kirası yalnız readiness-proof üreticisinde.
  TUTARSIZ.
- **"CAS" gerçek CAS değil**: `acquire_remote_cas_lease` oku→karar→`tmp.replace` yazar (`:274-301`),
  dosya kilidi / atomik karşılaştırma yok → iki süreç aynı bayat kirayı okuyup ikisi de epoch+1
  yazabilir (TOCTOU). `acquire_lease` de aynı (`:352-398`). Modül docstring'i "NOT a mutex,
  trusted-witness" diye kabul ediyor (`:30-39`); "Tier-1 yapısal olarak imkânsız yarış" iddiası
  (`:12`) ile çelişik.
- Kira süresi 5 dk (`:67`), orkestratörde yalnız preflight'ta yenilenir; cycle bundan uzun sürer →
  cycle ortasında kira bayatlar, başka host alabilir.
- Yerel kira `host_id` GHA'da `RUNNER_NAME`'den (`:132-135`): iki farklı GHA runner'ı farklı host
  sayılır; workflow kontrolü `gha-runner:` önekli sahibi zaten bloklamaz
  (`aria-auto-cycle.yml:417-418`) → GHA↔GHA dışlaması tamamen `concurrency` grubuna kalıyor.

### autonomous_planner_dispatcher.py (286)

- Döngü: ARIA_STOP → `agent_claim` profil kapısı → `planner_dispatch_hook` → idle'da 30 s uyku.
  Üretim: orkestratörde `planner_drainer` olarak `max_iterations_per_phase` ile
  (`autonomy_orchestrator.py:1008-1012,1665-1669`); daemon modu CLI (`cli.py:4263-4273`). Şema 4.
- **Yalnız 3 rol** talep eder: `primary_plan, challenger_plan, cross_review` (`:78-88`).
  completeness_critique, judgment rolleri, implementation, verification vb. bu daemon tarafından
  claim EDİLMEZ (başka yollar: executor workflow). MIMARI Şema 4 "kuyruk:
  agent-invocations/requests.jsonl" tümünü kapsıyor gibi — rol alt kümesi dokümanda yok
  (EKSİK-DOKÜMAN).
- `status` `no_pending`/backoff dışındaki her sonuçta (ör. `failed`, `claim_failed`) UYKU YOK
  (`:235-251`) → kalıcı hata veren istek tekrar tekrar anında claim edilir (sıcak döngü;
  `max_iterations` yoksa sınırsız).
- Her tik 2 governance satırı (started+completed) → daemon boşta 30 s'de bir 2 satır ≈ 5 760/gün.
- **MIMARI Şema 0/8/16 "Droplet daemon'ları: planner-dispatch · worker-scheduler"**: repoda bu ikisi
  için systemd birimi yok (`infrastructure/aria/` = yalnız `aria-gateway.service`,
  `aria-egress-proxy.service`, `aria-telemetry.*`); gateway kodu da bunları başlatmıyor (grep boş).
  Fiilen yalnız orkestratör içinde sınırlı iterasyonlu drainer. TUTARSIZ.

### autonomous_worker_scheduler.py (300)

- Planner daemon'un ikizi; `worker_dispatch_hook.dispatch_one_pending_worker_assignment`. Üretim:
  orkestratör `worker_drainer` (`autonomy_orchestrator.py:1013-1016,2548-2553`), CLI
  `cli.py:4295-4310`.
- `max_workers` parametresi kabul edilir, loglanır, hiç kullanılmaz (`:92,106-111`) — ölü parametre.
- `merges_completed` sayacı ölü: hook'ta merge yolu sabit
  `blocked / enterprise_readiness_claim_id_required` (`worker_dispatch_hook.py:453-458`) →
  `"merged"` durumu erişilemez.

### autonomy_evidence.py (4166)

- İki rol: (1) `derive_autonomy_evidence_status` — hedef-SHA'ya bağlı, salt-okuma yetenek kanıt
  durumu (7 yetenek: cycle_runtime, executor, finding_funnel, fixture_calibration,
  pre_merge_perimeter, enterprise_readiness, autonomy_unlock); üretim çağıranı yalnız CLI
  `autonomy status --evidence` (`cli.py:6164`), hiçbir workflow çağırmıyor (grep). (2)
  `_verify_published_snapshot_commit` / `_verify_snapshot_and_collect_evidence` — **state publish'in
  kendisini kapılar** (`state_store.py:1132-1140,1759-1761,3590-3613`). Şema 6 (publish CAS) / Şema
  23-24; yetenek modeli hiçbir şemada yok.
- **B1 kök nedeninin mimari yeri (yeni)**: yayın doğrulayıcı, 7 yeteneğin `count_surfaces`
  birleşimine giren her ledger'a kanıt bütçesi uygular: yüzey başına ≤64 MB
  (`_MAX_EVIDENCE_LEDGER_BLOB_BYTES`, `:593`), toplam ≤80 MB (`:594`), ≤100 000 satır (`:596`) →
  `state_commit_surface_too_large:<yüzey>` (`:2543-2554`). `raw_findings` yalnız `finding_funnel`'ın
  SAYIM yüzeyi (`:1027-1032`) ve tek kullanımı `raw_unique_fingerprints` teşhis sayacı
  (`:3031-3032,3175`). Yani **yalnız teşhis amaçlı bir sayaç, tüm aria/state yayınını
  reddettiriyor**; 34 500 satırlık raw-findings büyüdükçe (satır tavanı 100 000) bu kapı kalıcı
  olarak kapanır. Compactor düzeltmesi bu bağı çözmüyor, yalnız boyutu küçültüyor.
- **4/7 yetenek yapısal olarak `live_proven` olamaz**: `finding_funnel` (promotions),
  `fixture_calibration`, `autonomy_unlock` sözleşmelerinde `authoritative_sha_field=None`
  (`:1009-1015,1060-1068,1207-1213`) → her terminal satır `proof_target_sha_unavailable` blokajı
  alır, hiç admissible olmaz (`:1287-1292`); `pre_merge_perimeter` `contracts=()` (`:1129`). Hiçbir
  sözleşme `proof_kind="code"` değil → `code_proven` durumu da erişilemez. `overall_state` = min →
  üst durum en iyi ihtimalle `declared`.
- **`enterprise_readiness` kalıcı `operator_blocked`**: `_apply_operator_prerequisites`
  (`:3910-4008`) `_mode_a_signed_readiness_live_proven`'ı ister; bu fonksiyon
  `schema_id == "aria/enterprise-readiness-claim/v3"` ve v=3 sözleşme arar (`:3626-3653`), ama
  `CAPABILITY_SPECS["enterprise_readiness"]` yalnız v2 sözleşmesi taşır (`:1165-1178`) →
  `signed_contracts` her zaman boş → her türetmede `github_app_mode_a_unconfigured` +
  `operator_blocked`. v3 üreticisi repoda yok (grep: yalnız
  `tests/test_autonomy_evidence_status.py:5328` ve plan dokümanı). Operatör GitHub App'i kursa da bu
  blokaj kalkmaz. EKSİK-KOD.
- Kanıt tazeliği kırılgan: her canlı kanıt, kanıt SHA'sındaki "authority" dosyalarının hash'i
  hedefinkine eşitse sayılır (`:1990-2000`); ortak authority listesi
  `ledger.py, tool_registry.py, workspace.py, state_*.py, autonomy_evidence.py` + yetenek dosyaları
  (cycle.py, autonomy_orchestrator.py, ci_executor.py…) (`:830-845`) → bu dosyalardan birine dokunan
  her main commit'i o yeteneğin tüm geçmiş canlı kanıtını geçersiz kılar
  (`proof_authority_changed`). Ayrıca çalışan `autonomy_evidence.py` baytları hedefteki blob'la
  birebir olmalı (`:1747-1788`).
- Kapanış politikası `docs/aria/policy/autonomy-closure-findings.json` kodda sabit tabloyla
  (`_EXPECTED_CLOSURE_POLICY_SEMANTICS/REFERENCES`, `:73-583`) birebir eşleşmek zorunda; yeni bulgu
  eklemek kod değişikliği ister (tasarım gereği, dokümanda yok).

### autonomy_ladder.py (209)

- Burn-in → merdiven köprüsü. Üretim: `aria-auto-cycle.yml:867-881` (`mode=burn-in-observe`
  dispatch'inde geçen rapordan sonra `record_burn_in_acceptance(mode="real")`), CLI `cli.py:6092`,
  `tools/aria-acceptance/burn_in_driver.py:51-59` (mock). Şema 7 (LADDER).
- Docstring "Operator-invoked, not automatic — run_observe_burn_in'den çağrılmaz" (`:141-145`):
  doğru ama workflow burn-in adımı bunu otomatik zincirliyor; "operatör" = workflow_dispatch seçimi.
- İdempotensi fail-open: `_recorded_observe_reasons` her istisnayı yutup `set()` döner (`:112-118`)
  → ledger okunamazsa aynı burn-in cycle'ları ikinci kez `observe_success` olarak yazılır (L1 sayacı
  şişer). Kilit altında değil (oku-sonra-yaz).
- Mock ledger `append_jsonl` ile (`:106`) — beyan dışı yüzey; bilinçli (yorum `:92-93`).
- (agent_surface ek) **`DISPATCHABLE_ROLES` tek dağıtım kapısı değil**: yorum
  (`agent_surface.py:73-77`) "executor bu kümeye göre claim eder" der; bu yalnız
  `ci_executor.claim_and_dispatch_one` (`--consume`, `ci_executor.py:1283-1287`) için geçerli. Gece
  drain'i `_ROLE_QUOTA_ORDER` içinde `maintenance_utility`'yi de (kümede YOK) dağıtır
  (`tools/aria-poc/ci_executor_drain.py:319-334`). Canlı kanıt:
  `origin/aria/state:tools/agent-invocations/results.jsonl` → 24 `maintenance_utility` **accepted**
  sonuç, 2026-08-05 → **2026-09-21**; 86 `maintenance_utility` isteği (hepsi
  `aria-autonomy-planner`, 47 Ağustos / 39 Eylül). Önceki raporun (§3.5) "maintenance_utility
  dispatch edilemez" ifadesi en az bu rol için canlı veriyle çelişiyor (genesis drafter gövdesi
  iddiası ayrı konu).

### autonomy_orchestrator.py (2832)

- Tam zincir sürücü; üretim: `aria-auto-cycle.yml:810-816` → `cli.py:5890-6030` →
  `run_autonomy_orchestrator`. Şema 1/16.
- **5 DI kancasından 2'si ölü**: `cost_telemetry_hook` ve `profile_gate` çözülür (`:1035-1040`) ama
  gövdede HİÇ çağrılmaz (grep: yalnız `:945-946,1035-1040`). CLI üretim fabrikası
  `select_cost_telemetry_hook(profile)` geçirir (`cli.py:~6005`) → yok sayılır; `profile_gate`
  CLI'dan hiç geçirilmez. MIMARI Şema 22 "cycle_phases/ 5 Protocol DI kancası … seçim profil
  SSoT'sundan" → 2'si için EKSİK-KOD.
- **Bütçe parametreleri yalnız loglanır**: `max_budget_usd_per_cycle=3.00`,
  `max_budget_usd_per_run=20.00` (`:955,960`; CLI `--max-budget-usd-per-run`, `cli.py:2391`) yalnız
  `autonomy_orchestrator_started` satırına yazılır (`:1159-1160`), hiçbir kontrol kullanmaz. Maliyet
  kapısı zaten `managed_subscription`'da etkisiz (önceki K9); bu ikisi ayrıca ölü.
- **Yakınsama tur sayısı dokümanla çelişiyor**: nightly `autonomy run` `--max-rounds` vermez → CLI
  varsayılanı **2** (`cli.py:2384-2389`, "V8 maliyeti için 4'ten indirildi"),
  `--challenger-timeout-seconds` varsayılanı 300 s (`cli.py:2377`); fonksiyon varsayılanı 4
  (`:931`). MIMARI Şema 0/3/16 ve NASIL §9.3 "≤5 tur / MAX_CROSS_REVIEW_ROUNDS=5"
  (`plan_convergence.py:92`) yalnız manuel `plan advance` yolunda. aria/state'teki 12/15 terk
  edilmiş planla ilişkili olabilecek bir sınır (nedensellik doğrulanmadı). TUTARSIZ.
- **next_cycle_queue projeksiyonu** her öğeyi `role=maintenance_utility`,
  `target_agent=aria-autonomy-planner` isteğine çevirir (`:389-401`), `allowed_scope` varsayılanı
  `["aria-kernel/**","aria-tools/**",".claude/**"]` (`:298`) — kernel ve ajan tanımlarını kapsayan
  geniş kapsam; kanıt yoksa `evidence_refs=[queue_item_id]` (kanıt olmayan bir kimlik, `:381-382`).
  Kuyruk boşaltması cycle'dan ÖNCE koşar (`:1502-1508`) → bu cycle'ın ürettiği öğeler bir sonraki
  koşuya kalır.
- Deadline → ARIA_STOP yazımı (`:2754-2781`) yalnız tam CONVERGED→review→merge yolunun sonunda
  kontrol edilir; `no_pressure`, `convergence_invalid_plan`, `convergence_blocked`,
  `specialist_review_blocked` dallarındaki `continue`'lar bu kontrolü atlar
  (`:1824-1852,1926-1978,1994-2030,2442-2473`). MIMARI Şema 1 "cycle_deadline ≥1800s? → ARIA_STOP"
  her döngü için geçerli gibi çiziyor → KAYMA.
- Maliyet/breaker preflight: failure breaker tüm aksiyon-yetkili profillerde, cost breaker + host
  kirası yalnız `autonomous` (`:583-672`). DOĞRU (Şema 1 D1-D3 "sadece autonomous" yalnız D1/D3 için
  doğru; D2 standard/strict'te de koşar).
- Queue remint soyağacı sayımı `qid in suggested_prompt` alt-dize eşleşmesiyle (`:265-268`,
  `:437-444`) — kimlik değil metin araması.

### autonomy_state.py (373)

- `autonomy_state.jsonl` reducer'ı. Üretim: orkestratör her geçişte
  `AutonomyStateReducer.transition`; `autonomy_evidence` akış sayacı; CLI status. Şema 1/6.
- `human_required_count` alanını HİÇBİR çağıran yazmıyor (grep: yalnız reducer'ın kendisi, `:296`) →
  `AutonomyState.human_required_count` her zaman 0; aria/state'te 114 HUMAN_REQUIRED dosyası varken
  durum yüzeyi 0 gösterir. Ölü alan.
- `AUTONOMY_PHASES` (`:49-84`) "keşif ipucu", kapalı değil ve kodda hiç okunmuyor (grep boş);
  orkestratörün yazdığı `memory_hook_recorded`, `v9_implementation_phase_started/resolved`,
  `bridge_replay_required`, `cost_breaker_tripped` vb. listede yok. Docstring "auto_merges_completed
  = auto_merge_completed satır sayısı" der (`:100`), kod delta toplar (`:193`). KAYMA (kod yorumu).

### autonomy_unlock.py (417)

- Kabul olayları ledger'ı, merdiven kararı, `assert_autonomy_unlocked` (merge_authority),
  `resolve_scheduled_profile` (üretim: `aria-auto-cycle.yml:599-615`). Şema 7.
- **"Açık uç" bayatlık kontrolü üretimde kapalı**: `verdict_from_rows(..., now=None)` iken
  `continuity_stale` hiç hesaplanmaz (`:167-177`); `evaluate_autonomy_unlock` `now` parametresi
  almaz ve geçirmez (`:229-247`) → yorumdaki "30 kusursuz cycle bir ay önce bittiyse kilit açılmaz"
  kuralı (`:168-170`) hiçbir üretim çağrısında devrede değil.
  `autonomy_evidence._stream_unlock_verdict_counts` da yalnız ardışık boşluğa bakar
  (`autonomy_evidence.py:3551-3554`). EKSİK-KOD.
- **L1 "30 ardışık gece" değil, tek iş**: L1 = 30 `observe_success`
  (`docs/aria/policy/autonomy-unlock.json`); burn-in adımı TEK workflow koşusunda
  `--cycles 30 --min-valid-cycles 20` çalıştırıp her geçerli cycle için bir olay yazar
  (`aria-auto-cycle.yml:848-856,881`) → birkaç dakika arayla 20–30 zaman damgası; 72 s boşluk kuralı
  (`:44,139-160`) bunları ardışık sayar. Docstring'in öncülü "N ardışık temiz cycle istikrarı
  gösterir / 17 günlük kesinti" (`:106-115`) 1–2 iş koşusuyla karşılanabiliyor. MIMARI Şema 7 LADDER
  / NASIL §15 "30 observe" gece sayısı gibi okunuyor → EKSİK-DOKÜMAN.
- **`critical_violation` üreticisi yok**: `record_acceptance_event` tek üretim çağıranı
  `autonomy_ladder.py:84` ve yalnız `observe_success` yazar (grep). "Sıfır kritik ihlal" şartı
  (MIMARI Şema 7 GATE, `:195-196`) boş küme üzerinden her zaman doğru — ihlali kaydeden kod olmadığı
  için. (K10'un kapsamadığı ek üye.) EKSİK-KOD.
- `record_acceptance_event` `row_id` zaman damgası içerir (`:83`) → aynı olay tekrar yazılırsa
  farklı id; idempotensi çağırana kalmış (bkz. autonomy_ladder fail-open).

### belief_escalation.py (276)

- `escalate_stuck_contradictions` (üretim: cycle fazı `cycle.py:1419-1421`) ≥3 farklı cycle'da açık
  çelişki → `HR-belief-<id>` HUMAN_REQUIRED; `execute_belief_panel_correction` (üretim:
  `human_required_adjudication.py:934-936`) panel kararını `false_positive` / `ai_consensus`
  feedback satırına çevirir. Şema 9/12.
- **MIMARI Şema 12 "panel oybirliği yalnız aşağı yönlü inanç düzeltmesi yapabilir"** KAYMA: aşağı
  yönlü düzeltmeyi herhangi bir _resolve quorum_ (2-1 dahil) yapar; oybirliği yalnız satırın
  "ANCHOR/ground truth" sayılıp sayılmayacağını belirler (yorum `:139-141`,
  `feedback_store.is_ground_truth_row`). Yani **oybirlikli bir AI paneli satırı ground-truth anchor
  olabiliyor** — "LLM hakem, hakikat değil" anlatısına istisna, dokümanda yok.
- "open" filtresi fiilen ölü: `memory.py` çelişki satırlarını yalnız `status: "open"` ile yazar
  (`memory.py:1288,1310,1332`), kapatan satır yok; sayım satır başına (fold değil) → bir inanç 3
  cycle çelişki kaydı aldıktan sonra kalıcı "stuck" (`:79-92`); tekrar tekrar aynı HR id'sine
  katlanır.
- `_panel_agent_model` her istisnayı yutar, boş model → gözlemci listeden düşer
  (`:167-174,270-274`); tüm modeller okunamazsa `observers=None` ile yazılır.

### bridge_status_ledger.py (431)

- Kabul edilmiş sonuçların köprü durumu + çökme kurtarma + `replay_pending_bridges` (üretim:
  orkestratör `_default_bridge_drainer`, `autonomy_orchestrator.py:141-172,1674-1720`). Şema 20 BS —
  "retry ≤3 → permanent_fail" DOĞRU (`:79-87,392-401`).
- `ledger_hash`/`envelope_evidence_hash` taşımayan eski satırlar köprü-zorunlu rolde bile
  `not_required` sayılır (`:192-200`) — sessiz uyumluluk yolu.
- `derive_bridge_state` her satır için köprü ledger'ını baştan yükler (`:148-167` her çağrıda
  `load_declared_jsonl`) → drain O(sonuç × ledger); `pending_after` hesabı ikinci tam tur.
- Orkestratörde `strict/autonomous` iken `pending_after>0` (retry bütçesi kalan tek satır bile) veya
  `status in {skipped,unknown,failed}` → tüm orkestratör koşusu `bridge_replay_required` ile biter,
  yakınsama/merge fazları o gece hiç koşmaz (`autonomy_orchestrator.py:1700-1732`); drain gece
  başına ≤`max_iterations_per_phase`=10 satır işler. Dokümanda yok (EKSİK-DOKÜMAN).

### budget.py (1003)

- Model fiyat tabloları + alias fiyatlama (`price_spawn_reservation`, üretim:
  `native_admission.py:287`, `ci_executor.py:2174`), native deneme rezervasyonu
  `_reserve_native_runtime_attempt` (üretim: `ci_executor.py:3160,3281,3422`,
  `ci_executor_judge_batch.py:362`), duvar saati (`record_run_wall_clock`, `WallClockExhausted` —
  üretim `ci_executor.py:1560-1621,4533-4562`), maliyet atıfı `cost-attribution/<ay>.jsonl`.
  Şema 15.
- **Üç ayrı bütçe sistemi yan yana**: (1) `DEFAULT_BUDGET` günlük $5 / aylık $100 / eylem başı $1,
  `budget/usage.jsonl` (`:185-191,321-322`) — tek üretim yazıcısı `llm_bridge.py:28` (operatör yanıt
  doğrulama) + CLI; (2) "per-run" ledger `budget-ledger.jsonl` $20/koşu, $3/cycle (`:343-345`); (3)
  `cost_budget` (genesis policy 0.5/25/300). NASIL §11'deki "$0.50 / $5 / $100" üçlüsü (1) ile
  (3)'ün karışımı (per-action $1 değil $0.50). KAYMA (önceki raporda yalnız (3)'e göre
  değerlendirildi).
- **`reserve_cycle_budget` üretimde hiç çağrılmıyor** (grep: yalnız testler) ve
  `ARIA_BUDGET_RESERVATION_TOKEN`'ı set eden kod yok → `ci_executor._try_reconcile_envelope_cost`
  her zaman erken döner (`ci_executor.py:1652-1654`). MIMARI Şema 15
  `AB -->|geçti| RESV[reserve_cycle_budget token=ledger_hash · reconcile sonrası] --> ATTR` akışı →
  EKSİK-KOD (rezervasyon/uzlaştırma zinciri ölü; yalnız cost-attribution yazımı canlı).
- Per-run ledger ledger disiplini dışında: kendi hash zinciri, ham `open("a")` (`:374-386`), okuma
  bozuk satırı sessizce atlar ve zinciri doğrulamaz (`:389-403`) — bir satırı bozmak "harcanmış"
  tutarı düşürür. `_per_run_remaining` "koşu başına" değil ledger ömrü boyunca tüm uzlaştırmaları
  toplar (`:406-414`).
- `record_cost_attribution` `signer_key_fp` yoksa `"SHA256:no-key"` sentinel'ini kabul eder
  (`:782-787`) — docstring'in "maliyet defteri sahteciliği H-5 kapatıldı" iddiası (`:759-766`)
  imzasız satırı reddetmiyor; %50 token sapması yalnız bayrak.
