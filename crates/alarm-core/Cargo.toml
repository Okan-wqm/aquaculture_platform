[package]
name = "alarm-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Pure alarm decision core (condition test, deadband hysteresis, delay gate) — the drift-zero SSoT shared by the Rust edge alarm engine and the TypeScript SCADA alarm engine."

[lib]
path = "src/lib.rs"

[dev-dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
