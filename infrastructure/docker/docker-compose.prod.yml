# =============================================================================
# Aquaculture Platform - Production Docker Compose
# DigitalOcean Droplet Deployment
# =============================================================================
# ARCH-006: THIS FILE IS A COPY — the canonical production compose is at the
# repository root: /docker-compose.prod.yml
# All changes must be made there. This copy exists only for documentation
# context within infrastructure/docker/. Keep in sync manually.
# =============================================================================
# Usage (from repo root):
#   docker compose -f docker-compose.prod.yml pull
#   docker compose -f docker-compose.prod.yml up -d
# =============================================================================

# =============================================================================
# Networks
# =============================================================================
networks:
  aqua-network:
    driver: bridge
  aqua-internal:
    driver: bridge
    internal: true

# =============================================================================
# Volumes
# =============================================================================
volumes:
  postgres_data:
  redis_data:
  nats_data:
  backup_data:

# =============================================================================
# Services
# =============================================================================
services:
  # ===========================================================================
  # Infrastructure Services
  # ===========================================================================

  # PostgreSQL with TimescaleDB
  postgres:
    image: timescale/timescaledb:2.17.2-pg16
    container_name: aqua-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aquaculture}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
      POSTGRES_DB: ${POSTGRES_DB:-aquaculture}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aquaculture}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7.2.7-alpine
    container_name: aqua-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD required}
    volumes:
      - redis_data:/data
    networks:
      - aqua-internal
    healthcheck:
      # SEC-014: Use REDISCLI_AUTH env var to avoid password in process list
      test: ["CMD-SHELL", "REDISCLI_AUTH=${REDIS_PASSWORD} redis-cli ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NATS Message Broker
  nats:
    image: nats:2.10.24-alpine
    container_name: aqua-nats
    restart: unless-stopped
    # ARCH-010/ARCH-018: Use config file for explicit JetStream limits + http_port
    # The config file enables http_port 8222 (required for /healthz endpoint below)
    command: ["--config", "/etc/nats/nats.conf"]
    volumes:
      - nats_data:/data
      # Path relative to repo root (this file is run with: docker compose -f docker-compose.prod.yml from repo root)
      - ./infrastructure/docker/nats/nats.conf:/etc/nats/nats.conf:ro
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===========================================================================
  # Backend Services (from GHCR)
  # ===========================================================================

  gateway-api:
    image: ghcr.io/okan-wqm/aquaculture_platform/gateway-api:latest
    container_name: aqua-gateway
    restart: unless-stopped
    environment:
      SERVICE_NAME: gateway-api
      NODE_ENV: production
      GATEWAY_PORT: 3000
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=384'
      # ARCH-V11-009: CORS_ORIGINS required — gateway throws if wildcard in production
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-1h}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
      TRUST_PROXY: "1"
      # Service URLs for federation
      AUTH_SERVICE_URL: http://auth-service:3000/graphql
      FARM_SERVICE_URL: http://farm-service:3000/graphql
      SENSOR_SERVICE_URL: http://sensor-service:3000/graphql
      HR_SERVICE_URL: http://hr-service:3000/graphql
      BILLING_SERVICE_URL: http://billing-service:3000/graphql
      ALERT_SERVICE_URL: http://alert-engine:3000/graphql
      ADMIN_SERVICE_URL: http://admin-api-service:3000/graphql
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      nats:
        condition: service_healthy
    networks:
      - aqua-network
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  auth-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/auth-service:latest
    container_name: aqua-auth
    restart: unless-stopped
    environment:
      SERVICE_NAME: auth-service
      NODE_ENV: production
      AUTH_SERVICE_PORT: 3000
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: 'false'
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-1h}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  farm-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/farm-service:latest
    container_name: aqua-farm
    restart: unless-stopped
    environment:
      # ARCH-V11-001: SERVICE_NAME identifies this service in NATS consumer groups
      # and structured logging. Without it, hostname is used as fallback.
      SERVICE_NAME: farm-service
      NODE_ENV: production
      # ARCH-V11-002: FARM_SERVICE_PORT is the canonical port env var read by main.ts.
      # PORT is also read as fallback. Both must agree with the healthcheck port.
      FARM_SERVICE_PORT: 3000
      PORT: 3000
      # ARCH-V11-003: NODE_OPTIONS caps V8 heap to prevent OOM kills inside the
      # container memory limit (384M in droplet, adjust as needed).
      NODE_OPTIONS: '--max-old-space-size=256'
      # ARCH-V11-004: CORS_ORIGINS is REQUIRED in production — farm-service main.ts
      # throws if this is missing when NODE_ENV=production.
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: 'false'
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      # ARCH-V11-005: ENCRYPTION_KEY is required for sentinel-hub and regulatory
      # credential encryption at rest. Service starts without it but those features fail.
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-}
      # ARCH-V11-006: INTERNAL_SERVICE_SECRET enables HMAC-signed inter-service auth.
      # Without it, ServiceIdentityGuard throws in production for GraphQL requests.
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      # ARCH-V11-007: Farm-service loads 71 entities and 25+ modules; bootstrap can
      # take 30-60s under memory pressure. 90s start_period prevents false negatives.
      start_period: 90s

  sensor-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/sensor-service:latest
    container_name: aqua-sensor
    restart: unless-stopped
    environment:
      SERVICE_NAME: sensor-service
      NODE_ENV: production
      SENSOR_SERVICE_PORT: 3000
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      # ARCH-V11-010: CORS_ORIGINS required — sensor-service throws if wildcard in production
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  hr-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/hr-service:latest
    container_name: aqua-hr
    restart: unless-stopped
    environment:
      SERVICE_NAME: hr-service
      NODE_ENV: production
      HR_SERVICE_PORT: 3000
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: 'false'
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  billing-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/billing-service:latest
    container_name: aqua-billing
    restart: unless-stopped
    environment:
      SERVICE_NAME: billing-service
      NODE_ENV: production
      BILLING_SERVICE_PORT: 3000
      PORT: 3000
      NODE_OPTIONS: '--max-old-space-size=256'
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SYNC: 'false'
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SERVICE_SECRET: ${INTERNAL_SERVICE_SECRET:-}
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  alert-engine:
    image: ghcr.io/okan-wqm/aquaculture_platform/alert-engine:latest
    container_name: aqua-alert
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      # ARCH-V11-011: CORS_ORIGINS required — alert-engine throws if wildcard in production
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      - postgres
      - redis
      - nats
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  notification-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/notification-service:latest
    container_name: aqua-notification
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      # ARCH-V11-012: CORS_ORIGINS required — notification-service throws if wildcard in production
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.suderra.com}
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      # Email config (optional)
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
    depends_on:
      - postgres
      - redis
      - nats
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  admin-api-service:
    image: ghcr.io/okan-wqm/aquaculture_platform/admin-api-service:latest
    container_name: aqua-admin-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-aquaculture}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_NAME: ${POSTGRES_DB:-aquaculture}
      DATABASE_SCHEMA: admin
      DATABASE_SYNC: 'false' # ARCH-DB-001: Production'da synchronize kapalı, migration kullanılmalı
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      NATS_URL: nats://nats:4222
      JWT_SECRET: ${JWT_SECRET}
      # Rate limiting: admin panel fires ~15 requests on page load
      THROTTLE_DEFAULT_LIMIT: 500
      THROTTLE_DEFAULT_TTL: 60
    volumes:
      - backup_data:/backups
    depends_on:
      - postgres
      - redis
    networks:
      - aqua-internal
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ===========================================================================
  # Frontend Services (from GHCR)
  # ===========================================================================

  shell:
    image: ghcr.io/okan-wqm/aquaculture_platform/shell:latest
    container_name: aqua-shell
    restart: unless-stopped
    networks:
      - aqua-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3

  dashboard:
    image: ghcr.io/okan-wqm/aquaculture_platform/dashboard:latest
    container_name: aqua-dashboard
    restart: unless-stopped
    networks:
      - aqua-network

  farm-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/farm-module:latest
    container_name: aqua-farm-module
    restart: unless-stopped
    networks:
      - aqua-network

  hr-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/hr-module:latest
    container_name: aqua-hr-module
    restart: unless-stopped
    networks:
      - aqua-network

  sensor-module:
    image: ghcr.io/okan-wqm/aquaculture_platform/sensor-module:latest
    container_name: aqua-sensor-module
    restart: unless-stopped
    networks:
      - aqua-network

  admin-panel:
    image: ghcr.io/okan-wqm/aquaculture_platform/admin-panel:latest
    container_name: aqua-admin-panel
    restart: unless-stopped
    networks:
      - aqua-network

  tenant-admin:
    image: ghcr.io/okan-wqm/aquaculture_platform/tenant-admin:latest
    container_name: aqua-tenant-admin
    restart: unless-stopped
    networks:
      - aqua-network

  aquamobil:
    image: ghcr.io/okan-wqm/aquaculture_platform/aquamobil:latest
    container_name: aqua-mobile
    restart: unless-stopped
    networks:
      - aqua-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ===========================================================================
  # Reverse Proxy (Nginx)
  # ===========================================================================
  nginx:
    image: nginx:1.27.5-alpine
    container_name: aqua-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/certbot:/var/www/certbot:ro
    depends_on:
      - gateway-api
      - shell
      - aquamobil
    networks:
      - aqua-network
    healthcheck:
      # ARCH-012: Use /health (nginx-level endpoint) — nginx has no /health/live path
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
