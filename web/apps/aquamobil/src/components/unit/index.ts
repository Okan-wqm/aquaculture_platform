/**
 * The read-only unit-detail sections, shared by the phone's TankDetailPage and
 * the tablet board's inspector pane. Neither owns them; both import them.
 */
export { UnitConfiguration } from './UnitConfiguration';
export { UnitVitals } from './UnitVitals';
