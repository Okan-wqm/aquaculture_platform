# Generated from platform service catalog.
# source: platform/libs/service-catalog/src/index.ts
# generator: scripts/service-catalog/generate-artifacts.ts
# generatorVersion: 2
# catalogHash: 3ecb0b77bbfa7abee6999df31022cbefc6fa5a6edf3310ea024cc499cd87d393
# do not edit by hand
version: 1
compose_files:
  - docker-compose.droplet.yml
runtime_required_env:
  - name: MINIO_USER
    owner: platform-service-catalog
    class: runtime-env
    purpose: "runtime deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "operator supplied per release"
    provisioning_mode: "deploy environment"
    required_by: [docker-compose.droplet.yml]
  - name: SERVICE_IDENTITY_SIGNING_KID
    owner: platform-service-catalog
    class: runtime-env
    purpose: "runtime deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "operator supplied per release"
    provisioning_mode: "deploy environment"
    required_by: [docker-compose.droplet.yml]
  - name: TAG
    owner: platform-service-catalog
    class: runtime-env
    purpose: "runtime deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "operator supplied per release"
    provisioning_mode: "deploy environment"
    required_by: [docker-compose.droplet.yml]
secrets:
  - name: ADMIN_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: AI_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: AI_TENANT_SECRET_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: ALERT_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: AUTH_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: BILLING_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: CONFIG_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: CONFIG_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: CREDENTIAL_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: EVENT_STORE_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: FARM_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: GATEWAY_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: HR_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: HYDROPONICS_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MESSAGING_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MFA_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MINIO_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_AUTH_SECRET
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_SENSOR_SERVICE_HASH
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: MQTT_SENSOR_SERVICE_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: NOTIFICATION_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: OBSERVABILITY_INTERNAL_API_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: OBSERVABILITY_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: PASSWORD_PEPPER
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: POSTGRES_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: REDIS_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: SENSOR_SERVICE_DB_PASS
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: SERVICE_IDENTITY_KEYRING
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: SUPER_ADMIN_PASSWORD
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
  - name: WEBHOOK_ENCRYPTION_KEY
    owner: platform-service-catalog
    class: secret
    purpose: "secret deploy variable; generated from active droplet compose and platform service catalog"
    rotation_intent: "rotate through deploy secret bootstrap or external vault"
    provisioning_mode: "first-run bootstrap or external secret manager"
    required_by: [docker-compose.droplet.yml]
