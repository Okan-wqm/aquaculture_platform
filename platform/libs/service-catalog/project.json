{
  "name": "service-catalog",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "platform/libs/service-catalog/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:lib"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx tsc -p platform/libs/service-catalog/tsconfig.lib.json --noEmit"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/platform/libs/service-catalog"],
      "options": {
        "jestConfig": "platform/libs/service-catalog/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["platform/libs/service-catalog/src/**/*.ts"]
      }
    }
  }
}
