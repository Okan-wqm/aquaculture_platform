[package]
name = "observability"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Tracing + OTLP + Prometheus initialisation, common across every Rust service. Aligns log/trace shape with NestJS @platform/backend-common so a single OTel collector ingests both."

[lib]
path = "src/lib.rs"

# [dependencies] intentionally empty during Faz 0 PR-A scaffold. tracing,
# tracing-subscriber, tracing-opentelemetry, opentelemetry, opentelemetry_sdk,
# opentelemetry-otlp, metrics + metrics-exporter-prometheus land alongside
# the init function + masking layer in Faz 2.

[lints]
workspace = true
