/**
 * Generated browser projection of farm mutation authorization. The generator
 * discovers actual useCanMutate calls and references the canonical role arrays;
 * this compatibility path intentionally contains no literal role mirror.
 */
export {
  FRONTEND_MUTATION_ROLES,
  type FrontendMutationName,
} from './farm-mutation-authorization.generated';
