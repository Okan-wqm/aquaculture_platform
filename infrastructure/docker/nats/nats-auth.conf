# =============================================================================
# NATS Authentication Configuration (disabled / no-op)
#
# This is the default (development) auth configuration — authentication is off.
# For production, replace this file with nats-auth-enabled.conf by mounting
# the enabled version at /etc/nats/nats-auth.conf in docker-compose.
#
# See nats-auth-enabled.conf for the production auth block.
# =============================================================================
# Authentication disabled — no authorization{} block
